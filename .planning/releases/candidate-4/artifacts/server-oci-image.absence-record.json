{
  "artifactName": "server-oci-image",
  "kind": "oci-image",
  "produced": false,
  "revision": "069df06570f7bc65ccb646935fd637dcc83bbd9c",
  "reason": "Nothing in this repository builds or pushes the server image to any registry, so there is no published digest to record or sign. This is a recorded owner decision, not an oversight (.planning/WINDOWS.md row 80). CHANGED AT CANDIDATE-3 IN ONE RESPECT, and unchanged since: the image-compose-deploy lane now PASSES (run 34777805271, cases=3), so an image IS built at this revision -- inside the lane, from the repository root context, and then discarded when the lane ends. That is a lane, not a released artefact: nothing retains those bytes, nothing names their digest, and no registry serves them. The absence recorded here is therefore unchanged in substance; what changed is that the reason is now solely the missing publish pipeline rather than the missing publish pipeline plus a lane that could not build the image at all. The product code at this revision is byte-identical to candidate-3's, so nothing about this absence could have changed between them.",
  "toClose": "Close WINDOWS.md row 80: decide the registry and push trigger, add the build+push pipeline, then sign and record the pushed digest.",
  "owner": "BACKLOG"
}
