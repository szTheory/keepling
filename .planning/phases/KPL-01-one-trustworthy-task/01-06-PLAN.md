---
phase: KPL-01-one-trustworthy-task
plan: 06
type: execute
wave: 4
depends_on: [01-05]
files_modified:
  - apps/server/lib/keepling/accounts.ex
  - apps/server/lib/keepling/accounts/account.ex
  - apps/server/lib/keepling_web/controllers/auth_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/lib/mix/tasks/keepling.setup_token.ex
  - apps/server/lib/mix/tasks/keepling.timezone.ex
  - apps/server/priv/repo/migrations/20260830000200_add_closed_setup_and_timezone.exs
  - apps/server/test/keepling/accounts/setup_timezone_test.exs
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/generated/keepling.ts
autonomous: true
requirements: [SRV-01, SRV-02, SRV-03, QUAL-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An operator can issue a short-lived single-use setup token, and one transaction creates the sole account with an explicit valid IANA timezone while permanently consuming setup capability (D-45, D-05)."
    - "Concurrent issuance/consumption has one winner; only token hashes are stored and generic operator/browser output never exposes credentials or private identifiers."
    - "An operator-mediated timezone command validates an IANA name, never rewrites task civil dates, records privacy-safe account audit, and invalidates affected Today/Upcoming/activity projections (D-05, D-60, D-61)."
  artifacts:
    - path: "apps/server/lib/mix/tasks/keepling.setup_token.ex"
      provides: "Explicit operator setup-token issuance command"
    - path: "apps/server/lib/mix/tasks/keepling.timezone.ex"
      provides: "Operator-mediated account timezone change command"
    - path: "apps/server/test/keepling/accounts/setup_timezone_test.exs"
      provides: "Independent-connection singleton/token/timezone proof"
  key_links:
    - from: "apps/server/lib/mix/tasks/keepling.setup_token.ex"
      to: "apps/server/lib/keepling/accounts.ex"
      via: "hashed expiring setup issuance API"
      pattern: "issue_setup_token"
    - from: "apps/server/lib/keepling_web/controllers/auth_controller.ex"
      to: "apps/server/lib/keepling/accounts.ex"
      via: "setup token consumption plus explicit IANA timezone"
      pattern: "consume_setup"
    - from: "apps/server/lib/mix/tasks/keepling.timezone.ex"
      to: "Today/Upcoming/activity view revisions"
      via: "shared account-setting command and projection invalidation"
      pattern: "timezone"
---

<objective>Make closed first-account setup and canonical account timezone operationally reachable and transactionally exact before login, recovery, or date projections consume them (D-05, D-45).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-05-SUMMARY.md</context>
<scope_exception>The 10-file slice owns the inseparable setup/timezone contract: account representation, operator issuance/change commands, setup HTTP consumption, schema, OpenAPI/generated DTOs, and independent race evidence. Date columns and date truth remain exclusively Plan 01-12.</scope_exception>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Issue and atomically consume the sole-account setup capability</name>
  <reversibility rating="costly">D-45 singleton setup and the account timezone are durable account invariants used by every later adapter.</reversibility>
  <files>apps/server/lib/keepling/accounts.ex, apps/server/lib/keepling/accounts/account.ex, apps/server/lib/keepling_web/controllers/auth_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/lib/mix/tasks/keepling.setup_token.ex, apps/server/priv/repo/migrations/20260830000200_add_closed_setup_and_timezone.exs, apps/server/test/keepling/accounts/setup_timezone_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts</files>
  <behavior>
    - "The operator command prints one generic setup URL once, stores only a cryptographic hash plus expiry/consumption metadata, and accepts no password argument or environment value."
    - "Setup requires the token, password, and an explicit valid IANA timezone; invalid/expired/consumed tokens and invalid zones create no account."
    - "Concurrent token issuance or setup consumption yields exactly one usable capability and exactly one account; the winning transaction permanently disables setup."
  </behavior>
  <action>Implement D-45 with `Mix.Tasks.Keepling.SetupToken` calling a shared `Keepling.Accounts.issue_setup_token/1` application API. Emit a short-lived high-entropy URL once with generic surrounding output, persist only its hash and lifecycle metadata, and enforce a database singleton/locking rule so concurrent issuance cannot create multiple live setup capabilities. Add the versioned setup endpoint and DTO requiring password plus explicit IANA timezone per D-05. In one database transaction lock setup state, validate/consume the token, create the sole account with measured Argon2id credential and timezone, and permanently consume setup capability; reject public registration and first-request ownership. Start with independent PostgreSQL races for simultaneous issuance and consumption, including loser state and exact absence of extra accounts. Regenerate the TypeScript contract and keep drift checks green.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/accounts/setup_timezone_test.exs --only setup' &amp;&amp; pnpm contracts:check</automated></verify>
  <done>The explicit setup-token operator command and browser consumption path prove hashed, expiring, single-use, singleton account creation with permanent setup disablement.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Change account timezone through the operator application seam</name>
  <files>apps/server/lib/keepling/accounts.ex, apps/server/lib/keepling/accounts/account.ex, apps/server/lib/mix/tasks/keepling.timezone.ex, apps/server/priv/repo/migrations/20260830000200_add_closed_setup_and_timezone.exs, apps/server/test/keepling/accounts/setup_timezone_test.exs</files>
  <behavior>
    - "Only a valid IANA name is accepted; deployment/device defaults and fixed offsets are rejected."
    - "Changing zone leaves every stored planned_on/deadline_on and UTC acceptance instant untouched."
    - "Accepted change records a privacy-safe account audit fact and advances affected view revisions so stale cursors refresh under the new account day."
  </behavior>
  <action>Implement `Mix.Tasks.Keepling.Timezone` as the D-05 operator-mediated caller of the same application/account-setting command used by transports, not as direct Ecto mutation. Validate against the configured IANA database, lock the sole account, update only the zone, record a closed privacy-safe account/security audit fact separate from D-56 task activity per D-61, and increment Today/Upcoming plus activity-render projection revisions so cached/cursor results refresh. Never rewrite civil task dates or UTC instants. Return generic success/error output without raw account IDs. Prove DST boundary reinterpretation, unchanged stored dates/times, invalid-name no-op, and concurrent change serialization.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/accounts/setup_timezone_test.exs --only timezone'</automated></verify>
  <done>Initial and changed account timezone have one validated owner, operator reachability, audit separation, and deterministic projection invalidation before temporal queries ship.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V2 Business Logic, V4 API, V6 Authentication, V8 Authorization, V9 Tokens, V14 Data Protection, and V16 Logging.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-11 | Spoofing | setup capability | high | mitigate | Operator-only issuance, high entropy, hash storage, expiry, one-shot consumption, and generic responses. |
| T-KPL01-12 | Elevation | singleton account setup | high | mitigate | Database singleton/locks and one-transaction account creation plus permanent setup consumption. |
| T-KPL01-13 | Tampering | account timezone | high | mitigate | IANA validation, shared application command, serialized update, no date rewrite, and view-revision invalidation. |
| T-KPL01-14 | Information disclosure | operator output/audit | high | mitigate | No passwords, raw token after issuance, account IDs, IP/fingerprints, or task content in logs/audit. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Run the independent setup/timezone race suite and contract drift check.</verification>
<success_criteria>D-45 setup and D-05 canonical timezone are executable prerequisites, not policy citations inside the later date plan.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-06-SUMMARY.md`.</output>
