---
phase: KPL-01-one-trustworthy-task
plan: 06
type: execute
wave: 6
depends_on: [01-05]
files_modified:
  - apps/server/lib/keepling/domain/task.ex
  - apps/server/lib/keepling/domain/merge.ex
  - apps/server/lib/keepling/application/commands.ex
  - apps/server/lib/keepling/adapters/postgres/command_store.ex
  - apps/server/priv/repo/migrations/20260830000500_add_conflicts_and_trash.exs
  - apps/server/test/keepling/application/task_lifecycle_test.exs
  - apps/server/test/keepling/application/trash_restore_test.exs
  - apps/server/test/keepling/adapters/postgres/conflict_test.exs
  - apps/web/src/features/tasks/LifecycleActions.tsx
  - apps/web/src/features/tasks/ConflictResolver.tsx
  - apps/web/src/features/lists/TrashList.tsx
  - apps/web/src/features/tasks/lifecycle.test.tsx
  - apps/web/e2e/lifecycle-recovery.spec.ts
  - packages/contracts/vectors/lifecycle-conflicts.json
autonomous: true
requirements: [GTD-05, GTD-06, SRV-03, WEB-01, WEB-02, QUAL-01]
estimate: { tokens: 76000, raw_tokens: 76000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Complete/reopen are revision-aware idempotent transitions that may rebase across unrelated edits and return already_satisfied; opposite lifecycle intent conflicts (D-33..D-35)."
    - "Trash/restore require exact revision, preserve stable identity, fields, activity, prior completion/order information, and never hard-delete or auto-expire (D-25, D-34, D-43)."
    - "Touched-field three-way merge rebases only when current equals base or requested, reruns invariants, persists structured affected-field conflicts, and resolves with a new mutation identity (D-33..D-36)."
    - "Timeout/connection loss/auth expiry preserves the submitted identity and draft; result lookup or same-ID retry distinguishes unknown from rejection (D-37..D-38)."
    - "Repeated lifecycle input and concurrent response-loss/conflict scenarios are stable, account-scoped, and create no extra effect/activity."
  artifacts:
    - { path: "apps/server/lib/keepling/domain/merge.ex", provides: "Server-owned narrow touched-field merge and lifecycle compatibility" }
    - { path: "apps/web/src/features/tasks/ConflictResolver.tsx", provides: "Persistent affected-field conflict resolution" }
    - { path: "apps/web/src/features/lists/TrashList.tsx", provides: "Recoverable Trash/restore route" }
  key_links:
    - { from: "ConflictResolver.tsx", to: "Commands.dispatch", via: "new mutation identity against latest revision", pattern: "latest_revision" }
    - { from: "submission.ts", to: "GET /api/v1/mutations/:mutation_id", via: "same identity after response loss", pattern: "Check again" }
  flagged_assumptions:
    - "GTD-05 and GTD-06 unclassified edge rows remain unresolved; lifecycle property tests must surface uncategorized state transitions rather than infer them."
    - "SRV-03 unclassified edge row remains unresolved beyond the explicit idempotency/concurrency contract; unknown terminal-result classes must be reported for decision."
---

<objective>Complete the lifecycle and conflict-recovery slice: complete/reopen, Trash/restore, narrow merge, persisted conflicts, and exact unknown-delivery reconciliation (D-24..D-25, D-31..D-39, D-43).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-05-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Execute complete/reopen and Trash/restore as exact semantic transitions</name>
  <read_first>01-CONTEXT.md D-15,D-24..D-25,D-31..D-35,D-43; validation lifecycle/trash rows; existing command kernel/views</read_first>
  <files>apps/server/lib/keepling/domain/task.ex, apps/server/lib/keepling/application/commands.ex, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/priv/repo/migrations/20260830000500_add_conflicts_and_trash.exs, apps/server/test/keepling/application/task_lifecycle_test.exs, apps/server/test/keepling/application/trash_restore_test.exs, packages/contracts/vectors/lifecycle-conflicts.json</files>
  <behavior>
    - "Complete/reopen replay, already_satisfied, unrelated-edit rebase and opposing-transition conflict preserve one terminal result."
    - "Trash/restore exact-revision transitions retain all task/history/date/organization/order fields and prior completion state."
    - "Completion moves after acknowledgement to Completed today; restore response names actual Inbox/Today destinations."
  </behavior>
  <action>Write state-machine/vector tests first. Implement closed complete/reopen/trash/restore decisions and persisted mappings through the common receipt transaction. Complete/reopen may rebase only across unrelated edits; Trash, restore, and edit/complete-versus-Trash follow D-34 exact conflict rules. Store trashed state and timestamp without delete/retention paths, preserve prior completion/order metadata, append exactly one closed activity per accepted transition, and update view revisions atomically. Return canonical snapshot, membership destinations, warnings and exact receipt envelope.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/application/task_lifecycle_test.exs test/keepling/application/trash_restore_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm contracts:check</automated></verify>
  <acceptance_criteria>All transition/replay/rebase/conflict/retention/destination vectors pass; no hard-delete path exists; every accepted transition has one snapshot revision, receipt, and activity.</acceptance_criteria>
  <done>GTD-05 and GTD-06 are durable semantic lifecycle operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and resolve narrow semantic conflicts</name>
  <read_first>01-CONTEXT.md D-33..D-36,D-39; RESEARCH Pattern 3 and persisted-conflict pitfall; UI-SPEC conflict resolution</read_first>
  <reversibility rating="costly">Merge/conflict behavior becomes compatibility vectors for later offline and MCP clients.</reversibility>
  <files>apps/server/lib/keepling/domain/merge.ex, apps/server/lib/keepling/application/commands.ex, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/test/keepling/adapters/postgres/conflict_test.exs, apps/web/src/features/tasks/ConflictResolver.tsx, apps/web/src/features/tasks/lifecycle.test.tsx, packages/contracts/openapi/keepling.yaml, packages/contracts/vectors/lifecycle-conflicts.json</files>
  <behavior>
    - "Disjoint touched fields merge; overlapping or lifecycle-incompatible fields persist an affected-field conflict whose replay never changes."
    - "Use mine/current or continued editing preserves nonconflicting draft fields and submits a new identity against the latest revision."
    - "Cross-account conflict/receipt IDs disclose nothing and mutate nothing."
  </behavior>
  <action>Implement pure `Keepling.Domain.Merge` over closed touched/base/requested values, rerun aggregate invariants after any allowed rebase, and commit conflict/result rows rather than rolling them back. Map only affected fields to the contract. Implement the inline `This task changed somewhere else.` resolver with labeled plain-text Your/Current values, Use mine/current, Keep editing, six-line note disclosure, keyboard/focus behavior and non-color choice state. Ban timestamp tokens, generic JSON Patch, last-write-wins, client merge rules, leases, CRDTs, event sourcing and per-field revisions per D-39.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/adapters/postgres/conflict_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @keepling/web test --run src/features/tasks/lifecycle.test.tsx &amp;&amp; pnpm contracts:check</automated></verify>
  <acceptance_criteria>Real interleavings prove merge/conflict/replay/resolution and account isolation; UI preserves all unaffected work and predictable focus; forbidden alternative concurrency mechanisms are absent from production behavior.</acceptance_criteria>
  <done>Concurrent edits never silently overwrite and every conflict remains stable and resolvable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile lifecycle commands after response loss</name>
  <read_first>01-CONTEXT.md D-11,D-24..D-29,D-37..D-38; UI-SPEC acknowledgement/recovery exact copy; existing submission state machine</read_first>
  <files>apps/web/src/features/tasks/LifecycleActions.tsx, apps/web/src/features/lists/TrashList.tsx, apps/web/src/commands/submission.ts, apps/web/src/features/tasks/lifecycle.test.tsx, apps/web/e2e/lifecycle-recovery.spec.ts</files>
  <behavior>
    - "Unknown result shows Checking whether your change was saved… and Check again; no failed claim or new identity occurs."
    - "Acknowledged completion/Trash removes or moves row then focuses next/previous/heading; restore stays in Trash and announces actual destinations."
    - "Initial/empty/updating/failure/auth/stale/conflict states remain distinct and retain last accepted rows."
  </behavior>
  <action>Wire all lifecycle buttons and Trash route through the existing exact submission state machine, disabling accidental duplicate activation while retaining the fixed request. Apply returned snapshots/projections only after matching acknowledgement; show persistent unknown/auth/conflict panels and precise recovery actions. Implement Completed-today placement, dedicated Completed grouping, Trash newest-first, restore-in-place removal/announcement, focus restoration and reduced-motion transitions. Add test-only response drop after database commit behind test-only compilation/runtime gating and prove the route is absent in production.</action>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/tasks/lifecycle.test.tsx &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @lifecycle-recovery</automated></verify>
  <acceptance_criteria>Real-stack E2E covers happy, validation, duplicate, stale, conflict, auth-expired, pre-accept disconnect and post-commit response loss; focus/copy/result identity are exact and test fault controls cannot ship.</acceptance_criteria>
  <done>The browser lifecycle is honest under acknowledgement, conflict, and uncertain delivery.</done>
</task>
</tasks>

## Artifacts this phase produces
- Trash/conflict migration; `Task` lifecycle decisions; `Merge` decision engine; complete/reopen/trash/restore/conflict-resolution commands and vectors.
- `LifecycleActions`, inline `ConflictResolver`, `TrashList`, Completed integration, persistent recovery panels, and `@lifecycle-recovery` real-stack fault evidence.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser lifecycle/conflict input → command kernel | Untrusted target/revision/base/current choices and replay identities. |
| test fault controller → server | Fault injection must never be reachable in a production build. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-22 | Tampering | lifecycle/merge | high | mitigate | Account scope, expected revision, server-owned merge/invariants and stored conflict. |
| T-KPL01-23 | Repudiation | unknown delivery | high | mitigate | Fixed mutation identity, result lookup/same-ID replay, terminal receipt/activity evidence. |
| T-KPL01-24 | Elevation | task/conflict IDs | high | mitigate | Session-derived account predicates; possession never authorization; cross-account tests. |
| T-KPL01-25 | Tampering | stale/unknown replay | high | mitigate | Fingerprint mismatch error, exact revisions, immutable original result, new identity only for resolution. |
| T-KPL01-26 | Elevation | fault controls | high | mitigate | Test-only compile/runtime gate and production route-absence test. |
| T-KPL01-27 | Tampering | untrusted conflict/activity values | high | mitigate | Plain-text rendering only and hostile-content component/E2E fixtures. |
| T-KPL01-SC | Tampering | UI packages | high | mitigate | Exact pins approved by Plan 01-02. |
</threat_model>
<verification>Run lifecycle/Trash/conflict ExUnit, contract drift, component and real-stack response-loss suites; all high-severity threat tests must pass.</verification>
<success_criteria>Complete/reopen/Trash/restore work through the browser with exact replay, explicit conflicts and honest uncertain-delivery recovery.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-06-SUMMARY.md` when done.</output>
