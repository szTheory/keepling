---
phase: KPL-01-one-trustworthy-task
plan: 27
type: execute
wave: 22
depends_on: [01-26]
files_modified:
  - apps/web/src/features/activity/ActivityList.tsx
  - apps/web/src/features/auth/LoginForm.tsx
  - apps/web/src/features/auth/Reauthenticate.tsx
  - apps/web/src/features/auth/RecoveryReset.tsx
  - apps/web/src/features/auth/SetupForm.tsx
  - apps/web/src/features/lists/TaskList.tsx
  - apps/web/src/features/lists/TrashList.tsx
  - apps/web/src/features/organizations/OrganizationFields.tsx
  - apps/web/src/features/recovery/MutationRecoveryPanel.tsx
  - apps/web/src/features/tasks/ConflictResolver.tsx
  - apps/web/src/test/ui-contract.test.tsx
autonomous: true
gap_closure: true
requirements: [GTD-01, GTD-02, GTD-03, GTD-04, GTD-05, GTD-06, GTD-07, WEB-01, WEB-02, QUAL-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every shipped browser feature uses only the UI-SPEC typography, weight, spacing, semantic color, and interactive-target scales."
    - "Authentication, list, organization, activity, conflict, and recovery states retain their hierarchy and behavior after mechanical normalization."
    - "One exhaustive source-level contract gate rejects future off-scale utilities across the complete production TSX tree."
  artifacts:
    - path: apps/web/src/test/ui-contract.test.tsx
      provides: "Exhaustive production-tree token and spacing drift gate"
    - path: apps/web/src/features/tasks/ConflictResolver.tsx
      provides: "Token-compliant nonterminal recovery choices"
    - path: apps/web/src/features/recovery/MutationRecoveryPanel.tsx
      provides: "Token-compliant uncertain-delivery recovery surface"
  key_links:
    - from: apps/web/src/test/ui-contract.test.tsx
      to: apps/web/src/features
      via: "production-only source scan with explicit allowlist for semantic arbitrary values"
    - from: apps/web/src/features/auth/Reauthenticate.tsx
      to: packages/design-tokens/css.css
      via: "shared semantic type, spacing, focus, and status roles from Plan 26"
    - from: apps/web/src/features/tasks/ConflictResolver.tsx
      to: apps/web/src/components/ui/button.tsx
      via: "shared 44px controls and semantic disabled/destructive variants"
---

<objective>
Complete the UI audit's token and spacing remediation across every remaining shipped browser feature.

Purpose: Turn the UI-SPEC scale from a shared-component convention into an enforceable production-tree contract without mixing mechanical cleanup into modal or workspace architecture.
Output: Normalized feature surfaces and an exhaustive token/spacing drift test.
</objective>

<scope_rationale>This plan is deliberately limited to mechanical scale normalization in the ten feature files left after the shared primitives/shell work; its three feature-group tasks share one exhaustive production-source gate and do not alter routing, modal architecture, or domain behavior.</scope_rationale>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/brand/BRAND-SEED.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-REVIEW.md
@.planning/phases/KPL-01-one-trustworthy-task/01-26-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Normalize authentication surfaces as one complete responsive path</name>
  <files>apps/web/src/features/auth/LoginForm.tsx, apps/web/src/features/auth/Reauthenticate.tsx, apps/web/src/features/auth/RecoveryReset.tsx, apps/web/src/features/auth/SetupForm.tsx, apps/web/src/test/ui-contract.test.tsx</files>
  <read_first>apps/web/src/features/auth/LoginForm.tsx, apps/web/src/features/auth/Reauthenticate.tsx, apps/web/src/features/auth/RecoveryReset.tsx, apps/web/src/features/auth/SetupForm.tsx, apps/web/src/features/auth/auth.test.tsx, packages/design-tokens/css.css, .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md</read_first>
  <behavior>
    - "Login, setup, recovery, and reauthentication use only 14/16/20/28px type, 400/600 weights, and 4/8/16/24/32/48/64px spacing."
    - "Inputs and actions retain 44px targets, password-manager-friendly semantics, visible focus, error associations, paste, reveal, and autofill behavior."
    - "Long localized/error text reflows at 320px without overflow or reducing controls below target size."
  </behavior>
  <action>Mechanically replace off-scale typography, weights, spacing, and control sizes in the four authentication surfaces with the semantic tokens and shared Button/input conventions established by Plan 26. Preserve password field names, autocomplete attributes, paste/reveal behavior, recovery token handling, exact reauthentication state, and D-46/D-47/D-51 semantics. Extend the production-source contract in `ui-contract.test.tsx` to include these four files before changing them, so the tracer goes red on their current audited values and green only after normalization. Do not restyle by hiding labels, truncating errors, tightening targets, or changing authentication behavior. This tracer proves one complete unauthenticated-to-reauthenticated visual path before normalizing other feature groups.</action>
  <acceptance_criteria>All authentication surfaces conform to the declared scales without changing password-manager or recovery behavior.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @keepling/web test --run src/features/auth/auth.test.tsx src/test/ui-contract.test.tsx &amp;&amp; pnpm --filter @keepling/web typecheck</automated>
  </verify>
  <fails_when>Off-scale utilities remain in the four files, any label/error association changes, paste/reveal/autocomplete regresses, targets shrink, or auth recovery semantics change.</fails_when>
  <done>The complete authentication path is token-compliant and behaviorally unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize list, organization, and activity hierarchy</name>
  <files>apps/web/src/features/activity/ActivityList.tsx, apps/web/src/features/lists/TaskList.tsx, apps/web/src/features/lists/TrashList.tsx, apps/web/src/features/organizations/OrganizationFields.tsx, apps/web/src/test/ui-contract.test.tsx</files>
  <read_first>apps/web/src/features/activity/ActivityList.tsx, apps/web/src/features/lists/TaskList.tsx, apps/web/src/features/lists/TrashList.tsx, apps/web/src/features/organizations/OrganizationFields.tsx, apps/web/src/test/ui-contract.test.tsx, .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md</read_first>
  <behavior>
    - "List headings, rows, membership reasons, activity facts, organization fields, empty/loading/error states, and Load more controls use declared scales only."
    - "Stable row focus, append focus, live announcements, independent scrolling, and long untrusted text behavior remain unchanged."
    - "State remains text/semantic, never color-only, in light and dark themes."
  </behavior>
  <action>Extend the production-source contract to ActivityList, TaskList, TrashList, and OrganizationFields, then replace their off-scale utilities with the Plan 26 semantic tokens and spacing/type scales until that focused gate passes. Preserve D-21 through D-29 ordering, pagination, focus, loading, announcement, and motion behavior; preserve D-58 text-only rendering. Consolidate repeated visual state patterns only when doing so does not couple feature state machines or cross the existing component boundary. Do not alter route composition from Plan 25.</action>
  <acceptance_criteria>List, activity, and organization surfaces conform to the scale while all existing data, focus, pagination, and recovery contracts remain intact.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @keepling/web test --run src/test/ui-contract.test.tsx &amp;&amp; pnpm --filter @keepling/web typecheck</automated>
  </verify>
  <fails_when>Off-scale utilities remain in these files, hierarchy becomes color-only, long text is clipped, row/load focus changes, pagination behavior changes, or the shared workspace is bypassed.</fails_when>
  <done>Primary information-dense feature surfaces use the same deliberate scale without behavioral drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize recovery/conflict surfaces and make the gate exhaustive</name>
  <files>apps/web/src/features/recovery/MutationRecoveryPanel.tsx, apps/web/src/features/tasks/ConflictResolver.tsx, apps/web/src/test/ui-contract.test.tsx</files>
  <read_first>apps/web/src/features/recovery/MutationRecoveryPanel.tsx, apps/web/src/features/tasks/ConflictResolver.tsx, apps/web/src/test/ui-contract.test.tsx, .planning/phases/KPL-01-one-trustworthy-task/01-21-SUMMARY.md, .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md, .planning/phases/KPL-01-one-trustworthy-task/01-UI-REVIEW.md</read_first>
  <behavior>
    - "Conflict choices and uncertain-delivery panels use declared scales and semantic disabled/status colors while retaining exact resolution and pending locks."
    - "The production TSX source tree contains none of the audit's off-scale type, weight, spacing, small-target, hardcoded destructive-color, or fixed-workspace patterns outside a documented semantic allowlist."
    - "The exhaustive gate ignores comments/test fixtures and reports the file plus offending token when it fails."
  </behavior>
  <action>Normalize MutationRecoveryPanel and ConflictResolver without altering Plan 21's exact resolution lock, nonterminal disabled choices, retry identity, or recovery messages. Then expand `ui-contract.test.tsx` from Plan 26's scoped check to scan every production `.tsx` file under `apps/web/src`, excluding tests and generated output. Reject the audit's off-scale typography/weights/spacing, undersized interactive variants, hardcoded destructive foreground, and fixed workspace arithmetic; allow arbitrary values only through a small named semantic allowlist with a reason and contract assertion. Parse production class strings/regions so comments cannot satisfy or fail the gate accidentally. Run the complete web suite to catch hierarchy changes that source scanning cannot prove.</action>
  <acceptance_criteria>All remaining feature surfaces are normalized and a precise exhaustive test prevents recurrence across the shipped browser tree.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @keepling/web test --run src/test/ui-contract.test.tsx &amp;&amp; pnpm --filter @keepling/web test &amp;&amp; pnpm --filter @keepling/web lint &amp;&amp; pnpm --filter @keepling/web typecheck</automated>
  </verify>
  <fails_when>Any audited off-scale production pattern remains, the gate scans comments/tests instead of production classes, an allowlist lacks a semantic reason, conflict/recovery controls unlock early, exact resolution identity changes, or the full web suite regresses.</fails_when>
  <done>The entire production browser tree is protected by one precise token/spacing contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted long content → constrained layout | User text must reflow without hiding state or controls. |
| Recovery/conflict state → user choice | Visual normalization must not enable choices before terminal reconciliation. |
| Token policy → production class source | The gate must inspect executable styling rather than comments or tests. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-KPL01-27-01 | Tampering | Conflict/recovery controls | medium | mitigate | Preserve exact resolution identity and disabled nonterminal controls with full regression tests. |
| T-KPL01-27-02 | Denial of service | Long content and responsive feature layouts | medium | mitigate | Declared reflow scales, 44px controls, and existing long-content tests remain enforced. |
| T-KPL01-27-03 | Spoofing | Color/status hierarchy | low | mitigate | Semantic text and token roles retain non-color status cues. |
| T-KPL01-27-SC | Tampering | Dependency supply chain | low | accept | Mechanical source normalization adds no package or external service. |
</threat_model>

<verification>
Run all focused commands and `./tooling/test-phase-1.sh --run`. Then rerun `$gsd-ui-review KPL-01`; the regenerated audit must close the narrow-navigation, workspace, modal, copy, token, and spacing automated findings. Preserve the three current `01-VERIFICATION.md` human checks for real VoiceOver, perceptual route/theme/zoom review, and a real password manager; do not fabricate automated substitutes or hand-edit the audit score.
</verification>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Trust every browser change | 01-27 | COVERED | Consistent visual hierarchy no longer varies by feature. |
| REQ | GTD-01..GTD-07 | Phase 1 feature surfaces | 01-27 | COVERED | All remaining shipped feature groups are normalized. |
| REQ | WEB-01/WEB-02/QUAL-01 | Browser interaction, recovery, quality | 01-27 | COVERED | Exhaustive drift gate plus full suite protects behavior. |
| RESEARCH | — | Semantic design tokens and calm deliberate UI | 01-26/01-27 | COVERED | Source tokens flow through all production components. |
| CONTEXT | D-13/D-21..D-29/D-36/D-37/D-58/D-59 | Focus, list, conflict, recovery, text/activity | 01-27 | COVERED | Mechanical normalization preserves state-machine behavior. |
| CONTEXT | D-01..D-63 | Other locked decisions | 01-01..01-26 | COVERED | No semantics are reduced or deferred. |
</source_coverage_audit>

<success_criteria>
- No audited off-scale utility remains in production TSX outside an explicit semantic allowlist.
- Authentication, list, activity, organization, recovery, and conflict behavior stays green.
- The full Phase 1 gate passes and the UI re-audit closes automated findings.
- Genuine VoiceOver, perceptual, and password-manager validation remains honestly human-needed.
</success_criteria>

<output>
Create `.planning/phases/KPL-01-one-trustworthy-task/01-27-SUMMARY.md` when done.
</output>
