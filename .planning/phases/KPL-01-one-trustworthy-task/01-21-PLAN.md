---
phase: KPL-01-one-trustworthy-task
plan: 21
type: execute
wave: 18
depends_on: [01-19]
files_modified:
  - apps/web/src/features/tasks/ConflictResolver.tsx
  - apps/web/src/features/tasks/conflict-resolver.test.tsx
  - apps/web/e2e/conflict-resolution-recovery.spec.ts
autonomous: true
gap_closure: true
requirements: [SRV-03, WEB-02, QUAL-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After conflict resolution dispatch begins, mine/current selections and Keep editing remain disabled through in-flight, unknown, and authentication-required states (D-35, D-36, D-37)."
    - "The exact conflict-resolution request and mutation identity remain reachable until matching acknowledgement or verified terminal rejection; delayed or lost responses cannot be fenced or replaced (D-31, D-35, D-37, D-38)."
    - "Deferred-response and real after-commit-loss tests prove canonical accepted choices are reconciled and never hidden by later interaction."
  artifacts:
    - path: "apps/web/src/features/tasks/ConflictResolver.tsx"
      provides: "Immutable dispatched resolution and nonterminal interaction lock"
    - path: "apps/web/e2e/conflict-resolution-recovery.spec.ts"
      provides: "Deferred-response and real PostgreSQL after-commit-loss identity-preservation evidence"
  key_links:
    - from: "ConflictResolver.tsx selection controls"
      to: "ExactSubmission snapshot"
      via: "one nonterminal-state predicate that freezes all resolution-changing actions"
      pattern: "in_flight|unknown|authentication_required"
    - from: "ConflictResolver.tsx"
      to: "durable conflict mutation lookup"
      via: "unchanged mutation/task/conflict identity through check and authentication resume"
      pattern: "mutationId"
---

<objective>Preserve the exact conflict resolution across delayed, lost, and authentication-interrupted responses so an accepted choice can never be hidden or replaced (D-31, D-35..D-38).</objective>

## Phase Goal

**As a** Keepling user, **I want to** manage one task end to end, **so that** I can trust every browser change.

<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>

<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md
@.planning/phases/KPL-01-one-trustworthy-task/01-16-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-17-SUMMARY.md</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Freeze a dispatched conflict resolution through deferred response</name>
  <files>apps/web/src/features/tasks/ConflictResolver.tsx, apps/web/src/features/tasks/conflict-resolver.test.tsx</files>
  <read_first>apps/web/src/features/tasks/ConflictResolver.tsx; apps/web/src/features/tasks/conflict-resolver.test.tsx; apps/web/src/commands/submission.ts; .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md section “Conflict resolution”; .planning/phases/KPL-01-one-trustworthy-task/01-REVIEW.md finding CR-03</read_first>
  <behavior>
    - "A controllably deferred resolution response disables every Use mine/Use current choice, Keep editing, and duplicate Save resolution activation."
    - "Click attempts during in-flight, unknown, or authentication-required states cannot call fence, clear the recovery panel, or create a second mutation identity."
    - "A matching acknowledgement applies the original choice; a verified terminal rejection alone unlocks selection for a new attempt."
  </behavior>
  <action>Write a deferred-promise component test first. Derive one `resolutionLocked` predicate from the exact submission states `in_flight`, `unknown`, and `authentication_required`; use it for both field choices and Keep editing as well as Save. Remove the choice path that fences or clears a dispatched exact submission. Per D-35/D-37, retain the immutable prepared resolution and identity until matching acknowledgement or terminal rejection; per D-36/UI-SPEC, preserve affected-field labels, keyboard behavior, focus, and non-color selection semantics while disabled. Keep nonconflicting draft values intact.</action>
  <acceptance_criteria>While a deferred response is outstanding, all resolution-changing controls are disabled, no replacement request is created, and resolving the original promise applies only the original mine/current selections.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/tasks/conflict-resolver.test.tsx</automated></verify>
  <fails_when>A choice or Keep editing can fence the exact submission after dispatch, the recovery state disappears before a terminal result, or a second mutation identity is observed.</fails_when>
  <done>The mounted resolver preserves one exact dispatched choice through every nonterminal UI state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove conflict identity survives after-commit response loss</name>
  <files>apps/web/e2e/conflict-resolution-recovery.spec.ts</files>
  <read_first>apps/web/e2e/lifecycle-recovery.spec.ts; apps/web/e2e/support/stack.ts; apps/server/lib/keepling_web/controllers/test_fault_controller.ex; apps/web/src/features/tasks/ConflictResolver.tsx; .planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md second gap</read_first>
  <behavior>
    - "The real fault boundary commits a resolution in PostgreSQL and drops its response."
    - "Attempting another choice during recovery cannot alter the displayed selection or original mutation identity."
    - "Check again or sign-in continuation looks up the original receipt, reconciles the accepted canonical task once, and closes the conflict."
  </behavior>
  <action>Add a dedicated Playwright scenario using the existing credentialed test-only `after_commit` fault header around the real command boundary. Create a persisted conflict, choose a resolution, lose the response after commit, attempt to change the choice and resubmit, then verify the controls remain locked and the original mutation is the one queried/reconciled. Assert the final task snapshot reflects the first accepted selection and that no replacement receipt/effect is created. Preserve production fault-route isolation from Plan 01-17.</action>
  <acceptance_criteria>The real Phoenix/PostgreSQL test proves response loss cannot discard, replace, or obscure the originally accepted conflict resolution.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test:e2e --grep @conflict-resolution-recovery</automated></verify>
  <fails_when>The test uses a mocked API instead of the real fault boundary, a second resolution reaches the server, or final canonical state differs from the first dispatched choice.</fails_when>
  <done>Deferred and after-commit-loss evidence close the verifier’s P16.2 conflict-resolution gap.</done>
</task>

</tasks>

<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V1 Encoding, V2 Business Logic, V3 Web Frontend, V4 API, V8 Authorization, and V16 Error Handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-G21-01 | Tampering | conflict resolution controls | high | mitigate | Freeze all resolution-changing controls for every nonterminal state and block replacement identity creation. |
| T-KPL01-G21-02 | Repudiation | accepted resolution after response loss | high | mitigate | Retain exact request identity, durable lookup, matching acknowledgement checks, and real after-commit-loss proof. |
| T-KPL01-G21-03 | Elevation | conflict/task identities | high | mitigate | Existing session-derived account scoping remains authoritative; E2E enters only through authenticated semantic routes. |
| T-KPL01-G21-04 | Information disclosure | rendered conflict values | medium | mitigate | Preserve plain-text rendering and technical-detail boundaries from D-58/UI-SPEC. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>

<source_coverage>
GOAL trust through conflict recovery: covered. REQ SRV-03/WEB-02/QUAL-01: covered by component and real-stack fault evidence. RESEARCH semantic-conflict and response-lost-after-commit matrix: covered. CONTEXT D-31, D-35..D-38, D-51, D-58: preserved or implemented; all remaining decisions stay covered by completed Plans 01-01..01-19. Deferred ideas remain excluded.
</source_coverage>

<verification>Run focused conflict tests, the new real-stack recovery spec, and `./tooling/test-phase-1.sh --run`; any high threat or identity mismatch blocks completion.</verification>
<success_criteria>A dispatched conflict resolution is immutable until a verified terminal result and remains exactly recoverable after response delay, loss, or authentication interruption.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-21-SUMMARY.md`.</output>
