---
phase: KPL-01-one-trustworthy-task
plan: 24
type: execute
wave: 19
depends_on: [01-23]
files_modified:
  - apps/server/config/config.exs
  - apps/server/lib/keepling/adapters/postgres/task_views.ex
  - apps/server/lib/keepling_web/endpoint.ex
  - apps/server/lib/keepling_web/security_headers.ex
  - apps/server/test/keepling/adapters/postgres/task_views_test.exs
  - apps/server/test/keepling_web/security_headers_test.exs
  - apps/web/src/App.tsx
  - apps/web/src/app/AuthProvider.tsx
  - apps/web/src/app/routes.tsx
  - apps/web/src/features/auth/auth.test.tsx
  - apps/web/src/test/security-content.test.tsx
autonomous: true
gap_closure: true
requirements: [SRV-01, SRV-02, WEB-01, WEB-02, QUAL-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Production browser responses carry a restrictive, tested Content-Security-Policy while hostile task content remains inert text."
    - "Every PostgreSQL task-view read has a bounded query timeout and returns the existing infrastructure-failure contract when the bound is exceeded."
    - "Authentication continuations owned by a routed surface are unregistered on route change or unmount and can never update an abandoned surface."
  artifacts:
    - path: apps/server/lib/keepling_web/security_headers.ex
      provides: "Central production-safe browser security-header policy"
    - path: apps/server/lib/keepling/adapters/postgres/task_views.ex
      provides: "Bounded task-view query execution"
    - path: apps/web/src/app/AuthProvider.tsx
      provides: "Owner-scoped continuation registration and cancellation"
  key_links:
    - from: apps/server/lib/keepling_web/endpoint.ex
      to: apps/server/lib/keepling_web/security_headers.ex
      via: "endpoint plug before Plug.Static and the router"
    - from: apps/server/lib/keepling/adapters/postgres/task_views.ex
      to: apps/server/config/config.exs
      via: "one explicit task-view timeout used by SQL and transactional reads"
    - from: apps/web/src/app/routes.tsx
      to: apps/web/src/app/AuthProvider.tsx
      via: "route-lifetime scope disposal fences pending continuation callbacks"
---

<objective>
Close the remaining Phase KPL-01 security audit findings without weakening the established task, session, or authentication contracts.

Purpose: Block the one open high-severity content-injection threat and mitigate the two open denial-of-service findings with executable evidence.
Output: Restrictive CSP enforcement, bounded task-view reads, owner-scoped authentication continuations, hostile-content tests, and focused recovery tests.
</objective>

<scope_rationale>The three tasks close the only findings in one authoritative security re-audit and share one blocking full-phase/security gate; each task has disjoint implementation ownership, so keeping them together gives the re-auditor one atomic security-hardening result without expanding product scope.</scope_rationale>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-SECURITY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md
@.planning/phases/KPL-01-one-trustworthy-task/01-20-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-23-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce the content boundary from HTTP response to rendered hostile fixture</name>
  <files>apps/server/lib/keepling_web/security_headers.ex, apps/server/lib/keepling_web/endpoint.ex, apps/server/test/keepling_web/security_headers_test.exs, apps/web/src/test/security-content.test.tsx</files>
  <read_first>apps/server/lib/keepling_web/endpoint.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling/security_audit_test.exs, apps/web/src/features/tasks/TaskEditor.tsx, apps/web/src/features/activity/ActivityList.tsx, apps/web/src/features/tasks/ConflictResolver.tsx, .planning/phases/KPL-01-one-trustworthy-task/01-SECURITY.md</read_first>
  <behavior>
    - "A normal browser/static response includes a single CSP that denies objects, framing, foreign bases, and foreign form targets and permits only the same-origin resources Keepling actually uses."
    - "The production policy contains no unsafe-eval or wildcard source and does not depend on a development-only Vite origin."
    - "Hostile titles, notes, activity values, and conflict values containing markup, event handlers, URL schemes, and closing tags render as literal text without creating executable nodes or attributes."
  </behavior>
  <action>Create `KeeplingWeb.SecurityHeaders` as a small Plug and wire it in `Endpoint` before `Plug.Static`, so the policy also covers a production-served application shell rather than API routes only. Define the least-permissive same-origin CSP compatible with the checked-in application: `default-src`, `base-uri`, `frame-ancestors`, `object-src`, `form-action`, `script-src`, `style-src`, `img-src`, and `connect-src` must be explicit; do not add wildcard sources, inline script permission, eval permission, or environment-specific remote origins merely to satisfy tests. Add endpoint-level tests for static/not-found and API responses plus a production-policy invariant test. Add a browser component test that renders actual task/activity/conflict surfaces with hostile strings and asserts text fidelity and absence of injected elements/handlers. Preserve D-58's untrusted-text rule and the UI-SPEC's no-hidden-markup contract. This is the vertical tracer for T-KPL01-41: HTTP mitigation and real rendering evidence must pass together.</action>
  <acceptance_criteria>CSP is applied at the endpoint boundary, its production form is restrictive, and hostile canonical values remain visible inert text in actual UI components.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/security_headers_test.exs' &amp;&amp; pnpm --filter @keepling/web test --run src/test/security-content.test.tsx</automated>
  </verify>
  <fails_when>The CSP is route-only, absent from static responses, admits wildcard/eval sources, breaks the production application resource set, or hostile content produces markup, handlers, navigation, or executable DOM.</fails_when>
  <done>T-KPL01-41 has production-boundary and component-level executable mitigation evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound every task-view database read</name>
  <files>apps/server/config/config.exs, apps/server/lib/keepling/adapters/postgres/task_views.ex, apps/server/test/keepling/adapters/postgres/task_views_test.exs</files>
  <read_first>apps/server/config/config.exs, apps/server/config/runtime.exs, apps/server/lib/keepling/adapters/postgres/task_views.ex, apps/server/test/keepling/adapters/postgres/task_views_test.exs, .planning/phases/KPL-01-one-trustworthy-task/01-SECURITY.md</read_first>
  <behavior>
    - "Inbox, Today, Upcoming, Completed, and Today receipt lookups all consume the same finite task-view timeout policy."
    - "A deliberately delayed PostgreSQL task-view query exceeds the test bound, is cancelled by the adapter/Repo boundary, and maps to `:infrastructure_failure` without leaking SQL details."
    - "Normal keyset pagination, account scoping, and Today ordering retain their existing results under the production bound."
  </behavior>
  <action>Add one explicit, positive task-view query timeout under the Keepling application configuration and thread it through every `Repo.transact`, `Ecto.Adapters.SQL.query`, and `query!` operation reachable from the TaskViews port. Keep the bound above normal production work and below an unbounded web request; do not change query semantics, cursor limits, schemas, or API error codes. Provide a test-only delayed-query seam inside the existing adapter test boundary (not a production route) that proves the configured timeout is actually passed to PostgreSQL/DBConnection and translated through the existing infrastructure-failure result. Retain the current account/index/limit tests as regression evidence for T-KPL01-28.</action>
  <acceptance_criteria>No task-view database operation can wait indefinitely, timeout failure is stable and content-free, and ordinary task-view tests remain green.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/adapters/postgres/task_views_test.exs'</automated>
  </verify>
  <fails_when>Any TaskViews SQL/transaction path omits the bound, the timeout is infinite/non-positive, delayed work succeeds past the test deadline, error details escape, or view/order behavior changes.</fails_when>
  <done>T-KPL01-28 has executable cancellation evidence at the real PostgreSQL adapter boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dispose routed authentication continuations on unmount</name>
  <files>apps/web/src/app/AuthProvider.tsx, apps/web/src/App.tsx, apps/web/src/app/routes.tsx, apps/web/src/features/auth/auth.test.tsx</files>
  <read_first>apps/web/src/app/AuthProvider.tsx, apps/web/src/App.tsx, apps/web/src/app/routes.tsx, apps/web/src/features/auth/auth.test.tsx, .planning/phases/KPL-01-one-trustworthy-task/01-20-SUMMARY.md, .planning/phases/KPL-01-one-trustworthy-task/01-SECURITY.md</read_first>
  <behavior>
    - "Each routed recovery surface registers continuations under a stable route-lifetime owner and unregisters that owner's pending entries when the pathname changes or the surface unmounts."
    - "A disposed continuation is excluded from an in-progress or later drain even if its Promise resolves after disposal."
    - "Live concurrent continuations still drain once in generation order, replacement by semantic identity still works, and logout still clears everything."
  </behavior>
  <action>Extend the Plan 20 continuation coordinator with an owner-scoped registration API that returns an idempotent disposer and fences state publication by both authentication generation and owner liveness. In `AppRoutes`, create a stable scope for the current routed surface, pass its scoped authentication callback to every routed component and to the authenticated shell render path, and dispose it on route change/unmount. Adapt `App` to provide authenticated content through that scoped callback instead of capturing the global callback before the route boundary. Tests must cover unmount before reauthentication, route change during an active drain, late Promise settlement, duplicate disposer calls, and preservation of concurrent/replacement behavior. Do not suppress React warnings, swallow live continuation failures, or cancel unrelated owners. This mitigates T-KPL01-G20-04 while preserving D-11, D-37, and D-51.</action>
  <acceptance_criteria>Abandoned surfaces cannot resume or publish state, while mounted exact-intent continuations retain the Plan 20 recovery contract.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @keepling/web test --run src/features/auth/auth.test.tsx &amp;&amp; pnpm --filter @keepling/web typecheck</automated>
  </verify>
  <fails_when>Unmounted callbacks execute, late drains update route state, disposal clears another owner, semantic replacement/order changes, or logout leaves resumable work.</fails_when>
  <done>T-KPL01-G20-04 has deterministic unregister, unmount, and late-settlement tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical task content → browser DOM | User-controlled strings must stay inert text under defense in depth. |
| HTTP response → browser execution policy | Production responses define which resources and embedding contexts the browser may execute. |
| API request → PostgreSQL task views | Authenticated callers can trigger bounded but potentially expensive list work. |
| Expired session → continuation coordinator | Deferred callbacks cross time and component-lifetime boundaries. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-KPL01-41 | Tampering | Browser content rendering and response policy | high | mitigate | Task 1 adds endpoint CSP plus hostile real-component fixtures; plan cannot pass without both. |
| T-KPL01-28 | Denial of service | PostgreSQL task-view adapter | medium | mitigate | Task 2 applies and proves one finite query timeout across every TaskViews read. |
| T-KPL01-G20-04 | Denial of service | Authentication continuation coordinator | medium | mitigate | Task 3 unregisters route-owned continuations and fences late settlement. |
| T-KPL01-24-SC | Tampering | Dependency supply chain | low | accept | No package install or dependency change is planned. |
</threat_model>

<verification>
Run each focused command, then `./tooling/test-phase-1.sh --run`. After implementation, rerun `$gsd-secure-phase KPL-01`; the regenerated audit must report `threats_open: 0`. Do not hand-edit `01-SECURITY.md` or reinterpret a failing high-severity finding as accepted.
</verification>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Browser changes remain trustworthy | 01-24 | COVERED | Content execution, bounded reads, and abandoned recovery are closed. |
| REQ | SRV-01/SRV-02 | Semantic server boundary and web/API proof | 01-24 | COVERED | Timeout behavior and endpoint headers have real-stack tests. |
| REQ | WEB-01/WEB-02 | Browser lifecycle and recovery | 01-24 | COVERED | Hostile rendering and continuation disposal preserve honest state. |
| REQ | QUAL-01 | Security and recovery evidence | 01-24 | COVERED | All three open security findings receive executable gates. |
| RESEARCH | — | Text-only rendering, bounded work, privacy-safe failures | 01-24 | COVERED | No new dependency or external integration is introduced. |
| CONTEXT | D-11/D-37/D-51/D-58 | Preserve exact intent and render canonical values as text | 01-24 | COVERED | Owner disposal does not alter live recovery identity; CSP is defense in depth. |
| CONTEXT | D-01..D-63 | Remaining Phase 1 locked decisions | 01-01..01-23 | COVERED | This additive plan does not supersede completed decision coverage. |
</source_coverage_audit>

<success_criteria>
- The formerly open high-severity threat is blocked by CSP and hostile-content evidence.
- All task-view reads demonstrably time out through the existing stable failure contract.
- Route/unmount cleanup removes only abandoned authentication continuations.
- The full Phase 1 automated gate remains green and the security re-audit has no open threats.
</success_criteria>

<output>
Create `.planning/phases/KPL-01-one-trustworthy-task/01-24-SUMMARY.md` when done.
</output>
