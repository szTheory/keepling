---
phase: KPL-01-one-trustworthy-task
plan: 23
type: execute
wave: 18
depends_on: [01-19]
files_modified:
  - apps/server/lib/keepling_web/router.ex
  - apps/server/test/keepling_web/test_fault_test.exs
  - apps/web/src/features/sessions/SessionList.tsx
  - apps/web/src/features/sessions/session-list.test.tsx
  - apps/web/e2e/session-reconciliation.spec.ts
autonomous: true
gap_closure: true
requirements: [SRV-01, SRV-03, WEB-02, QUAL-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After an uncertain session rename or revoke response, Keepling reloads the authoritative session inventory before claiming changed, unchanged, revoked, or active state (D-37, D-38, D-50)."
    - "After an uncertain current-session logout response, Keepling probes authentication state and transitions to logged out when the session is gone; it never claims the session remains active without proof (D-48, D-52)."
    - "After-commit-loss tests cover session rename, revoke, and logout against the real server and PostgreSQL without adding schema or weakening recent-auth/CSRF controls."
  artifacts:
    - path: "apps/web/src/features/sessions/SessionList.tsx"
      provides: "Authoritative inventory/authentication reconciliation for uncertain session mutations"
    - path: "apps/web/e2e/session-reconciliation.spec.ts"
      provides: "Real after-commit-loss proof for rename, revoke, and current-session logout"
  key_links:
    - from: "SessionList mutation catch paths"
      to: "listSessions/getSession authoritative reads"
      via: "post-failure reconciliation before outcome copy"
      pattern: "listSessions|getSession"
    - from: "current-session logout reconciliation"
      to: "AuthProvider clear/logout transition"
      via: "authentication_required probe proves the session is gone"
      pattern: "onLoggedOut"
---

<objective>Make Today-independent session administration honest after uncertain delivery by reconciling authoritative session/authentication state before making definitive claims (D-37..D-38, D-48, D-50, D-52).</objective>

## Phase Goal

**As a** Keepling user, **I want to** manage one task end to end, **so that** I can trust every browser change.

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>

<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md
@.planning/phases/KPL-01-one-trustworthy-task/01-08-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-17-SUMMARY.md</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reconcile uncertain session administration before outcome copy</name>
  <files>apps/web/src/features/sessions/SessionList.tsx, apps/web/src/features/sessions/session-list.test.tsx</files>
  <read_first>apps/web/src/features/sessions/SessionList.tsx; apps/web/src/features/auth/auth.test.tsx; apps/web/src/api/keepling.ts; apps/web/src/app/AuthProvider.tsx; .planning/phases/KPL-01-one-trustworthy-task/01-REVIEW.md finding WR-02; .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md error and recovery copy</read_first>
  <behavior>
    - "On a transport/5xx rename failure, reload Sessions: matching new label proves success, old label proves unchanged, and an unreadable inventory remains explicitly unknown with Retry checking."
    - "On a transport/5xx revoke failure, absence proves revocation, presence proves active, and an unreadable inventory remains explicitly unknown."
    - "On a transport/5xx logout failure, authentication_required from the session probe proves logout and calls onLoggedOut; an authenticated probe proves still signed in; an unreadable probe remains unknown."
  </behavior>
  <action>Start with component tests for rename, other-session revoke, and current-session logout where the mutation rejects after the fake server state has already changed. Add an explicit session-action recovery state that preserves the intended action and disables conflicting controls while checking. For non-authentication transport/parsed-5xx failures, reconcile with authoritative `listSessions` for rename/revoke and the existing session probe for logout. Per D-37/D-38, reserve definitive changed/unchanged/revoked/active copy for proven state; if reconciliation also fails, show persistent unknown copy with a named Check again action that repeats only the read, not the mutation. Per D-48/D-52, retain CSRF/recent-auth behavior and clear browser authentication only when logout is proved. No persistence or contract change is needed because authoritative reads distinguish the required terminal states.</action>
  <acceptance_criteria>Component tests prove every session action reports only authoritative state, preserves uncertain intent for read-only reconciliation, and never automatically resends a possibly committed mutation.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/sessions/session-list.test.tsx</automated></verify>
  <fails_when>A generic failure immediately claims the old label/session is unchanged or active, Check again resends the mutation, current logout remains rendered after authentication is proved absent, or recent-auth/CSRF handling is bypassed.</fails_when>
  <done>Session administration has an honest local reconciliation state machine without new canonical storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove rename, revoke, and logout after-commit loss on the real stack</name>
  <files>apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/test_fault_test.exs, apps/web/e2e/session-reconciliation.spec.ts</files>
  <read_first>apps/server/lib/keepling_web/router.ex; apps/server/test/keepling_web/test_fault_test.exs; apps/server/lib/keepling_web/controllers/test_fault_controller.ex; apps/server/lib/keepling_web/controllers/auth_controller.ex; apps/web/e2e/auth-recovery.spec.ts; apps/web/e2e/lifecycle-recovery.spec.ts; apps/web/e2e/support/stack.ts; apps/web/src/features/sessions/SessionList.tsx</read_first>
  <behavior>
    - "Rename commits, response is lost, inventory reconciliation displays the accepted label without retrying the write."
    - "Other-session revoke commits, response is lost, inventory reconciliation removes it and reports revocation."
    - "Current-session logout commits, response is lost, authentication probing transitions to the logged-out UI."
    - "The DELETE /api/v1/sessions/:id route traverses the test fault plug only in MIX_ENV=test, after authentication, origin, and recent-auth checks; production keeps the original four-pipeline route."
  </behavior>
  <action>Add a test-only `recent_auth_mutation_pipelines` selection in `KeeplingWeb.Router`: in test it must be `[:api, :authenticated, :mutation, :recent_auth, :test_fault]`, while every non-test build must remain `[:api, :authenticated, :mutation, :recent_auth]`; route `DELETE /api/v1/sessions/:id` through that selection. Keep `:test_fault` after `:recent_auth` so the credentialed fault cannot bypass authentication, trusted-origin/CSRF, or recent-auth authorization. Extend `test_fault_test.exs` to assert this exact test/production split and retain the existing compile-gated controller/route isolation evidence. Then add a dedicated Playwright suite using the resulting after-commit seam on rename, revoke, and logout session endpoints. Seed two tracked sessions, exercise rename and revoke through the UI, and verify the authoritative PostgreSQL-backed inventory determines the outcome after the response is dropped. Exercise current-session logout with response loss and verify the browser probes auth, clears account-scoped state, and shows login after the server session is gone. Assert no duplicate mutation request is sent during reconciliation.</action>
  <acceptance_criteria>The focused router test proves the recent-auth DELETE route gains fault injection only in test and preserves all authorization plugs; all three real after-commit-loss scenarios converge to committed server truth with accurate copy and no automatic duplicate write.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/test_fault_test.exs' &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @session-reconciliation</automated></verify>
  <fails_when>The DELETE session route still bypasses the test fault plug, the fault plug precedes recent-auth or appears in the production pipeline, the server test fails, any E2E scenario relies only on mocks, the UI claims a result before authoritative reconciliation, reconciliation retries the write, or logged-out server state leaves authenticated UI visible.</fails_when>
  <done>Session rename, revoke, and logout have real-stack uncertain-delivery evidence and close the remaining P17.1/SRV-03 gap.</done>
</task>

</tasks>

<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V3 Web Frontend, V4 API, V6 Authentication, V7 Session Management, V8 Authorization, V14 Data Protection, and V16 Error Handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-G23-01 | Spoofing | current-session logout probe | high | mitigate | Use the existing cookie-authenticated session endpoint; only server `authentication_required` proves logout. |
| T-KPL01-G23-02 | Tampering | session rename/revoke outcome | high | mitigate | Reconcile account-scoped authoritative inventory and never infer from a missing response. |
| T-KPL01-G23-03 | Repudiation | uncertain administration | high | mitigate | Persistent named checking state, read-only Check again, and real after-commit-loss evidence. |
| T-KPL01-G23-04 | Information disclosure | session inventory | high | mitigate | Existing authenticated account scope and coarse activity fields; no raw source/fingerprint/token logging. |
| T-KPL01-G23-05 | Denial | repeated reconciliation | medium | mitigate | User-triggered bounded read retries with controls fenced during each check; no automatic write replay loop. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>

<source_coverage>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Trust every browser change across recovery | 01-20..01-23 | COVERED | All three verifier gaps and five review findings have exact tasks/evidence. |
| REQ | GTD-01..02, GTD-04..07, SRV-02 | Previously satisfied Phase 1 behavior | 01-01..01-19 | COVERED | Gap plans preserve these tested paths. |
| REQ | GTD-03 | Today order recovery | 01-22 | COVERED | Global exact-move lock plus real loss test. |
| REQ | SRV-01 | Authentication/session recovery | 01-20, 01-23 | COVERED | Expired reads, concurrent continuations, session reconciliation. |
| REQ | SRV-03 | Stable exact results for conflict/Today/session mutations | 01-21..01-23 | COVERED | Exact identities or authoritative reconciliation. |
| REQ | WEB-01 | Existing real browser daily loop | 01-05, 01-09, 01-13..01-19 | COVERED | Preserved; new E2E extends it. |
| REQ | WEB-02 | Authentication, conflict, retry, unknown states | 01-20..01-23 | COVERED | Current missing recovery states are explicitly tested. |
| REQ | QUAL-01 | Boundary-appropriate deterministic evidence | 01-20..01-23 | COVERED | Focused Vitest plus real Phoenix/PostgreSQL tests. |
| RESEARCH | — | Auth expiry, response-loss, conflict, reorder and browser evidence matrices | 01-20..01-23 | COVERED | Includes every named missing real-stack/deferred/after-commit case. |
| CONTEXT | D-11, D-13, D-17..19, D-22, D-26..29, D-31..38, D-48, D-50..52, D-58..59 | Decisions implicated by gaps | 01-20..01-23 | COVERED | Each applicable action cites its decision IDs. |
| CONTEXT | D-01..10, D-12, D-14..16, D-20..21, D-23..25, D-30, D-39..47, D-49, D-53..57, D-60..63 | Previously implemented decisions | 01-01..01-19 | COVERED | Gap plans neither revise nor reduce them. |
| CONTEXT | Deferred Ideas | Explicitly deferred scope | — | EXCLUDED | Browser-offline, later clients, passkeys, global history, and other deferred breadth remain out of scope. |

</source_coverage>

<verification>Run focused auth/session tests, the new real-stack session suite, then `./tooling/test-phase-1.sh --run`; any high threat, false definitive copy, or automatic write replay blocks completion.</verification>
<success_criteria>Rename, revoke, and logout always reconcile authoritative state after uncertainty and never claim an unproved outcome.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-23-SUMMARY.md`.</output>
