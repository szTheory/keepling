---
phase: KPL-01-one-trustworthy-task
plan: 08
type: execute
wave: 6
depends_on: [01-07]
files_modified:
  - apps/web/src/api/keepling.ts
  - apps/web/src/app/routes.tsx
  - apps/web/src/app/AuthProvider.tsx
  - apps/web/src/app/AppShell.tsx
  - apps/web/src/features/auth/LoginForm.tsx
  - apps/web/src/features/auth/SetupForm.tsx
  - apps/web/src/features/auth/RecoveryReset.tsx
  - apps/web/src/features/auth/Reauthenticate.tsx
  - apps/web/src/features/sessions/SessionList.tsx
  - apps/web/src/features/auth/auth.test.tsx
  - apps/web/e2e/auth-recovery.spec.ts
autonomous: true
requirements: [SRV-01, WEB-02, QUAL-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A routed setup-token form requires an explicit IANA timezone and creates the sole account once; login exposes password reveal/AutoFill and recovery links open a one-use token form (D-05, D-45..D-47)."
    - "Settings/Sessions is reachable from navigation and supports label, revoke-other, and current-session logout flows (D-50, D-52)."
    - "Auth interruption preserves dirty drafts and the original submitted identity through reauthentication (D-51)."
  artifacts:
    - path: "apps/web/src/features/auth/RecoveryReset.tsx"
      provides: "Browser recovery-token consumption flow"
    - path: "apps/web/src/features/sessions/SessionList.tsx"
      provides: "Reachable session inventory and revocation UI"
  key_links:
    - from: "apps/web/src/app/routes.tsx"
      to: "apps/web/src/api/keepling.ts"
      via: "generated auth/session facade routes"
      pattern: "recovery|sessions"
---
<objective>Make closed setup, login, recovery, reauthentication, logout, and session administration reachable and testable in the browser (D-05, D-45..D-47, D-50..D-52).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-07-SUMMARY.md</context>
<scope_exception>The 11 browser files are one reachable auth/session composition: routes, provider, setup/login/recovery forms, Sessions UI, component evidence, and real token E2E must agree on the already generated DTOs. Reads are bounded to these files, the 01-07 contract summary, and the exact web feature/API assignments in 01-PATTERNS.md.</scope_exception>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route setup, login, recovery, and reauthentication</name>
  <files>apps/web/src/api/keepling.ts, apps/web/src/app/routes.tsx, apps/web/src/app/AuthProvider.tsx, apps/web/src/features/auth/SetupForm.tsx, apps/web/src/features/auth/LoginForm.tsx, apps/web/src/features/auth/RecoveryReset.tsx, apps/web/src/features/auth/Reauthenticate.tsx, apps/web/src/features/auth/auth.test.tsx</files>
  <behavior>
    - "A setup URL renders password plus required IANA timezone fields and reaches terminal created/invalid/expired/consumed states without public registration."
    - "Login input has current-password autocomplete, paste support, a named reveal control, and no composition-rule client rejection."
    - "A recovery URL token reaches a new-password form and terminal consumed/expired/invalid states."
    - "Reauthentication preserves pre-submit and submitted-unknown intent with the original identity."
  </behavior>
  <action>Follow 01-PATTERNS.md's API config-seam/no-fetch-analog and feature role-match/no-feature-state-analog guidance. Consume the already generated and drift-checked auth DTOs from Plans 01-06/01-07 through the handwritten facade; do not edit OpenAPI or generated output here. Compose `/setup/:token`, `/login`, `/recover/:token`, and inline reauthentication from routes/AuthProvider. The D-45 setup form requires password and explicit valid IANA timezone per D-05, exposes no public registration entry, and renders created/invalid/expired/consumed terminal states. Implement D-46 reveal/AutoFill controls, D-47 recovery consumption states, and D-51 copy/focus behavior. Never store browser credentials or replace an unknown mutation identity.</action>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/auth/auth.test.tsx</automated></verify>
  <done>Login, recovery, and reauthentication are routed browser capabilities.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Compose reachable Sessions and prove the real flow</name>
  <files>apps/web/src/api/keepling.ts, apps/web/src/app/routes.tsx, apps/web/src/app/AppShell.tsx, apps/web/src/features/sessions/SessionList.tsx, apps/web/src/features/auth/auth.test.tsx, apps/web/e2e/auth-recovery.spec.ts</files>
  <behavior>
    - "Secondary navigation reaches Settings/Sessions and shows every D-50 field."
    - "Revoke other confirms and updates; revoke current uses dirty-aware logout fencing/clear/reload."
    - "Real operator setup and recovery links are each consumed once in Playwright."
  </behavior>
  <action>Follow 01-PATTERNS.md's `apps/web/src/features/**/*` role-match guidance and browser evidence matrix. Add the Sessions navigation/route and facade calls. Implement editable labels, exact revoke confirmations, current marker, client kind, created/coarse activity, and D-52 current-session behavior. In Playwright issue and consume a real operator setup link with an explicit timezone, prove second setup fails, then exercise password reveal/AutoFill attributes, one-use recovery consumption, session label/revoke, auth expiry before submit and after commit/response loss.</action>
  <verify><automated>pnpm --filter @keepling/web test:e2e --grep @auth-recovery</automated><human-check>Use a password manager, keyboard, and VoiceOver for login/recovery/session flows.</human-check></verify>
  <done>D-46, D-47, and D-50 are implemented as reachable UI, not citation-only policy.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V3 Web Frontend, V4 API, V6 Authentication, V7 Session Management, and V8 Authorization.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-17 | Spoofing | browser auth | high | mitigate | Generated DTOs, same-origin cookie/CSRF, rotation, and no credential Web Storage. |
| T-KPL01-18 | Tampering | recovery/session actions | high | mitigate | One-use token states, recent-auth, account scope, exact confirmation. |
| T-KPL01-19 | Repudiation | interrupted auth | high | mitigate | Fixed mutation identity and explicit pre-submit/unknown state machine. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Run auth components and real recovery/session Playwright.</verification>
<success_criteria>A user can sign in, recover, reauthenticate, inspect/label/revoke sessions, and log out without losing or ambiguously replaying intent.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-08-SUMMARY.md`.</output>
