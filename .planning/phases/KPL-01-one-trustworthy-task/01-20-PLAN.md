---
phase: KPL-01-one-trustworthy-task
plan: 20
type: execute
wave: 18
depends_on: [01-19]
files_modified:
  - apps/web/src/app/AuthProvider.tsx
  - apps/web/src/app/routes.tsx
  - apps/web/src/features/tasks/TaskEditor.tsx
  - apps/web/src/features/tasks/task-editor.test.tsx
  - apps/web/src/features/activity/ActivityList.tsx
  - apps/web/src/features/activity/activity-list.test.tsx
  - apps/web/src/features/organizations/OrganizationFields.tsx
  - apps/web/src/features/organizations/organization-fields.test.tsx
  - apps/web/src/features/auth/auth.test.tsx
  - apps/web/e2e/authenticated-read-recovery.spec.ts
autonomous: true
gap_closure: true
requirements: [SRV-01, WEB-02, QUAL-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Expired task-detail, activity-pagination, task-organization, project, and tag reads open one visible reauthentication flow and retry the exact interrupted read in its retained route/task context (D-27, D-51)."
    - "One successful session rotation drains every concurrently registered compatible continuation; no continuation is overwritten and every resume failure remains visible (D-48, D-51)."
    - "Dirty task drafts, pagination state, and route identity survive read authentication recovery, while UI copy and focus follow the approved UI-SPEC (D-11, D-13, D-51)."
  artifacts:
    - path: "apps/web/src/app/AuthProvider.tsx"
      provides: "Keyed concurrent authentication-continuation collection with awaited, visible resume outcomes"
    - path: "apps/web/e2e/authenticated-read-recovery.spec.ts"
      provides: "Real Phoenix/PostgreSQL expired-session proof for every named routed read and concurrent interruption"
  key_links:
    - from: "TaskEditor.tsx, ActivityList.tsx, OrganizationFields.tsx"
      to: "AuthProvider.tsx"
      via: "authentication_required classification and exact read continuation supplied through routes.tsx"
      pattern: "onAuthenticationRequired"
    - from: "AuthProvider.tsx"
      to: "Reauthenticate/LoginForm"
      via: "one rotated CSRF state drains all queued compatible continuations"
      pattern: "completeReauthentication"
---

<objective>Close the authentication-expired read and concurrent-continuation gap so every protected route resumes honestly without losing user context (D-11, D-13, D-27, D-48, D-51).</objective>

## Phase Goal

**As a** Keepling user, **I want to** manage one task end to end, **so that** I can trust every browser change.

<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>

<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md
@.planning/phases/KPL-01-one-trustworthy-task/01-08-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-17-SUMMARY.md</context>

<scope_exception>The 10-file scope is one atomic browser recovery contract: every routed read registers with the single AuthProvider coordinator, and the coordinator must drain all concurrent continuations correctly before any route can claim recovery. Splitting route adoption from coordinator semantics would leave a shipped interval where reads are still overwritten or resumed without visible failure handling; the three tasks keep each implementation/read set bounded while proving the shared contract together.</scope_exception>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resume an expired task-detail read through the real stack</name>
  <files>apps/web/src/app/routes.tsx, apps/web/src/features/tasks/TaskEditor.tsx, apps/web/src/features/tasks/task-editor.test.tsx, apps/web/src/features/activity/ActivityList.tsx, apps/web/e2e/authenticated-read-recovery.spec.ts</files>
  <read_first>apps/web/src/app/routes.tsx; apps/web/src/features/tasks/TaskEditor.tsx; apps/web/src/features/tasks/task-editor.test.tsx; apps/web/src/features/activity/ActivityList.tsx; apps/web/e2e/lifecycle-recovery.spec.ts; .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md sections “Mutation acknowledgement and recovery” and “Focus and keyboard behavior”</read_first>
  <behavior>
    - "Direct navigation to /tasks/:id after session expiry opens the visible sign-in continuation, retains task identity and dirty draft, and retries the same getTask/getTaskActivity reads after rotation."
    - "Initial activity and Load earlier activity authentication expiry resume the exact initial or cursor-bound read without silently resetting pagination."
    - "A failed resumed read renders a named recoverable error and does not become an unhandled promise rejection."
  </behavior>
  <action>Start with the real-stack expired-session task route case, then classify `KeeplingApiError.problem.code === authentication_required` at the TaskEditor and ActivityList read boundaries. Pass the route-owned continuation into ActivityList, capture whether the interrupted operation was initial task load, initial activity load, or cursor pagination, and retry that exact read after the rotated session is accepted. Preserve D-11/D-51 draft and route/task identity, retain D-27 last-good pagination state, and use the UI-SPEC authentication copy/focus behavior. Keep non-authentication failures in their existing named load/retry states and explicitly catch resumed failures. This route wiring and Task 3's coordinator drain are one delivery unit because every new read continuation immediately depends on exact concurrent registration and visible failure handling.</action>
  <acceptance_criteria>Real Phoenix/PostgreSQL navigation to an expired `/tasks/:id` session and expired activity pagination each require sign-in once, return to the same route, restore authoritative content, and preserve any mounted draft and cursor state.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/tasks/task-editor.test.tsx src/features/activity/activity-list.test.tsx &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @authenticated-read-task</automated></verify>
  <fails_when>Any protected task/activity read collapses authentication expiry into a generic error, retries a different route/cursor, loses a dirty draft, or rejects outside an explicit UI state.</fails_when>
  <done>The first real task-detail/activity read path survives genuine session expiry end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend exact read recovery to organizations, projects, and tags</name>
  <files>apps/web/src/features/organizations/OrganizationFields.tsx, apps/web/src/features/organizations/organization-fields.test.tsx, apps/web/e2e/authenticated-read-recovery.spec.ts</files>
  <read_first>apps/web/src/features/organizations/OrganizationFields.tsx; apps/web/src/features/organizations/organization-fields.test.tsx; apps/web/e2e/authenticated-read-recovery.spec.ts; .planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md first gap</read_first>
  <behavior>
    - "Expired `/tasks/:id/organizations` retains task identity and retries the exact task-plus-organization read."
    - "Expired `/projects` and `/tags` retain the requested organization kind and retry that same collection read."
    - "Generic read failure and authentication-required remain distinct visible states."
  </behavior>
  <action>Apply the same route-retaining read-continuation contract to OrganizationFields and OrganizationManager. Per D-17..D-19, retain stable task/organization identity and assignment drafts; do not route around the semantic API or reclassify organization command recovery. Add focused component cases plus real-stack expired-session scenarios for assignment, project management, and tag management.</action>
  <acceptance_criteria>All three organization routes resume after real session expiry with their original route, task/kind context, draft values, and authoritative results intact.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/organizations/organization-fields.test.tsx &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @authenticated-read-organizations</automated></verify>
  <fails_when>Any organization read needs manual navigation/retry after sign-in, restores the wrong kind/task, or overwrites assignment edits.</fails_when>
  <done>Task assignment, Projects, and Tags use the same exact authentication-recovery boundary as task detail and activity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drain concurrent reauthentication continuations safely</name>
  <reversibility rating="reversible">The keyed continuation collection is local browser orchestration behind the existing authentication context contract.</reversibility>
  <files>apps/web/src/app/AuthProvider.tsx, apps/web/src/features/auth/auth.test.tsx, apps/web/e2e/authenticated-read-recovery.spec.ts</files>
  <read_first>apps/web/src/app/AuthProvider.tsx; apps/web/src/features/auth/auth.test.tsx; apps/web/src/features/auth/Reauthenticate.tsx; apps/web/e2e/authenticated-read-recovery.spec.ts; .planning/phases/KPL-01-one-trustworthy-task/01-REVIEW.md finding WR-01</read_first>
  <behavior>
    - "Two simultaneous authentication failures register two continuations without last-writer loss."
    - "One successful sign-in/reauthentication supplies the rotated CSRF token to every compatible continuation exactly once."
    - "Each resume is awaited or caught; one failure remains visibly recoverable and does not suppress the other continuation."
  </behavior>
  <action>Replace the single callback slot with a keyed collection whose key identifies the interrupted operation/identity, deduplicates repeated registration of the same continuation, and preserves arrival order for deterministic tests. Per D-48/D-51, one authentication rotation drains all compatible entries exactly once using the new CSRF token. Keep the overlay active until draining has settled, aggregate failures into a visible recoverable interruption state, and ensure `clearAuthentication` fences the whole collection. Do not log route IDs, mutation IDs, tokens, or task content. Land this coordinator with Tasks 1-2 so no routed read can register against the old single-slot semantics.</action>
  <acceptance_criteria>A deterministic concurrent-interruption test proves neither continuation is overwritten, both receive the rotated CSRF state once, and a failing resume is caught and presented without hiding successful recovery.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/auth/auth.test.tsx &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @authenticated-read-concurrent</automated></verify>
  <fails_when>Only the last callback runs, the overlay clears before drain completion, a callback executes twice, or any rejection escapes as an unhandled promise.</fails_when>
  <done>Concurrent reads and mutations can share one authentication rotation without losing any continuation.</done>
</task>

</tasks>

<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V3 Web Frontend, V6 Authentication, V7 Session Management, V8 Authorization, V14 Data Protection, and V16 Error Handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-G20-01 | Spoofing | resumed authenticated reads | high | mitigate | Resume only after server-issued session/CSRF rotation; every read remains session/account scoped; real-stack expiry tests block completion. |
| T-KPL01-G20-02 | Tampering | concurrent continuation collection | high | mitigate | Keyed deduplication, exact-once drain, deterministic ordering, and continuation-overwrite tests. |
| T-KPL01-G20-03 | Information disclosure | recovery state/errors | high | mitigate | No credentials, raw tokens, task content, or arbitrary identifiers in diagnostics; closed user-visible errors only. |
| T-KPL01-G20-04 | Denial | repeated authentication failures | medium | mitigate | Deduplicate operation keys, bound collection to mounted interrupted work, and fence it on logout/unmount. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>

<source_coverage>
GOAL trust through authentication interruption: covered here. REQ SRV-01/WEB-02/QUAL-01: covered by real-stack and concurrent tests. RESEARCH response-loss/auth-expiry matrix and UI evidence matrix: covered. CONTEXT D-11, D-13, D-17..D-19, D-27, D-48, D-51: implemented directly; other D-01..D-63 decisions remain covered by completed Plans 01-01..01-19 and are not changed. Deferred ideas remain excluded.
</source_coverage>

<verification>Run all focused Vitest cases, the new real-stack expired-read suite, then `./tooling/test-phase-1.sh --run`; any high-severity threat or continuation loss blocks completion.</verification>
<success_criteria>Every named protected read and every concurrent continuation recovers through one visible authentication rotation without losing draft, route, pagination, or exact operation context.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-20-SUMMARY.md`.</output>
