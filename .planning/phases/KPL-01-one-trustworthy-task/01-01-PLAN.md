---
phase: KPL-01-one-trustworthy-task
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md
  - tooling/runtime-versions.env
  - tooling/runtime-preflight.sh
autonomous: false
requirements: [QUAL-01]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every research-flagged Hex/npm package is verified against its official registry and source before installation."
    - "Repository-owned tooling verifies or provisions Elixir 1.20.2 on OTP 29.0.5 and PostgreSQL 18.6 without changing the user's untracked .tool-versions."
  artifacts:
    - path: ".planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md"
      provides: "Per-package official provenance and human disposition"
    - path: "tooling/runtime-versions.env"
      provides: "Exact approved Elixir, OTP, and PostgreSQL version manifest"
    - path: "tooling/runtime-preflight.sh"
      provides: "Executable check/provision/exec gate for the approved runtime"
  key_links:
    - from: "01-RESEARCH.md Package Legitimacy Audit"
      to: "01-PACKAGE-APPROVAL.md"
      via: "blocking human package disposition"
      pattern: "hammer|react-router|tanstack|vitest|testing-library|axe-core"
    - from: "tooling/runtime-versions.env"
      to: "Plans 01-02 through 01-04"
      via: "runtime-preflight.sh --check/--exec after Wave 0"
      pattern: "1.20.2|29.0.5|18.6"
---

<objective>Clear the non-auto-approvable package-legitimacy gate and establish the repository-owned runtime gate before any scaffold, install, or test work.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify and pin every suspicious package</name>
  <files>.planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md</files>
  <action>Fetch official registry metadata and linked source for Hammer, React Router, TanStack Query, Vitest, Testing Library user-event/jest-dom/react, and axe Playwright at the versions proposed by the refreshed registry check. Present version, publisher/maintainers, repository, publish date, license, lifecycle scripts, and provenance. Require explicit approval for each package before any downstream manifest install. Record each approval/rejection with inspected exact version and URLs in the package-approval artifact. Stop for replanning on rejection; do not choose substitutes silently.</action>
  <verify><automated>test -f .planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md &amp;&amp; for field in Package Version Registry Repository Disposition; do rg -n "^${field}:" .planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md &gt;/dev/null; done</automated><human-check>Approve or reject every displayed official registry/source identity and exact version.</human-check></verify>
  <done>Every flagged package has an explicit recorded disposition before Plans 01-02 through 01-04 install approved pins.</done>
  <what-built>A provenance dossier and approved exact dependency pins.</what-built>
  <how-to-verify>Review official Hex/npm pages, linked repositories, publishers, dates, licenses, and scripts.</how-to-verify>
  <resume-signal>Reply `approved` or list rejected package names.</resume-signal>
</task>
<task type="auto">
  <name>Task 2: Pin and gate the researched server runtimes</name>
  <files>tooling/runtime-versions.env, tooling/runtime-preflight.sh</files>
  <action>Follow the exact root-tooling analog in 01-PATTERNS.md: POSIX sh, `set -eu`, repository-root resolution, specific failure diagnostics, and one concise success line. Create a committed manifest containing `ELIXIR_VERSION=1.20.2`, `ELIXIR_ASDF_VERSION=1.20.2-otp-29`, `OTP_VERSION=29.0.5`, and `POSTGRESQL_VERSION=18.6`. Implement `runtime-preflight.sh --check` to select the exact asdf Erlang/Elixir versions through environment variables, select Homebrew `postgresql@18` binaries by absolute prefix, and fail with expected/actual diagnostics unless `elixir --version`, the OTP release probe, `postgres --version`, `psql --version`, and `pg_config --version` all match the manifest. Implement `--provision` with explicit `asdf install erlang 29.0.5`, `asdf install elixir 1.20.2-otp-29`, and `brew install postgresql@18` commands, followed by the same check; fail rather than accept a different patch. Implement `--exec -- COMMAND...` so downstream Mix/PostgreSQL commands run with those selected binaries. Run `--check`; on the researched mismatch, run `--provision`, then require `--check` to pass before completing the task. Before and after either mode, preserve the SHA-256 of `.tool-versions` when it exists; never create, modify, delete, or stage that user-owned untracked file.</action>
  <verify><automated>set -eu; before=absent; if test -f .tool-versions; then before=$(shasum -a 256 .tool-versions); fi; ./tooling/runtime-preflight.sh --check; ./tooling/runtime-preflight.sh --exec -- sh -c 'elixir --version &amp;&amp; erl -noshell -eval '\''io:format("~s~n", [erlang:system_info(otp_release)]), halt().'\'' &amp;&amp; postgres --version &amp;&amp; psql --version'; after=absent; if test -f .tool-versions; then after=$(shasum -a 256 .tool-versions); fi; test "$before" = "$after"</automated></verify>
  <done>The exact researched Elixir/OTP/PostgreSQL runtime passes the executable gate, downstream commands have a stable wrapper, and `.tool-versions` is byte-for-byte unchanged and remains untracked.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V13 Configuration and V15 Secure Architecture.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-SC | Tampering | Hex/npm supply chain | high | mitigate | Official-registry provenance review, exact pins, lockfile, and blocking human approval. |
| T-KPL01-01 | Elevation | lifecycle scripts | medium | mitigate | Inspect scripts and retain pnpm built-dependency controls. |
| T-KPL01-RT | Tampering | runtime/toolchain selection | high | mitigate | Committed exact manifest, explicit provision commands, version-output checks, and an exec wrapper that does not consult the user-owned `.tool-versions`. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>The approval artifact contains official evidence and an explicit disposition for every flagged package; `./tooling/runtime-preflight.sh --check` passes without changing `.tool-versions`.</verification>
<success_criteria>No suspicious dependency enters Keepling without explicit human approval, and Plans 01-02/01-03 cannot begin on the stale local server runtime.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-01-SUMMARY.md`.</output>
