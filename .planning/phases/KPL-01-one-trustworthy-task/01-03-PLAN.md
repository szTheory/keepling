---
phase: KPL-01-one-trustworthy-task
plan: 03
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - apps/web/package.json
  - apps/web/vitest.config.ts
  - apps/web/src/test/setup.ts
  - apps/web/playwright.config.ts
  - apps/web/e2e/support/stack.ts
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/generated/keepling.ts
  - tooling/check-contracts.mjs
  - tooling/run-local-stack.sh
  - tooling/test-phase-1.sh
  - package.json
  - apps/web/README.md
autonomous: true
requirements: [QUAL-01, WEB-01, WEB-02, SRV-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Vitest setup, Playwright real-stack orchestration, contract generation, and native root scripts execute before any browser verification invokes them."
    - "The browser test stack starts Phoenix and PostgreSQL through one deterministic same-origin contract and cleans only owned processes."
  artifacts:
    - path: "apps/web/vitest.config.ts"
      provides: "Vitest/jsdom configuration"
    - path: "apps/web/playwright.config.ts"
      provides: "Real Phoenix/PostgreSQL browser orchestration"
    - path: "tooling/test-phase-1.sh"
      provides: "Fail-fast native-tool phase runner"
  key_links:
    - from: "packages/contracts/openapi/keepling.yaml"
      to: "packages/contracts/generated/keepling.ts"
      via: "deterministic generation and drift check"
      pattern: "contracts:check"
---

<objective>Execute the browser/contracts half of Wave 0 so Vitest, Playwright, generated transport, and native orchestration exist before dependent verification.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md
@.planning/phases/KPL-01-one-trustworthy-task/01-01-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VALIDATION.md
@apps/web/vite.config.ts</context>
<scope_exception>These 12 files form two runnable Wave 0 harness slices: browser runner configuration and the contract/local-stack command chain. Separating a config from its setup/support or OpenAPI source from generated output/drift command would leave a referenced command non-executable; task reads remain bounded to the exact named config, support, contract, and root-tool files.</scope_exception>
<tasks>
<task type="auto">
  <name>Task 1: Install executable Vitest and Playwright orchestration</name>
  <files>apps/web/package.json, apps/web/vitest.config.ts, apps/web/src/test/setup.ts, apps/web/playwright.config.ts, apps/web/e2e/support/stack.ts, apps/web/README.md</files>
  <action>Require `./tooling/runtime-preflight.sh --check` before install/config discovery and use its `--exec --` wrapper for every PostgreSQL/Phoenix command in the Playwright stack. Follow 01-PATTERNS.md's `apps/web/vitest.config.ts, playwright.config.ts, src/test/**/*` config-role analog and its no-test-implementation-analog RESEARCH guidance: configure jsdom, Testing Library matchers/cleanup and deterministic accessible helpers in Vitest. Configure Playwright to start the real PostgreSQL 18.6/Phoenix/Vite stack, seed deterministic account data, capture trace/screenshots, support test-only response drops, and shut down only owned processes. Add native package scripts without watch defaults. This task consumes the package approvals from Plan 01-01.</action>
  <verify><automated>pnpm --filter @keepling/web test --run --passWithNoTests &amp;&amp; pnpm --filter @keepling/web exec playwright test --list</automated></verify>
  <done>Both browser test runners load their config/setup and list cleanly before feature specs exist.</done>
</task>
<task type="auto">
  <name>Task 2: Establish generated-contract and local-stack commands</name>
  <files>packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts, tooling/check-contracts.mjs, tooling/run-local-stack.sh, tooling/test-phase-1.sh, package.json</files>
  <action>Follow 01-PATTERNS.md's `packages/contracts` no-implementation-analog guidance and root `package.json`/`tooling/**/*` exact coordination analog. Create the initial OpenAPI 3.1 document and deterministic generated TypeScript output in the same task, plus `pnpm contracts:check` drift enforcement. Add POSIX root-aware scripts using native Mix/pnpm and `runtime-preflight.sh --exec --` that verify PostgreSQL 18.6, migrate, start the same-origin stack, and fail fast. The Phase 1 runner initially calls only existing lanes and is extended by later plans; it must never report readiness for an absent lane.</action>
  <verify><automated>pnpm contracts:check &amp;&amp; ./tooling/run-local-stack.sh --check-config &amp;&amp; ./tooling/test-phase-1.sh --list</automated></verify>
  <done>All Wave 0 commands referenced by later plans exist and execute their configuration checks.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V3 Web Frontend, V4 API, V13 Configuration, and V15 Secure Architecture.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-04 | Elevation | test fault controls | high | mitigate | Test-only orchestration credential and production route-absence contract. |
| T-KPL01-05 | Tampering | generated contract | high | mitigate | Checked-in OpenAPI source, deterministic generation, and drift failure. |
| T-KPL01-06 | Denial | process cleanup | medium | mitigate | PID ownership and cleanup limited to processes started by the script. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Vitest config, Playwright discovery, contract drift, and local-stack config checks all pass.</verification>
<success_criteria>Dependent plans invoke no missing test config, support helper, or orchestration script.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-03-SUMMARY.md`.</output>
