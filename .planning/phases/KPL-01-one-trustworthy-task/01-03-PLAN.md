---
phase: KPL-01-one-trustworthy-task
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - apps/server/lib/keepling/accounts.ex
  - apps/server/lib/keepling/accounts/session.ex
  - apps/server/lib/keepling_web/auth.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/lib/mix/tasks/keepling.recover.ex
  - apps/server/priv/repo/migrations/20260830000200_expand_auth_lifecycle.exs
  - apps/server/test/keepling_web/auth_test.exs
  - apps/web/src/app/AuthProvider.tsx
  - apps/web/src/features/auth/Reauthenticate.tsx
  - apps/web/src/features/sessions/SessionList.tsx
  - apps/web/src/commands/submission.ts
  - apps/web/src/features/auth/auth.test.tsx
autonomous: true
requirements: [SRV-01, WEB-02, QUAL-01]
estimate: { tokens: 68000, raw_tokens: 68000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Closed setup, long password-manager-friendly Argon2id login, operator recovery, reauthentication, logout, session inventory/labels/revocation, and configurable expiry implement D-45..D-53 without public registration or mandatory email."
    - "Authentication interruption preserves dirty drafts and the original mutation identity, distinguishing not-submitted from submitted-but-unknown before lookup/retry (D-51..D-52)."
    - "Future native/MCP grants remain a separate authorization-code+PKCE/scoped-grant seam; browser cookies/passwords/server secrets are never reused as device credentials (D-54..D-55)."
    - "Auth-required, initial-loading, error, partial, and populated session states use explicit copy and never erase last accepted account state."
  artifacts:
    - { path: "apps/server/lib/keepling/accounts.ex", provides: "Closed identity, recovery, and tracked-session application API" }
    - { path: "apps/server/lib/keepling_web/auth.ex", provides: "Cookie, CSRF/origin, expiry, recent-auth, and revocation plugs" }
    - { path: "apps/web/src/commands/submission.ts", provides: "Draft-preserving auth interruption and exact mutation recovery state machine" }
  key_links:
    - { from: "apps/web/src/commands/submission.ts", to: "apps/server/lib/keepling_web/auth.ex", via: "reauth then mutation lookup/retry with unchanged identity", pattern: "mutationId" }
    - { from: "apps/server/lib/keepling_web/auth.ex", to: "apps/server/lib/keepling/accounts.ex", via: "hash-stored session lifecycle and server-derived principal", pattern: "Accounts" }
  flagged_assumptions:
    - "SRV-01 unclassified edge probe remains unresolved: auth boundary tests must surface uncategorized lifecycle behavior instead of treating successful login as full session proof."
---

<objective>
Expand the skeleton into the complete locked personal-account and browser-session lifecycle, including honest interrupted-auth recovery (D-45..D-55).

Purpose: Make authentication a recoverable boundary around user intent rather than a redirect that loses or ambiguously replays work.
Output: Recovery/operator commands, tracked/revocable sessions, rate limits/audit, React auth/session UI, and deterministic auth-expiry tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-01-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete closed recovery and tracked-session semantics</name>
  <read_first>01-CONTEXT.md D-45..D-50,D-53..D-55; 01-RESEARCH.md Pattern 7 and auth/session checklist; skeleton Accounts/Auth modules</read_first>
  <reversibility rating="costly">Session and future-grant boundaries become compatibility inputs for native clients and MCP, though browser policy remains internally versioned.</reversibility>
  <files>apps/server/lib/keepling/accounts.ex, apps/server/lib/keepling/accounts/session.ex, apps/server/lib/keepling_web/auth.ex, apps/server/lib/keepling_web/router.ex, apps/server/lib/mix/tasks/keepling.recover.ex, apps/server/priv/repo/migrations/20260830000200_expand_auth_lifecycle.exs, apps/server/test/keepling_web/auth_test.exs</files>
  <behavior>
    - "Setup and recovery secrets are short-lived, one-use, hash-stored, race-safe, and never accept passwords through CLI args/env/logs."
    - "Sessions enforce configurable 30-day idle, 180-day absolute, and 15-minute recent-auth defaults; ordinary task writes need no recent-auth."
    - "Every session is labelable/listable/revocable with current marker, client kind, creation time, and coarsened activity; no raw IP/fingerprint is retained."
    - "Setup/login/recovery use independent account/source buckets, generic responses, bounded backoff, and privacy-safe security audit."
  </behavior>
  <action>Write race, expiry-boundary, recovery-consumption, revocation, generic-response, and redaction tests first. D-46: implement long password-manager-friendly Argon2id passwords with measured work factor, paste/AutoFill/reveal, no composition/rotation/security-question/CAPTCHA-first/permanent-lockout policy. D-47: implement the operator one-use recovery link without accepting passwords through CLI arguments, environment variables, or logs. D-49: enforce configurable 30-day idle, 180-day absolute, and 15-minute recent-auth defaults, with ordinary task editing exempt from elevation. Expand auth state and implement `Keepling.Accounts` semantic operations, independent Hammer account/source buckets, privacy-safe security audit, and individual session list/label/revoke routes per D-45..D-53. Keep raw bearer material out of PostgreSQL and diagnostics; preserve a declared separate grant/credential port for later authorization-code+PKCE and scoped MCP grants without implementing deferred native/passkey flows (D-54..D-55).</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling_web/auth_test.exs</automated></verify>
  <acceptance_criteria>Independent-connection setup/recovery races have one winner; all expiry/revocation/recent-auth boundaries are explicit; public registration/email/passkey-only/JWT-storage paths are absent; redaction tests pass.</acceptance_criteria>
  <done>The server owns the entire locked one-account authentication and tracked-session policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve browser intent through reauthentication and logout</name>
  <read_first>01-CONTEXT.md D-11,D-37,D-51..D-52; UI-SPEC mutation acknowledgement, dirty-work dialog, focus/copy contracts; existing App/API facade</read_first>
  <files>apps/web/src/app/AuthProvider.tsx, apps/web/src/features/auth/Reauthenticate.tsx, apps/web/src/features/sessions/SessionList.tsx, apps/web/src/commands/submission.ts, apps/web/src/features/auth/auth.test.tsx</files>
  <behavior>
    - "Pre-submit expiry retains an editable draft and resumes only after sign-in; post-submit unknown retains request bytes and mutation identity, then queries/retries that identity."
    - "Logout with dirty work offers Stay here or explicit discard; confirmed logout revokes server-side, fences in-flight work, clears account cache/CSRF, and reloads."
    - "Session inventory/revocation is keyboard operable and current-session revocation uses the logout flow."
  </behavior>
  <action>Implement the explicit submission state machine separate from TanStack read caching, with `not_submitted`, `in_flight`, `unknown`, `authentication_required`, and terminal states. Render exact `Sign in again to finish saving. Your changes are still here.` / `Sign in and continue` copy, return focus predictably, and preserve the original identity across D-51 recovery. Implement Sessions UI with exact visible metadata, editable label, confirmation copy, and safe current-session flow. On logout, warn for dirty work, revoke first, fence requests, clear account-scoped QueryClient and CSRF state, then reload per D-52.</action>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/auth/auth.test.tsx</automated></verify>
  <acceptance_criteria>Component tests prove both auth-expiry timings, exact-ID recovery, dirty logout choices, cache fencing, accessible names/focus, and no browser credential storage.</acceptance_criteria>
  <done>Authentication transitions preserve or deliberately discard intent with no hard redirect or ambiguous replay.</done>
</task>

<task type="auto">
  <name>Task 3: Prove auth abuse and recovery through the real stack</name>
  <read_first>01-VALIDATION.md manual auth recovery row; 01-RESEARCH.md fault matrix and Known Threat Patterns; task-created auth routes/UI</read_first>
  <files>apps/web/e2e/auth-recovery.spec.ts, apps/server/test/keepling/security_audit_test.exs, apps/server/test/keepling/telemetry_redaction_test.exs</files>
  <action>Add real PostgreSQL/Playwright scenarios for generic failed login and bounded backoff, session fixation rotation, CSRF/origin rejection, revoke-other and revoke-current behavior, expiry before submission, expiry after commit plus dropped response, and dirty-logout cancellation. Capture server audit/telemetry during hostile task/password/token fixtures and assert only allow-listed low-cardinality fields are emitted. Add end-of-phase human-check instructions for password-manager paste/AutoFill/reveal and the intelligibility of reauthentication without exposing secrets.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/security_audit_test.exs test/keepling/telemetry_redaction_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @auth-recovery</automated><human-check>Use keyboard and VoiceOver to exercise login, reauthentication, dirty logout, session label/revocation, and verify copy/focus remain understandable.</human-check></verify>
  <acceptance_criteria>Real-stack evidence covers fixation, theft/revocation, CSRF/origin, rate-limit abuse, both expiry timings, cross-session fencing, and telemetry leakage with no high-severity gap.</acceptance_criteria>
  <done>SRV-01 and auth-related WEB-02 behavior have executable boundary evidence.</done>
</task>
</tasks>

## Artifacts this phase produces

- `Keepling.Accounts` setup/login/recovery/reauth/logout/session-list/label/revoke APIs; expanded session/security-audit migration and `Keepling.Recover` operator command.
- Auth/session Phoenix plugs and versioned routes; browser `AuthProvider`, `Reauthenticate`, `SessionList`, and `submission` state machine.
- ExUnit and Playwright auth/recovery/fixation/CSRF/rate-limit/redaction evidence.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| anonymous browser/operator token → auth | Untrusted password, setup/recovery token, source bucket and cookie state. |
| authenticated browser → session administration | A session may inspect/revoke only sessions for its account and recent-auth policy. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-07 | Spoofing | login/session | high | mitigate | Argon2id, generic response, rotation, hash storage, expiry and revocation. |
| T-KPL01-08 | Tampering | recovery/setup | high | mitigate | Short-lived one-use hash-stored token and transactional consumption/singleton constraints. |
| T-KPL01-09 | Tampering | cookie mutation | high | mitigate | CSRF header plus strict origin/host on every mutation. |
| T-KPL01-10 | Elevation | session revocation | high | mitigate | Account scope, recent-auth for sensitive action, current-session logout fencing. |
| T-KPL01-11 | Denial | setup/login/recovery | medium | mitigate | Independent source/account buckets, bounded backoff, no attacker-triggered permanent lockout. |
| T-KPL01-12 | Information disclosure | security audit/telemetry | high | mitigate | Generic response and allow-listed audit with negative secret/content/identifier tests. |
| T-KPL01-SC | Tampering | installed auth/UI packages | high | mitigate | Depends on blocking-human Plan 01-02 approval and exact pins. |
</threat_model>

<verification>Run server auth/redaction suites plus `@auth-recovery` Playwright against real PostgreSQL; no high-severity threat may remain open.</verification>
<success_criteria>A user can recover, sign in, inspect/revoke sessions, reauthenticate without losing intent, and log out safely under the exact D-45..D-55 contract.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-03-SUMMARY.md` when done.</output>
