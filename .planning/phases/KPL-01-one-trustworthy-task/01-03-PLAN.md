---
phase: KPL-01-one-trustworthy-task
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - apps/server/lib/keepling/application.ex
  - apps/server/lib/keepling_web.ex
  - apps/server/lib/keepling_web/endpoint.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/lib/keepling_web/telemetry.ex
  - apps/server/lib/keepling_web/controllers/error_json.ex
  - apps/server/priv/repo/seeds.exs
  - apps/server/test/test_helper.exs
  - apps/server/test/support/data_case.ex
  - apps/server/test/support/conn_case.ex
  - apps/server/test/support/concurrency_case.ex
  - apps/server/test/support/clock.ex
  - apps/server/test/architecture_test.exs
  - apps/server/test/keepling_web/controllers/error_json_test.exs
autonomous: true
requirements: [QUAL-01, SRV-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Phoenix Endpoint, web namespace, router, telemetry, JSON errors, and seeds exist before any HTTP tracer consumes them."
    - "ExUnit, SQL Sandbox, deterministic time, and real independent-connection barriers exist before server feature verification uses them."
    - "Architecture evidence enforces D-02 dependency direction at the first runnable web boundary."
  artifacts:
    - path: "apps/server/lib/keepling_web/endpoint.ex"
      provides: "Runnable Phoenix request endpoint"
    - path: "apps/server/test/support/concurrency_case.ex"
      provides: "Independent PostgreSQL connection and barrier helpers"
    - path: "apps/server/test/architecture_test.exs"
      provides: "Inward dependency assertions for the semantic boundary"
  key_links:
    - from: "apps/server/lib/keepling_web/endpoint.ex"
      to: "apps/server/lib/keepling/application.ex"
      via: "OTP-supervised Phoenix endpoint"
      pattern: "KeeplingWeb.Endpoint"
    - from: "apps/server/test/test_helper.exs"
      to: "apps/server/test/support/concurrency_case.ex"
      via: "compiled support before feature races"
      pattern: "ConcurrencyCase"
---

<objective>Complete the explicitly enumerated Phoenix web runtime and deterministic test boundary required by every later server slice (D-01, D-02).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md
@.planning/phases/KPL-01-one-trustworthy-task/01-02-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VALIDATION.md</context>
<scope_exception>The 14 files are split into two coherent tasks: Application plus six generated web/runtime outputs, and seven deterministic test outputs including the generated JSON-error test. Together they complete the generator file inventory without hiding a directory glob or overloading Plan 01-02.</scope_exception>
<tasks>
<task type="auto">
  <name>Task 1: Materialize the runnable Phoenix web runtime</name>
  <files>apps/server/lib/keepling/application.ex, apps/server/lib/keepling_web.ex, apps/server/lib/keepling_web/endpoint.ex, apps/server/lib/keepling_web/router.ex, apps/server/lib/keepling_web/telemetry.ex, apps/server/lib/keepling_web/controllers/error_json.ex, apps/server/priv/repo/seeds.exs</files>
  <action>Using the same approved Phoenix generator version and exact flags recorded by Plan 01-02, materialize exactly these remaining runtime outputs from a disposable generator directory. Extend `Keepling.Application` to supervise telemetry and `KeeplingWeb.Endpoint` after Repo, keep the initial router route-empty except for production-safe baseline plumbing, retain structured telemetry without private task/token/identifier fields, and provide deterministic seed entry without creating a public or first-request account. Preserve D-02: web modules may call application ports, while domain/application code never imports this namespace.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix phx.routes'</automated></verify>
  <done>The Phoenix Endpoint, namespace, router, telemetry, JSON error surface, and seed entry all compile before Plan 01-05 adds production routes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove deterministic concurrency and architecture support</name>
  <files>apps/server/test/test_helper.exs, apps/server/test/support/data_case.ex, apps/server/test/support/conn_case.ex, apps/server/test/support/concurrency_case.ex, apps/server/test/support/clock.ex, apps/server/test/architecture_test.exs, apps/server/test/keepling_web/controllers/error_json_test.exs</files>
  <behavior>
    - "Two independently checked-out database connections can pause and release on a deterministic barrier."
    - "Domain/application source imports fail when they point to Phoenix, Ecto schemas, generated clients, UI, MCP, or client persistence."
    - "Endpoint requests run through ConnCase while database tests retain SQL Sandbox ownership."
  </behavior>
  <action>Configure ExUnit and SQL Sandbox, retain the generated JSON-error test, then add DataCase, ConnCase, deterministic clock/ID injection, and ConcurrencyCase using independent PostgreSQL processes and explicit barriers. Add an architecture test for the D-02 inward boundary and a self-test proving the barrier uses distinct connections rather than sequential calls. Keep Ecto schemas and Phoenix connection helpers outside domain/application representations.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/architecture_test.exs'</automated></verify>
  <done>Every later server verification has a runnable Endpoint, ExUnit harness, independent-connection race helper, and executable dependency guard.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V13 Configuration and V15 Secure Architecture.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-05 | Tampering | test database isolation | high | mitigate | Dedicated test Repo, Sandbox ownership, distinct-connection self-test, and explicit barriers. |
| T-KPL01-06 | Elevation | dependency direction | high | mitigate | Compile/xref/source assertions prevent transport or persistence imports in business rules. |
| T-KPL01-07 | Information disclosure | baseline telemetry/errors | high | mitigate | Closed JSON error fields and low-cardinality telemetry metadata before feature routes exist. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Compile the complete server and run the architecture/concurrency support test through the approved runtime wrapper.</verification>
<success_criteria>Plan 01-05 depends on a complete, runnable, explicitly owned Phoenix/Ecto application rather than an incomplete scaffold promise.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-03-SUMMARY.md`.</output>
