---
phase: KPL-01-one-trustworthy-task
plan: 05
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified:
  - apps/server/lib/keepling/domain/task.ex
  - apps/server/lib/keepling/application/commands.ex
  - apps/server/lib/keepling/adapters/postgres/command_store.ex
  - apps/server/lib/keepling_web/auth.ex
  - apps/server/lib/keepling_web/controllers/command_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/priv/repo/migrations/20260830000100_create_core_task_command_tables.exs
  - apps/server/test/keepling/adapters/postgres/idempotency_test.exs
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/generated/keepling.ts
  - apps/web/src/api/keepling.ts
  - apps/web/src/App.tsx
  - apps/web/src/features/capture/QuickCapture.tsx
  - apps/web/e2e/skeleton.spec.ts
autonomous: true
requirements: [GTD-01, SRV-01, SRV-02, SRV-03, WEB-01, QUAL-01]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A seeded closed-account user can authenticate, capture a titled task into explicit Inbox state, receive the exact mutation acknowledgement, and reload it from PostgreSQL (D-10, D-11, D-14, D-31, D-38, D-45, D-48)."
    - "Concurrent duplicate delivery creates one task and one activity and replays the stored result; changed semantics return mutation_identity_reused (D-31, D-32, D-56)."
    - "React reaches the semantic command only through the handwritten facade, generated OpenAPI contract, Phoenix router/controller, and application command boundary (D-02, D-03)."
  artifacts:
    - path: "apps/server/lib/keepling/application/commands.ex"
      provides: "Shared semantic CaptureTask boundary"
    - path: "apps/web/src/features/capture/QuickCapture.tsx"
      provides: "Production browser capture interaction"
    - path: "apps/web/e2e/skeleton.spec.ts"
      provides: "Real browser/Phoenix/PostgreSQL tracer proof"
  key_links:
    - from: "apps/web/src/features/capture/QuickCapture.tsx"
      to: "apps/web/src/api/keepling.ts"
      via: "fixed task and mutation identities"
      pattern: "mutationId"
    - from: "apps/server/lib/keepling_web/controllers/command_controller.ex"
      to: "apps/server/lib/keepling/application/commands.ex"
      via: "authenticated DTO mapping"
      pattern: "Commands.dispatch"
---

<objective>Ship the production Walking Skeleton: one authenticated capture flows React → browser facade → generated contract → Phoenix controller → shared command → PostgreSQL and back (D-01..D-03, D-10..D-11, D-14, D-31..D-32, D-38, D-45, D-48, D-56).</objective>
## Phase Goal

**As a** personal Keepling user, **I want to** authenticate and complete the core task lifecycle through the browser, **so that** I can trust accepted task changes are durably stored and honestly reported.

<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-03-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-04-SUMMARY.md</context>
<scope_exception>The 14-file tracer is intentionally one production vertical path: the migration, pure/application decisions, PostgreSQL interpreter, Phoenix mapping, OpenAPI/generated DTO pair, facade, UI, and real-stack test must land atomically or the end-to-end proof is invalid. Reads are bounded to the named path and exact 01-PATTERNS.md assignments for each crossed boundary.</scope_exception>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Walk one signed-in capture through every production boundary</name>
  <reversibility rating="costly">D-14 explicit Inbox state is canonical storage and a cross-adapter semantic contract; replacing it requires stored-task migration and coordinated command/client/MCP changes.</reversibility>
  <files>apps/server/lib/keepling/domain/task.ex, apps/server/lib/keepling/application/commands.ex, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/lib/keepling_web/auth.ex, apps/server/lib/keepling_web/controllers/command_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/priv/repo/migrations/20260830000100_create_core_task_command_tables.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts, apps/web/src/api/keepling.ts, apps/web/src/App.tsx, apps/web/src/features/capture/QuickCapture.tsx, apps/web/e2e/skeleton.spec.ts</files>
  <behavior>
    - "A real browser authenticates a seeded closed account, captures one title, sees Destination: Inbox, and reloads the same task."
    - "The draft clears only when the response mutation identity matches the submitted identity."
  </behavior>
  <action>Start red with the real-stack E2E happy path. Follow 01-PATTERNS.md's pure-decision, PostgreSQL no-analog transaction, Phoenix no-analog controller, contract boundary, API facade, and feature role-match assignments. Implement the separate D-02 domain, application, Ecto, transport DTO, generated wire, facade, and React representations. Per D-14, the migration owns explicit canonical Inbox task state alongside the singleton account/session core, account-scoped receipts, and base activity row; it does not introduce planned_on or deadline_on. Authenticate the test user with the D-48 cookie/CSRF/origin contract. Expose login fixture, Inbox read, capture command, and mutation lookup routes through a Phoenix controller that calls Commands.dispatch. Render Quick Capture with D-10 Inbox copy; the optional Today control is added by the sole temporal-contract owner in Plan 01-12. Preserve title and fixed identities until exact D-38 acknowledgement.</action>
  <verify><automated>pnpm --filter @keepling/web test:e2e --grep @skeleton</automated></verify>
  <done>One production-quality authenticated capture works end to end and remains the skeleton later slices extend.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Make first delivery and replay transactionally exact</name>
  <files>apps/server/lib/keepling/application/commands.ex, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/test/keepling/adapters/postgres/idempotency_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts, apps/web/src/api/keepling.ts</files>
  <behavior>
    - "Two independent connections deliver the same fingerprint once and receive semantically identical stored envelopes."
    - "A changed fingerprint under the same identity returns stable mutation_identity_reused and changes no task/activity."
    - "Authenticated semantic terminal outcomes are stored; pre-auth, CSRF/origin, and undecodable requests are not receipted."
  </behavior>
  <action>Follow 01-PATTERNS.md's PostgreSQL `Implementation analog: none` guidance and its Stable Semantic Results pattern. Use the already-created ConcurrencyCase barrier. Make the account-scoped unique receipt constraint arbitrate first delivery, store the terminal D-32 result with the snapshot/revision and one D-56 activity in the same transaction, and distinguish infrastructure failure from committed semantic outcomes. Update OpenAPI/generated facade mapping for exact acknowledgement and RFC 9457 errors. The detailed terminal receipt classes and account-lifetime retention are those resolved in 01-RESEARCH.md.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/adapters/postgres/idempotency_test.exs' &amp;&amp; pnpm contracts:check</automated></verify>
  <done>The tracer proves stable replay and the shared semantic boundary plus web/API adapter portion of SRV-02.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V2 Business Logic, V4 API, V7 Session Management, V8 Authorization, V14 Data Protection, and V15 Secure Architecture.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-09 | Spoofing | seeded login/session | high | mitigate | Opaque hashed session, rotation, CSRF/origin/host checks, and account scope. |
| T-KPL01-10 | Tampering | command receipt | high | mitigate | Unique account/mutation key, semantic fingerprint, stored terminal result, independent-connection race test. |
| T-KPL01-11 | Elevation | task/result lookup | high | mitigate | Session-derived account predicate; identifiers never authorize. |
| T-KPL01-12 | Information disclosure | telemetry/errors | high | mitigate | Closed Problems and allow-listed fields with no task content, raw token, or arbitrary identifier. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Run the skeleton E2E, idempotency race, architecture test, and contract drift.</verification>
<success_criteria>The Walking Skeleton is executable with real PostgreSQL and no direct React-to-Elixir or controller-to-Ecto bypass.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-05-SUMMARY.md`.</output>
