---
phase: KPL-01-one-trustworthy-task
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - apps/server/lib/keepling/domain/task_dates.ex
  - apps/server/lib/keepling/application/task_views.ex
  - apps/server/lib/keepling/adapters/postgres/task_views.ex
  - apps/server/priv/repo/migrations/20260830000400_add_task_dates_today_order.exs
  - apps/server/test/keepling/domain/task_dates_test.exs
  - apps/server/test/keepling/adapters/postgres/task_views_test.exs
  - apps/web/src/features/lists/TaskList.tsx
  - apps/web/src/features/lists/TodayList.tsx
  - apps/web/src/features/lists/UpcomingList.tsx
  - apps/web/src/features/lists/task-lists.test.tsx
  - packages/contracts/vectors/task-dates.json
autonomous: true
requirements: [GTD-03, GTD-04, WEB-01, WEB-02, SRV-03]
estimate: { tokens: 72000, raw_tokens: 72000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Nullable planned_on and deadline_on are civil dates; account IANA timezone plus injected UTC instant defines account day, with no persisted is_today or device-timezone membership (D-04..D-05)."
    - "Today includes active unfinished tasks whose planned/deadline date is on or before account day and labels planned/deadline/overdue/combined reasons; planned-after-deadline saves both with the locked warning (D-06..D-07)."
    - "Inbox, Today, Upcoming, Completed and pagination/reorder use deterministic complete sort tuples, explicit Load more, stale revisions, acknowledgement-before-move, focus restoration and reduced-motion behavior (D-21..D-30)."
    - "Repeated plan/unplan and concurrent Today moves have stable idempotent outcomes; one scoped-order winner leaves no duplicate positions."
    - statement: "Temporal vectors cover equal dates, DST transitions, null/single date combinations, and stable ordering when sort values compare equal."
      verification: backstop
  artifacts:
    - { path: "apps/server/lib/keepling/domain/task_dates.ex", provides: "Account-day/date truth table and membership reasons" }
    - { path: "apps/server/lib/keepling/adapters/postgres/task_views.ex", provides: "Account-bound list queries, cursors and Today ordering" }
    - { path: "apps/web/src/features/lists/TodayList.tsx", provides: "Overdue/Today sections and acknowledged semantic ordering" }
  key_links:
    - { from: "task-dates.json", to: "task_dates.ex and TaskList.tsx", via: "shared storage-neutral membership/reason vectors", pattern: "account_timezone" }
    - { from: "TodayList.tsx", to: "commands.ex", via: "move-before/move-after with expected scoped order revision", pattern: "order_revision" }
  flagged_assumptions:
    - "GTD-04 unclassified edge probe remains unresolved; date truth-table generation must surface uncategorized account-timezone behavior."
---

<objective>Deliver timezone-correct Today and Upcoming plus deterministic list ordering/pagination without conflating intent and deadline (D-04..D-08, D-21..D-30).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Encode account-day temporal truth and plan/unplan commands</name>
  <read_first>01-CONTEXT.md D-04..D-08,D-31..D-34; RESEARCH Pattern 4 and Open Question 4; existing Task/editor contracts</read_first>
  <reversibility rating="costly">Civil-date fields and account-day semantics are persisted/shared contracts; changing them later migrates data and every client.</reversibility>
  <files>apps/server/lib/keepling/domain/task_dates.ex, apps/server/lib/keepling/application/commands.ex, apps/server/priv/repo/migrations/20260830000400_add_task_dates_today_order.exs, apps/server/test/keepling/domain/task_dates_test.exs, packages/contracts/vectors/task-dates.json</files>
  <behavior>
    - "Separate nullable dates round-trip; exact times/start/reminders/recurrence are rejected as unsupported fields."
    - "Plan for Today resolves to accepted account civil date; unplan clears planned_on only; deadline is untouched."
    - "Past planned date classifies as Planned overdue; past deadline as Overdue deadline; both reasons remain explicit and a later future deadline also appears in Upcoming."
  </behavior>
  <action>Write a complete two-date/account-day table before code, including null/equal/past/today/future/DST cases and the classification choice documented above. Configure tzdata and require explicit setup-time IANA timezone (no deployment/device default); timezone changes remain operator-mediated in this phase and never rewrite dates. Implement pure membership/reason functions and semantic plan/unplan commands with stable replay, preserving both dates and emitting `planned_after_deadline` without blocking, per D-04..D-08. Keep unsupported temporal fields absent from schemas.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/domain/task_dates_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm contracts:check</automated></verify>
  <acceptance_criteria>The full truth table is deterministic across IANA/DST boundaries, Today intent never changes deadline, warning preserves both values, and device/deployment timezone cannot influence membership.</acceptance_criteria>
  <done>GTD-03 temporal semantics are explicit and storage-neutral.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Query Inbox, Today, Upcoming and Completed with stable cursors/order</name>
  <read_first>01-CONTEXT.md D-15,D-21..D-27; RESEARCH cursor/reorder fault matrix; task-created temporal vectors</read_first>
  <reversibility rating="costly">Today semantic move commands and scoped order revisions become convergence contracts for offline clients.</reversibility>
  <files>apps/server/lib/keepling/application/task_views.ex, apps/server/lib/keepling/adapters/postgres/task_views.ex, apps/server/test/keepling/adapters/postgres/task_views_test.exs, packages/contracts/openapi/keepling.yaml</files>
  <behavior>
    - "Inbox newest-capture then stable ID; Today fixed Overdue/Today plus manual order; Upcoming next future relevant date; Completed today/newest and older groups."
    - "Opaque HMAC cursors bind account, full unique tuple and view/order revision; stale cursor returns refresh action."
    - "Competing expected order revisions yield one accepted order and one stale/conflict with no duplicate rank."
  </behavior>
  <action>D-22: implement fixed Overdue/Today sections, acknowledged manual within-section ordering, deterministic fallback placement, and semantic move-before/move-after with expected scoped order revision. D-23: implement read-only Upcoming grouped by next future relevant account-zone civil date while preserving simultaneous Today and later-deadline reasons. D-26: implement opaque account-bound keyset cursors over full unique tuples plus view/order revision, explicit Load more, and stale-refresh outcome. Build server-owned projections and indexed keyset queries with explicit page limits and HMAC integrity. Use per-view revision invalidation documented in vectors: Today reorder changes Today order revision; membership-affecting accepted commands change the affected view revision; activity uses its separate revision; inserts make an older page cursor stale rather than claiming gap-free continuation. Use dense integer internal positions behind the semantic commands and transactional section renumbering; clients never submit ranks. Add independent-connection reorder and cursor-invalidation tests and preserve explicit Inbox state across completion/Trash visibility changes.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/adapters/postgres/task_views_test.exs</automated></verify>
  <acceptance_criteria>All view sort/group/membership cases match vectors; pages neither rely on offsets nor cross accounts; stale views fail explicitly; reorder races converge without silent wins.</acceptance_criteria>
  <done>Server projections provide deterministic GTD-04 view truth and Today ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render honest list states and acknowledged Today ordering</name>
  <read_first>UI-SPEC Responsive Workspace, Lists and lifecycle, mutation acknowledgement, focus/motion/copy; PATTERNS web feature section; server view contract</read_first>
  <files>apps/web/src/features/lists/TaskList.tsx, apps/web/src/features/lists/TodayList.tsx, apps/web/src/features/lists/UpcomingList.tsx, apps/web/src/features/lists/task-lists.test.tsx, apps/web/src/app/routes.tsx</files>
  <behavior>
    - "Initial load shows named structural skeleton, zero only after authoritative response, background failure keeps rows and Retry."
    - "Today reason is text, row menu exposes Move earlier/later, pointer drag is enhancement, and row moves after acknowledgement only."
    - "Load more focuses first appended row; removal focus falls next/previous/heading; reduced motion never carries correctness."
  </behavior>
  <action>Build semantic `ul/li` list routes for Inbox, Today, Upcoming and Completed, with exact empty/error/updating/stale copy, fixed headings, readable combined reasons, and explicit buttons. Use TanStack Query only for reads/last-good content and existing command state for moves. Preserve keyed focus/scroll across refresh and narrow Back, expose pointer drag only alongside menu controls, and implement 160/180ms limits plus reduced-motion/forced-color rules per D-27..D-29. Cover zero/one/many and long-content geometry without ARIA grids or infinite scroll.</action>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/lists/task-lists.test.tsx</automated></verify>
  <acceptance_criteria>All list routes distinguish loading/empty/updating/error/stale, display correct reasons/groups, acknowledge before reorder/removal, restore focus deterministically, and remain keyboard/reduced-motion usable.</acceptance_criteria>
  <done>A user can understand and operate Inbox, Today and Upcoming under explicit account-time semantics.</done>
</task>
</tasks>

## Artifacts this phase produces
- Date/order migration; `TaskDates` truth functions; plan/unplan and move-before/move-after commands; view query/cursor APIs and complete golden truth table.
- Semantic `TaskList`, `TodayList`, `UpcomingList`, Completed route, explicit empty/loading/updating/error/stale states, reason labels, pagination and focus/reduced-motion behavior.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| configured account timezone/clock → projections | Time configuration controls user-visible membership and expiry boundaries. |
| browser cursor/order command → PostgreSQL | Opaque cursor and neighbor IDs are untrusted and may be replayed across accounts/revisions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-18 | Tampering | cursor/order command | high | mitigate | HMAC integrity, session account scope, full tuple/revision, expected order revision and row locks. |
| T-KPL01-19 | Information disclosure | list cursors | medium | mitigate | Opaque signed payload plus server account predicate; no sensitive content embedded. |
| T-KPL01-20 | Denial | list queries | medium | mitigate | Bounded keyset pages, covering indexes and query timeout tests. |
| T-KPL01-21 | Repudiation | membership/reorder | medium | mitigate | Exact acknowledgement and accepted activity/revision evidence. |
| T-KPL01-SC | Tampering | query/router packages | high | mitigate | Exact pins approved by Plan 01-02. |
</threat_model>
<verification>Run temporal/vector/query/reorder suites and list component interactions; capture real-stack Today/Upcoming smoke in the final evidence plan.</verification>
<success_criteria>GTD-03 and GTD-04 are usable and deterministic across equal, null, DST, replay, cursor, and concurrent-order boundaries.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-05-SUMMARY.md` when done.</output>
