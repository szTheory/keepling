---
phase: KPL-01-one-trustworthy-task
plan: 26
type: execute
wave: 21
depends_on: [01-25]
files_modified:
  - apps/web/e2e/modal-keyboard.spec.ts
  - apps/web/src/app/AppShell.tsx
  - apps/web/src/components/ui/alert-dialog.tsx
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/features/capture/QuickCapture.tsx
  - apps/web/src/features/sessions/SessionList.tsx
  - apps/web/src/features/sessions/session-list.test.tsx
  - apps/web/src/features/tasks/TaskEditor.tsx
  - apps/web/src/features/tasks/task-editor.test.tsx
  - apps/web/src/index.css
  - apps/web/src/test/ui-contract.test.tsx
  - packages/design-tokens/css.css
  - packages/design-tokens/tokens.json
autonomous: true
gap_closure: true
requirements: [GTD-01, SRV-03, WEB-01, WEB-02, QUAL-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every destructive or dirty-work confirmation uses one accessible Alert Dialog primitive with Escape, focus containment, safe initial focus, and trigger focus return."
    - "Dirty-edit, capture authentication, and session confirmation copy matches the approved UI contract while preserving exact recovery semantics."
    - "Shared Button variants and modal/shell styling use the declared typography, weight, spacing, color, and target-size tokens with no hardcoded destructive foreground."
  artifacts:
    - path: apps/web/src/components/ui/alert-dialog.tsx
      provides: "Approved accessible consequential-confirmation primitive"
    - path: apps/web/e2e/modal-keyboard.spec.ts
      provides: "Keyboard/focus proof for each consequential modal"
    - path: packages/design-tokens/tokens.json
      provides: "Semantic supporting colors and component-scale source of truth"
  key_links:
    - from: apps/web/src/features/tasks/TaskEditor.tsx
      to: apps/web/src/components/ui/alert-dialog.tsx
      via: "dirty navigation confirmation with preserved draft"
    - from: apps/web/src/features/sessions/SessionList.tsx
      to: apps/web/src/components/ui/alert-dialog.tsx
      via: "session revoke confirmation"
    - from: apps/web/src/components/ui/button.tsx
      to: packages/design-tokens/css.css
      via: "semantic type, spacing, foreground, and target-size variables"
---

<objective>
Replace hand-built consequential overlays with approved accessible primitives and harden the shared copy/token contract.

Purpose: Ensure risky browser actions are understandable, keyboard-complete, focus-safe, and visually derived from the checked-in token source.
Output: Shared Alert Dialog, exact recovery/session copy, modal keyboard tests, and token-compliant shared component variants.
</objective>

<scope_rationale>The shared Alert Dialog, exact consequential copy, and semantic Button/modal tokens form one user-decision boundary: splitting them would leave some destructive actions on contradictory focus/copy/styling contracts, while the remaining broad mechanical spacing cleanup is already isolated in Plan 27.</scope_rationale>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/brand/BRAND-SEED.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-REVIEW.md
@.planning/phases/KPL-01-one-trustworthy-task/01-25-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Put dirty navigation and capture reauthentication on the exact contract</name>
  <files>apps/web/src/components/ui/alert-dialog.tsx, apps/web/src/features/tasks/TaskEditor.tsx, apps/web/src/features/capture/QuickCapture.tsx, apps/web/src/features/tasks/task-editor.test.tsx, apps/web/e2e/modal-keyboard.spec.ts</files>
  <read_first>apps/web/src/features/tasks/TaskEditor.tsx, apps/web/src/features/capture/QuickCapture.tsx, apps/web/src/features/tasks/task-editor.test.tsx, apps/web/e2e/phase1.spec.ts, .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md, .planning/phases/KPL-01-one-trustworthy-task/01-UI-REVIEW.md</read_first>
  <behavior>
    - "Dirty navigation opens an Alert Dialog headed `Discard unsaved changes?` with body `These edits haven’t been saved.` and explicit Save changes, Discard changes, and Keep editing choices."
    - "Initial focus is Keep editing, Tab/Shift-Tab wrap, Escape chooses Keep editing, and every close path restores focus to the initiating control."
    - "Save remains exact acknowledgement-gated; Discard navigates without saving; Keep editing and Escape preserve every field and the original mutation identity."
    - "Capture authentication interruption says exactly `Sign in again to finish saving. Your changes are still here.` with a visible `Sign in and continue`; the existing uncertain-delivery copy remains reserved for submitted-but-unknown work."
  </behavior>
  <action>Create the local Alert Dialog wrapper from the already-installed Base UI primitive and replace TaskEditor's hand-built `role="alertdialog"` overlay. Implement D-12 exactly: heading `Discard unsaved changes?`, body `These edits haven’t been saved.`, actions `Save changes`, `Discard changes`, and `Keep editing`, with initial focus on `Keep editing`. Preserve acknowledgement and mutation-identity gates from D-11/D-37, contain Tab, make Escape choose Keep editing, and return focus to the exact trigger. Correct QuickCapture's authentication-expiry state to show the exact two-sentence copy `Sign in again to finish saving. Your changes are still here.` plus a visible `Sign in and continue` action; do not collapse authentication-required and uncertain-delivery states. Start `modal-keyboard.spec.ts` with a real dirty-editor/capture tracer tagged `@modal-keyboard` and assert all exact strings, all three dirty actions, Escape, and focus return.</action>
  <acceptance_criteria>Dirty navigation exposes the exact D-12 three-action dialog, and capture auth expiry exposes the exact two-sentence copy plus visible `Sign in and continue`, without losing text, minting identity, or misreporting save state.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @keepling/web test --run src/features/tasks/task-editor.test.tsx &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @modal-keyboard</automated>
  </verify>
  <fails_when>A hand-built alertdialog remains in TaskEditor, any of the three D-12 actions is absent, initial focus is not Keep editing, Escape discards/saves, focus escapes or is lost, exact capture copy/action differs, dirty data changes on Keep editing, or authentication-required copy is used for an uncertain result.</fails_when>
  <done>The dirty-work dialog is D-12-exact and keyboard/focus complete; capture reauthentication shows the exact preserved-change copy and visible continuation action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidate logout and session revocation confirmations</name>
  <files>apps/web/src/app/AppShell.tsx, apps/web/src/features/sessions/SessionList.tsx, apps/web/src/features/sessions/session-list.test.tsx, apps/web/src/test/ui-contract.test.tsx, apps/web/e2e/modal-keyboard.spec.ts</files>
  <read_first>apps/web/src/app/AppShell.tsx, apps/web/src/features/sessions/SessionList.tsx, apps/web/src/features/sessions/session-list.test.tsx, apps/web/src/test/ui-contract.test.tsx, apps/web/e2e/modal-keyboard.spec.ts, .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md</read_first>
  <behavior>
    - "Logout with dirty work uses the shared Alert Dialog with D-12 actions Save changes, Discard changes and log out, and Keep editing; initial focus is Keep editing and no session changes before explicit destructive activation."
    - "Another-session confirmation is exactly `Revoke {session label}?` with body `Keepling on that device will need to sign in again.`, actions `Keep session active` / `Revoke session`, and initial focus `Keep session active`."
    - "The current-session control delegates to the existing logout flow and its confirmation explicitly says the current browser signs out; it never uses the other-session revoke command."
    - "Escape or the safe action leaves session state unchanged; destructive activation is single-flight; focus returns to the originating session button when the row remains."
    - "No production feature retains a hand-authored `role=alertdialog` overlay."
  </behavior>
  <action>Replace AppShell's dirty-logout overlay and SessionList's other-session revoke overlay with the shared Alert Dialog. For another session, render exact heading `Revoke {session label}?`, exact body `Keepling on that device will need to sign in again.`, and actions `Keep session active` / `Revoke session`, initially focusing `Keep session active`. Route the current-session action into AppShell's existing logout flow rather than `DELETE /sessions/:id`; its confirmation must explicitly state that the current browser signs out and must retain D-12's Save changes / Discard changes and log out / Keep editing dirty-work choices when applicable. Preserve Plan 23's disabled/submitted/uncertain reconciliation only for the other-session revoke command. Add component and Playwright cases for exact strings, Escape, forward/reverse focus wrap, trigger return, current-session logout delegation, absence of a current-session DELETE, other-session reconciliation, and double activation. Extend `ui-contract.test.tsx` to require the approved wrapper.</action>
  <acceptance_criteria>Other-session revocation uses the exact label/body/actions and recovery lock, while current-session revocation uses the existing logout flow with explicit current-browser sign-out wording and D-12 dirty-work protection.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @keepling/web test --run src/features/sessions/session-list.test.tsx src/test/ui-contract.test.tsx &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @modal-keyboard</automated>
  </verify>
  <fails_when>Any ad hoc alertdialog remains, safe action/Escape mutates state, another-session exact copy/action differs, the current session calls the revoke endpoint instead of logout, current-browser sign-out is not explicit, D-12 dirty-work choices are missing, duplicate destructive requests can start, focus is not contained/restored, or other-session reconciliation is bypassed.</fails_when>
  <done>Dirty logout, exact other-session revocation, and current-session logout delegation use the approved accessible confirmation contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move shared component styling onto declared semantic tokens</name>
  <files>packages/design-tokens/tokens.json, packages/design-tokens/css.css, apps/web/src/index.css, apps/web/src/components/ui/button.tsx, apps/web/src/test/ui-contract.test.tsx</files>
  <read_first>packages/design-tokens/tokens.json, packages/design-tokens/css.css, apps/web/src/index.css, apps/web/src/components/ui/button.tsx, apps/web/src/test/ui-contract.test.tsx, docs/brand/BRAND-SEED.md, .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md</read_first>
  <behavior>
    - "Supporting semantic colors used by dialogs, destructive buttons, recovery states, and muted text originate in tokens.json and have light/dark CSS outputs."
    - "Button variants use only 14px or 16px type, weight 600, 4/8/16px spacing increments, and a 44px minimum interactive target unless the control is provably inline."
    - "Destructive foreground is semantic in both themes and no component hardcodes `text-white`."
    - "A source contract test rejects undeclared Button/modal/shell type, weight, spacing, target, color, and the removed fixed workspace arithmetic."
  </behavior>
  <action>Add the supporting semantic roles required by shipped components to the DTCG-compatible token source and update its checked-in CSS output without promoting provisional brand colors to approved product roles. Map Tailwind theme variables in `index.css` to those roles. Normalize every Button size/variant to the UI-SPEC typography (14/16 only), weight (400/600 only), spacing scale, semantic destructive foreground, and 44px target contract; remove `text-xs`, `font-medium`, off-scale gaps/padding, and undersized shared variants. Extend `ui-contract.test.tsx` with region-scoped source assertions for the shared Button, Alert Dialog, WorkspaceShell, AppShell, and TaskEditor; comments and test fixtures must not self-satisfy the negative gates. Plan 27 will expand the spacing gate across remaining feature files.</action>
  <acceptance_criteria>Shared primitives and the shell/modal surfaces consume checked-in semantic tokens and fail tests on undeclared visual values.</acceptance_criteria>
  <verify>
    <automated>pnpm --filter @keepling/web test --run src/test/ui-contract.test.tsx &amp;&amp; pnpm --filter @keepling/web lint &amp;&amp; pnpm --filter @keepling/web typecheck</automated>
  </verify>
  <fails_when>Supporting colors remain hardcoded, generated CSS drifts from tokens.json, Button exposes off-contract type/weight/spacing/height, destructive text is hardcoded, fixed workspace arithmetic returns, or the negative gate can pass from comments/fixtures.</fails_when>
  <done>The shared visual primitives and consequential surfaces have an enforceable semantic token contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consequential trigger → modal decision | The overlay must accurately identify the action and prevent background activation. |
| Dirty draft → navigation/logout | User content and exact mutation identity must survive safe cancellation. |
| Token source → generated browser theme | Security/error affordances must remain distinguishable and contrast-capable in both themes. |

## STRIDE Threat Register — OWASP ASVS Level 1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-KPL01-26-01 | Spoofing | Consequential confirmation copy | medium | mitigate | Exact action/target copy and shared modal semantics prevent ambiguous destructive prompts. |
| T-KPL01-26-02 | Tampering | Modal background and focus | medium | mitigate | Inert background, focus containment, safe default, Escape, and trigger return. |
| T-KPL01-26-03 | Repudiation | Session revoke/logout activation | medium | mitigate | Single-flight explicit activation remains tied to Plan 23 reconciliation and result copy. |
| T-KPL01-26-04 | Information disclosure | Dirty task content | medium | mitigate | Cancel/Escape preserve content in the existing editor only; copy never emits content to telemetry. |
| T-KPL01-26-SC | Tampering | Dependency supply chain | low | accept | Existing Base UI is reused; no dependency install occurs. |
</threat_model>

<verification>
Run all focused commands and `./tooling/test-phase-1.sh --run`. Keyboard DOM behavior is automatic; real VoiceOver announcement quality and perceptual modal review remain explicit human UAT.
</verification>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Trust every browser change | 01-26 | COVERED | Consequential choices are explicit, safe, and recoverable. |
| REQ | GTD-01/SRV-03 | Capture and session behavior | 01-26 | COVERED | Exact auth and revoke copy preserve existing semantics. |
| REQ | WEB-01/WEB-02/QUAL-01 | Browser interaction, recovery, quality | 01-26 | COVERED | Modal keyboard/focus and token contracts are executable. |
| RESEARCH | — | Approved accessible primitives and semantic tokens | 01-26 | COVERED | Existing Base UI and checked-in token source are used. |
| CONTEXT | D-11/D-12/D-13/D-37/D-50..D-52 | Dirty work, keyboard, exact recovery, sessions | 01-26 | COVERED | Copy and primitive changes retain exact intent. |
| CONTEXT | D-01..D-63 | Other locked decisions | 01-01..01-25 | COVERED | No feature semantics are superseded. |
</source_coverage_audit>

<success_criteria>
- Every consequential overlay uses the approved Alert Dialog primitive and exact contract copy.
- Escape, focus wrap, safe initial focus, focus return, and double-activation behavior have real browser proof.
- Shared Button/modal/shell styles derive from semantic tokens and reject undeclared values.
- Existing recovery, session, and full-phase gates remain green.
</success_criteria>

<output>
Create `.planning/phases/KPL-01-one-trustworthy-task/01-26-SUMMARY.md` when done.
</output>
