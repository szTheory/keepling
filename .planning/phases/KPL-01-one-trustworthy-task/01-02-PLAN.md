---
phase: KPL-01-one-trustworthy-task
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - apps/server/mix.exs
  - apps/web/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [QUAL-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A human verifies official registry identity, source repository, release provenance, and inspected versions before any research-flagged package is installed."
  artifacts:
    - path: "apps/server/mix.exs"
      provides: "Approved pinned Hammer dependency only after checkpoint"
    - path: "apps/web/package.json"
      provides: "Approved pinned router/query/test/a11y dependencies only after checkpoint"
  key_links:
    - from: "01-RESEARCH.md Package Legitimacy Audit"
      to: "apps/server/mix.exs and apps/web/package.json"
      via: "blocking-human approval of each [SUS] package"
      pattern: "react-router|tanstack|vitest|testing-library|axe-core|hammer"
---

<objective>
Clear the mandatory package-legitimacy gate before expansion plans install the research-flagged routing, query, test, accessibility, and rate-limit packages.

Purpose: Prevent supply-chain assumptions from entering the implementation unattended.
Output: Human-recorded approval and exact pinned dependency changes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-01-SUMMARY.md
@apps/server/mix.exs
@apps/web/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify every [SUS] dependency before installation</name>
  <read_first>01-RESEARCH.md `Package Legitimacy Audit` and `Standard Stack`; current apps/server/mix.exs and apps/web/package.json</read_first>
  <files>apps/server/mix.exs, apps/web/package.json, pnpm-lock.yaml</files>
  <action>Fetch current official registry metadata and repository links for `hammer` 7.4.1, `react-router` 8.3.1, `@tanstack/react-query` 5.102.8, `vitest` 4.1.11, `@testing-library/react` 16.3.3, `@testing-library/user-event`, `@testing-library/jest-dom`, and `@axe-core/playwright` 4.13.0. Present package name, exact version, official registry URL, source repository, maintainers/publisher, publish date, scripts, license, and whether it still matches the research audit. Require the user to approve or reject each package; do not install before approval. After approval, pin only approved exact versions with Mix/pnpm and preserve the lockfile. A rejection stops downstream plans for replanning rather than selecting a substitute silently.</action>
  <verify>
    <automated>cd apps/server &amp;&amp; mix deps.tree &amp;&amp; cd ../web &amp;&amp; pnpm list --depth 0</automated>
    <human-check>Confirm each displayed registry project and source repository is the intended well-known package and explicitly approve the listed exact versions.</human-check>
  </verify>
  <acceptance_criteria>Every flagged package has an explicit human disposition; only approved exact versions appear in manifests/lockfile; no package with a rejected or unresolved identity is installed.</acceptance_criteria>
  <done>The supply-chain gate is recorded and downstream plans may use only the approved dependency set.</done>
  <what-built>A registry/repository provenance dossier for every research-flagged package, with no flagged installation performed before this checkpoint.</what-built>
  <how-to-verify>Review each package's official registry page, linked source repository, maintainers/publisher, license, publish date, lifecycle scripts, and exact proposed version. Approve the complete list or name any rejected package; rejection returns the phase to planning.</how-to-verify>
  <resume-signal>Reply `approved` to permit the listed exact pins, or list rejected package names.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- Exact approved `hammer`, `react-router`, TanStack Query, Vitest, Testing Library, and axe dependency pins in their native manifests and `pnpm-lock.yaml`.
- A Plan 01-02 summary recording the registry/repository evidence and per-package human disposition.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| package registry → repository | Third-party package code and metadata enter Keepling's build/runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-SC | Tampering | npm/Hex installs | high | mitigate | Blocking-human verification of official registry identity, source provenance, scripts, license, and exact version before install. |
| T-KPL01-06 | Elevation | install scripts | medium | mitigate | Inspect scripts, retain pnpm built-dependency allow-list, and reject unexpected lifecycle execution. |
</threat_model>

<verification>
The dependency trees contain only explicitly approved versions and the package checkpoint summary records the human decision.
</verification>

<success_criteria>
No [SUS] package enters Keepling without the non-auto-approvable legitimacy decision required by the research audit.
</success_criteria>

<output>
Create `.planning/phases/KPL-01-one-trustworthy-task/01-02-SUMMARY.md` when done.
</output>
