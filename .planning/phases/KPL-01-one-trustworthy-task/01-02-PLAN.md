---
phase: KPL-01-one-trustworthy-task
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - apps/server/.formatter.exs
  - apps/server/.gitignore
  - apps/server/mix.exs
  - apps/server/mix.lock
  - apps/server/README.md
  - apps/server/config/config.exs
  - apps/server/config/dev.exs
  - apps/server/config/prod.exs
  - apps/server/config/runtime.exs
  - apps/server/config/test.exs
  - apps/server/lib/keepling.ex
  - apps/server/lib/keepling/application.ex
  - apps/server/lib/keepling/repo.ex
autonomous: true
requirements: [QUAL-01, SRV-02]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The standalone Phoenix/Ecto application has a complete locked dependency graph, including the researched supported Hammer 7.4.1 release, and environment configuration before any web or test module compiles."
    - "The generated core preserves D-01 and D-02: one root repository, no umbrella or nested Git metadata, and inward-facing domain/application namespaces."
  artifacts:
    - path: "apps/server/mix.lock"
      provides: "Exact approved Hex dependency resolution, including Hammer 7.4.1"
    - path: "apps/server/config/runtime.exs"
      provides: "Runtime database, secret, host, and endpoint configuration seam"
    - path: "apps/server/lib/keepling/application.ex"
      provides: "OTP supervision root for Repo and later adapters"
  key_links:
    - from: "apps/server/mix.exs"
      to: "apps/server/mix.lock"
      via: "exact Hammer 7.4.1 requirement and locked Hex resolution"
      pattern: "hammer.*7\\.4\\.1"
    - from: "apps/server/config/runtime.exs"
      to: "apps/server/lib/keepling/application.ex"
      via: "runtime-owned Repo and endpoint configuration"
      pattern: "DATABASE_URL|SECRET_KEY_BASE"
---

<objective>Materialize the complete standalone server core and exact dependency lock before web runtime or test support is layered onto it (D-01, D-02).</objective>
<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md
@.planning/phases/KPL-01-one-trustworthy-task/01-01-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md</context>
<scope_exception>The 13 explicitly owned files are the smallest runnable Mix/config/OTP core. Phoenix web namespace, Endpoint, router, telemetry, seeds, and ExUnit support are deliberately owned by prerequisite Plan 01-03 so neither task hides generator output or exceeds the file-count gate.</scope_exception>
<tasks>
<task type="auto">
  <name>Task 1: Materialize the exact standalone Mix and OTP core</name>
  <files>apps/server/.formatter.exs, apps/server/.gitignore, apps/server/mix.exs, apps/server/mix.lock, apps/server/lib/keepling.ex, apps/server/lib/keepling/application.ex, apps/server/lib/keepling/repo.ex</files>
  <action>Require `./tooling/runtime-preflight.sh --check` and run Mix commands through its `--exec --` wrapper. Generate a current standalone Phoenix/Ecto application into a disposable directory with `--app keepling --module Keepling --database postgres --no-install --no-html --no-assets --no-live --no-mailer --no-dashboard --binary-id`, using the exact Hex versions approved in Plan 01-01, then copy only the seven listed core outputs into `apps/server`; the remaining repository outputs are enumerated and owned by Plan 01-03. Add the approved exact dependency `{:hammer, "== 7.4.1"}` to `mix.exs` and resolve that exact release into `mix.lock`. This task installs Hammer but keeps `Keepling.Application` supervising Repo only; Plan 01-07 owns the app-defined ETS limiter module, its OTP child, and all D-53 policy wiring. Preserve D-01 by using no umbrella, nested repository, submodule, or generated `.git` directory. Preserve D-02 by keeping `Keepling` and `Keepling.Application` free of transport DTO and React/client dependencies. Commit `mix.lock` with `mix.exs`; do not leave dependency resolution implicit.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix deps.get --check-locked &amp;&amp; mix deps.unlock --check-unused &amp;&amp; mix compile --warnings-as-errors'</automated></verify>
  <done>The exact approved server dependency graph, including Hammer 7.4.1 in both manifest and lockfile, is locked and the standalone OTP/Repo core compiles without relying on files outside the enumerated scaffold chain; D-53 limiter policy ownership remains explicit in Plan 01-07.</done>
</task>
<task type="auto">
  <name>Task 2: Own every server environment configuration</name>
  <files>apps/server/README.md, apps/server/config/config.exs, apps/server/config/dev.exs, apps/server/config/prod.exs, apps/server/config/runtime.exs, apps/server/config/test.exs</files>
  <action>Materialize all five generated environment files explicitly and adapt them to the repository-owned runtime gate. Configure PostgreSQL Repo settings, test SQL Sandbox, production `DATABASE_URL`/`SECRET_KEY_BASE` validation, host/port/IPv6 seams, and endpoint start policy without embedding credentials. Preserve the boundary ownership already stated in `apps/server/README.md` while adding exact preflight, setup, compile, and test commands. Make configuration errors name the missing key and environment; never silently fall back to a device or developer machine setting.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; MIX_ENV=test mix compile --warnings-as-errors &amp;&amp; MIX_ENV=prod mix compile --warnings-as-errors'</automated></verify>
  <done>Development, test, production, and runtime configuration are all present, explicit, and compile under the approved toolchain.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V13 Configuration and V15 Secure Architecture.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-02 | Tampering | dependency resolution | high | mitigate | Human-approved exact Hex inputs, explicit Hammer 7.4.1 manifest pin, committed `mix.lock`, and locked dependency verification. |
| T-KPL01-03 | Information disclosure | runtime configuration | high | mitigate | Required environment validation, no committed credentials, and generic production failure output. |
| T-KPL01-04 | Elevation | dependency direction | high | mitigate | D-02 core namespaces contain no Phoenix/generated/client dependency. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>The locked Mix core compiles in test and production environments through the exact runtime wrapper.</verification>
<success_criteria>Plan 01-03 can add the Endpoint/web/test layer without discovering a missing core generator output or an unlocked dependency.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-02-SUMMARY.md`.</output>
