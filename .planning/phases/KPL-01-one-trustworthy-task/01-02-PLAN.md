---
phase: KPL-01-one-trustworthy-task
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - apps/server/mix.exs
  - apps/server/config/config.exs
  - apps/server/config/test.exs
  - apps/server/lib/keepling/application.ex
  - apps/server/lib/keepling/repo.ex
  - apps/server/test/test_helper.exs
  - apps/server/test/support/data_case.ex
  - apps/server/test/support/conn_case.ex
  - apps/server/test/support/concurrency_case.ex
  - apps/server/test/support/clock.ex
  - apps/server/test/architecture_test.exs
  - apps/server/README.md
autonomous: true
requirements: [QUAL-01, SRV-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ExUnit, SQL Sandbox, deterministic time, and real independent-connection barriers exist before any server verification uses them."
    - "The standalone Phoenix/Ecto scaffold preserves D-01 and D-02 dependency direction without nested Git metadata."
  artifacts:
    - path: "apps/server/test/support/concurrency_case.ex"
      provides: "Independent PostgreSQL connection and barrier helpers"
    - path: "apps/server/test/architecture_test.exs"
      provides: "Inward dependency assertions for the shared semantic boundary"
  key_links:
    - from: "apps/server/test/test_helper.exs"
      to: "apps/server/test/support/concurrency_case.ex"
      via: "compiled support import before feature tests"
      pattern: "ConcurrencyCase"
---

<objective>Execute the server half of Wave 0 so later semantic, persistence, HTTP, and concurrency checks run on real support rather than missing helpers (D-01, D-02).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PACKAGE-APPROVAL.md
@.planning/phases/KPL-01-one-trustworthy-task/01-01-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VALIDATION.md</context>
<scope_exception>These 12 files are one generated server/test-support boundary: splitting Mix/config/application/Repo from the support cases would make neither scaffold nor its first executable proof runnable. Each task reads only its exact generated/config or two support/test files plus the named 01-PATTERNS.md sections.</scope_exception>
<tasks>
<task type="auto">
  <name>Task 1: Scaffold the standalone server and deterministic ExUnit boundary</name>
  <files>apps/server/mix.exs, apps/server/config/config.exs, apps/server/config/test.exs, apps/server/lib/keepling/application.ex, apps/server/lib/keepling/repo.ex, apps/server/test/test_helper.exs, apps/server/test/support/data_case.ex, apps/server/test/support/conn_case.ex, apps/server/test/support/clock.ex, apps/server/README.md</files>
  <action>First require `./tooling/runtime-preflight.sh --check` and run every Mix/scaffold command through `./tooling/runtime-preflight.sh --exec --`; stop with its expected/actual diagnostic on any Elixir 1.20.2, OTP 29.0.5, or PostgreSQL 18.6 mismatch. Follow 01-PATTERNS.md's `apps/server/mix.exs, config/**/*, generated runtime scaffold` boundary-doc-only assignment and its Phoenix generator guidance: generate the standalone non-LiveView Phoenix/Ecto application in place without nested Git metadata or an umbrella, preserving D-01. Install only exact Hex versions approved in Plan 01-01. Configure the test Repo/SQL Sandbox and deterministic clock/ID injection before any feature test is added. Keep domain and application namespaces independent of Phoenix and Ecto per D-02. Document exact native Mix setup and test commands.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test'</automated></verify>
  <done>The server scaffold compiles and the empty ExUnit harness executes against test configuration.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove concurrency and architecture support before feature use</name>
  <files>apps/server/test/support/concurrency_case.ex, apps/server/test/architecture_test.exs</files>
  <behavior>
    - "Two independently checked-out database connections can pause and release on a deterministic barrier."
    - "Domain/application source imports fail when they point to Phoenix, Ecto schemas, generated clients, UI, MCP, or client persistence."
  </behavior>
  <action>Follow 01-PATTERNS.md's `Server test support and *_test.exs files` no-implementation-analog guidance: use the RESEARCH Wave 0 support paths, independent PostgreSQL connections/processes, and explicit barriers. Create the independent-connection helper before any idempotency/reorder/undo test references it. Add source/xref architecture assertions for the D-02 inward boundary and a self-test proving the barrier exercises distinct connections rather than sequential calls.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/architecture_test.exs'</automated></verify>
  <done>Later server verifications have executable concurrency and boundary support.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V13 Configuration and V15 Secure Architecture.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-02 | Tampering | test database isolation | high | mitigate | Dedicated test Repo, Sandbox ownership, distinct-connection self-test, and explicit barriers. |
| T-KPL01-03 | Elevation | dependency direction | high | mitigate | Compile/xref/source assertions prevent transport or persistence imports in business rules. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Run the full empty server suite before any later plan.</verification>
<success_criteria>Server Wave 0 is executable and no later test refers to missing support.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-02-SUMMARY.md`.</output>
