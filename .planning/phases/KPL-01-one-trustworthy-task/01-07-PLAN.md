---
phase: KPL-01-one-trustworthy-task
plan: 07
type: execute
wave: 7
depends_on: [01-06]
files_modified:
  - apps/server/lib/keepling/application/undo.ex
  - apps/server/lib/keepling/adapters/postgres/command_store.ex
  - apps/server/priv/repo/migrations/20260830000600_add_undo_handles.exs
  - apps/server/test/keepling/application/undo_test.exs
  - apps/server/test/keepling/domain/long_sequence_test.exs
  - apps/server/test/keepling/telemetry_redaction_test.exs
  - apps/web/src/features/recovery/RecoveryStrip.tsx
  - apps/web/src/app/AppShell.tsx
  - apps/web/src/index.css
  - apps/web/src/test/ui-contract.test.tsx
  - apps/web/e2e/phase1.spec.ts
  - apps/web/e2e/visual.spec.ts
  - packages/design-tokens/tokens.json
  - packages/design-tokens/css.css
  - tooling/test-phase-1.sh
  - .planning/phases/KPL-01-one-trustworthy-task/01-VALIDATION.md
autonomous: true
requirements: [GTD-07, WEB-02, QUAL-01, GTD-01, GTD-02, GTD-03, GTD-04, GTD-05, GTD-06, SRV-01, SRV-02, SRV-03, WEB-01]
estimate: { tokens: 78000, raw_tokens: 78000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "A 24-hour high-entropy hash-stored account-bound one-shot handle can undo supported edits, clarify/return, plan/unplan, complete/reopen, Trash/restore at exactly the produced revision; expiry/stale/applied/auth/unknown states are explicit (D-40..D-44)."
    - "The latest eligible action remains in a persistent precisely labeled Recovery Strip; opaque handle never renders and Cmd/Ctrl-Z remains native text undo (D-40,D-44,D-59)."
    - "The approved wide/narrow workspace, semantic tokens, light/dark/forced-colors, keyboard/focus/live-region, 200% zoom, 320px reflow and Reduce Motion contracts pass at deterministic states (D-09,D-13,D-28..D-29,D-36,D-59..D-60)."
    - "Long adversarial command sequences, concurrent duplicate/reorder/undo races, before/after-commit loss, migrations, contracts, browser states and telemetry redaction pass at their real boundaries."
    - statement: "QUAL-01 adjacency, empty/null/single, equal-order stability and interruption/parallel edges are confirmed across storage-neutral vectors and real PostgreSQL/browser evidence."
      verification: backstop
  artifacts:
    - { path: "apps/server/lib/keepling/application/undo.ex", provides: "Typed compensation validation and inverse dispatch" }
    - { path: "apps/web/src/features/recovery/RecoveryStrip.tsx", provides: "Persistent latest eligible semantic undo" }
    - { path: "apps/web/e2e/phase1.spec.ts", provides: "Full authenticated lifecycle and adversarial real-stack suite" }
    - { path: "packages/design-tokens/tokens.json", provides: "DTCG-compatible Phase 1 semantic role source" }
  key_links:
    - { from: "RecoveryStrip.tsx", to: "Undo.execute", via: "opaque handle in request only, authenticated fresh mutation identity", pattern: "undo" }
    - { from: "tooling/test-phase-1.sh", to: "server/contracts/web/e2e suites", via: "one fail-fast native-tool phase gate", pattern: "test:phase1" }
  flagged_assumptions:
    - "GTD-07 unclassified edge probe remains unresolved; unsupported inverse cases must be surfaced rather than silently gaining undo behavior."
    - "QUAL-01 concurrency backstop requires fresh real-boundary evidence; an unavailable PostgreSQL/browser environment yields human_needed, never a pass."
  prohibitions:
    - statement: "MUST NOT use shaming, guilt, celebratory productivity language, or uncertain success claims; routine and recovery copy stays calm, specific, and evidence-based."
      status: unverified
      flagged: true
    - statement: "MUST NOT turn task activity into state reconstruction, diagnostic/security telemetry, or a store for prompts, model explanations, or hidden reasoning."
      status: unverified
      flagged: true
    - statement: "MUST NOT expand this personal browser phase into collaboration, hosted administration, hard deletion, durable browser-offline sync, or another deferred product surface."
      status: unverified
      flagged: true
---

<objective>Finish bounded undo and produce comprehensive interaction, accessibility, concurrency, privacy, and full-lifecycle evidence for the entire Phase 1 goal (D-09,D-13,D-28..D-29,D-36,D-40..D-44,D-51,D-59..D-60).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VALIDATION.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-06-SUMMARY.md
@docs/brand/BRAND-SEED.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Apply bounded one-shot semantic undo</name>
  <read_first>01-CONTEXT.md D-40..D-44,D-56..D-59; RESEARCH undo fault matrix; existing lifecycle/activity/receipt kernel</read_first>
  <reversibility rating="costly">Undo handle and typed inverse contracts become durable recovery semantics shared by later clients.</reversibility>
  <files>apps/server/lib/keepling/application/undo.ex, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/priv/repo/migrations/20260830000600_add_undo_handles.exs, apps/server/test/keepling/application/undo_test.exs, apps/web/src/features/recovery/RecoveryStrip.tsx, apps/web/src/features/recovery/recovery-strip.test.tsx, packages/contracts/openapi/keepling.yaml, packages/contracts/vectors/undo.json</files>
  <behavior>
    - "Supported edit, clarify/return, plan/unplan, complete/reopen and Trash/restore results may return one 24-hour handle; organization/reorder commands are explicitly unsupported."
    - "Authenticated owner at exact produced revision wins once; expired, stale, applied, unauthorized and uncertain states apply no inverse and return typed no-change."
    - "Accepted undo increments revision, appends undo_applied linked activity, consumes handle and stores terminal result atomically."
  </behavior>
  <action>Write expiry-boundary, ownership, exact-revision, concurrent-consumption, response-loss, activity-link and replay tests first. D-41: implement undo as a fresh idempotent semantic command that validates authentication, ownership, authorization, expiry/consumption and equality with the original produced revision, then atomically applies the typed inverse, increments revision, appends activity, consumes the handle and stores the result. D-42: return explicit no-change outcomes for expired, stale, already-applied, authentication-required and uncertain states; possession never authorizes. Store only a cryptographic hash plus closed typed inverse payload, account, original/produced revisions, expiry and consumed/result state. Adopt the explicit supported matrix in the behavior block because research left it open, and document it in contract/vectors. Render only the latest eligible action in a persistent Recovery Strip with precise labels, never the raw handle; preserve native browser editing undo and exact unknown/auth recovery.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/application/undo_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @keepling/web test --run src/features/recovery/recovery-strip.test.tsx &amp;&amp; pnpm contracts:check</automated></verify>
  <acceptance_criteria>All D-40..D-44 boundaries and races pass, only the supported matrix emits handles, one inverse/activity/result occurs, and no raw handle is returned by reads/rendered/logged.</acceptance_criteria>
  <done>A user can recover the latest supported consequential action with a bounded, honest, revision-aware undo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and test the approved responsive interaction contract</name>
  <read_first>01-UI-SPEC.md complete approved contract and 48/48 UI Considerations; BRAND-SEED.md; PATTERNS token/CSS and component conventions; existing App routes/features</read_first>
  <files>packages/design-tokens/tokens.json, packages/design-tokens/css.css, apps/web/src/app/AppShell.tsx, apps/web/src/index.css, apps/web/src/test/ui-contract.test.tsx, apps/web/e2e/visual.spec.ts</files>
  <action>Encode the exact Phase 1 spacing, four typography roles, light/dark semantic colors, accent/destructive roles, radii, focus and motion values in a DTCG-compatible source and deterministic CSS output. Implement the 224px / 360–440px / flexible-detail wide shell at 1024+, one routed surface below 1024, 16/24px narrow padding, independent panes, semantic header/nav/main, navigation order/aria-current, 44px targets and 52px rows. Use exact approved copy and component inventory, native lists/forms/time/buttons, one polite live region, assertive auth/destructive alerts only, stable focus, dirty dialog initial focus on Stay here, forced-colors outlines and reduced-motion alternatives. Add deterministic component/visual/axe/contrast cases for all UI-consideration categories: explicit empty/loading/error/populated/partial/zero-one-many plus overflow and long-text backstops at 320/768/1024/1440, both themes, 200% zoom, forced colors and reduced motion.</action>
  <verify><automated>pnpm --filter @keepling/web test --run src/test/ui-contract.test.tsx &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @visual-contract</automated><human-check>At 320, 768, 1024 and 1440px, review light/dark, forced colors, 200% zoom and Reduce Motion; then use keyboard and VoiceOver for capture, edit, clarify, reorder, lifecycle, undo, conflict, reauth, pagination and session revocation, recording announcement and focus targets.</human-check></verify>
  <acceptance_criteria>Every approved UI consideration/evidence state is represented; no horizontal page scroll at 320; color/motion never carry state alone; exact copy, focus and live announcements satisfy the contract.</acceptance_criteria>
  <done>The complete Phase 1 browser UI is calm, responsive, keyboard/screen-reader usable, and deterministically evidenced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seal the full lifecycle with adversarial boundary evidence</name>
  <read_first>01-VALIDATION.md full requirement/fault/manual matrices; RESEARCH validation/security sections; all prior plan summaries and current phase test scripts</read_first>
  <files>apps/server/test/keepling/domain/long_sequence_test.exs, apps/server/test/keepling/telemetry_redaction_test.exs, apps/web/e2e/phase1.spec.ts, tooling/test-phase-1.sh, .planning/phases/KPL-01-one-trustworthy-task/01-VALIDATION.md</files>
  <behavior>
    - "Seeded StreamData long sequences preserve invariants across edit/date/lifecycle/Trash/restore/undo and duplicate/conflicting delivery."
    - "Two-connection races and before/after-commit loss prove one durable effect, stable result/conflict/no-change, and no silent overwrite."
    - "Full browser flow covers login, capture, Inbox, clarify, Today, Upcoming, edit, complete/reopen, Trash/restore, undo, activity, sessions and recovery states against real PostgreSQL."
  </behavior>
  <action>Build the final fail-fast phase runner and full real-stack Playwright suite with deterministic clock/data, trace/screenshots and production-route assertions. Add seeded model-based command sequences, migration up/down/forward checks appropriate to expand-only migrations, cross-account tampering for task/receipt/conflict/cursor/undo, auth/session/CSRF abuse, duplicate and semantic-mismatch replay, stale/unknown mutation recovery, and telemetry capture around every error/command. Assert no task titles/notes/prompts/credentials/raw tokens/arbitrary IDs appear in diagnostics. Update `01-VALIDATION.md` task IDs/status only from fresh command results, set `nyquist_compliant: true` and `wave_0_complete: true` only when every automated lane exists and passes, and record measured runtimes/flakes/manual evidence without claiming Phase 2 deployment/restore or later-client readiness.</action>
  <verify><automated>./tooling/test-phase-1.sh</automated><human-check>Review the consolidated VoiceOver/focus/reflow/theme/forced-colors/Reduce-Motion and authentication-recovery evidence described in 01-VALIDATION.md; record defects rather than approving around them.</human-check></verify>
  <acceptance_criteria>All 13 Phase 1 requirement lanes and threat mitigations pass with fresh evidence; no three-task sampling gap exists; high-severity threats are closed; validation frontmatter/status reflects observed results only.</acceptance_criteria>
  <done>Phase 1 has reproducible domain, persistence, contract, browser, accessibility, security and privacy evidence at the boundaries that can prove each claim.</done>
</task>
</tasks>

## Artifacts this phase produces
- Undo migration, `Keepling.Application.Undo`, typed inverse/handle/result contracts, Recovery Strip, undo vectors and race/expiry/security tests.
- DTCG semantic token source/generated CSS, responsive `AppShell`, full UI-contract component/visual/a11y suites and consolidated manual evidence.
- Seeded long-sequence suite, full `@phase1` Playwright lifecycle, privacy/cross-account/fault tests, fail-fast `tooling/test-phase-1.sh`, and evidence-backed VALIDATION status.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser undo handle → server | High-entropy capability is untrusted and never sufficient authorization. |
| content/activity → diagnostics and rendered UI | Private user data may leak or execute if boundaries collapse. |
| test orchestration → production artifact | Test-only faults/secrets/fixtures must not ship. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-28 | Elevation | undo | high | mitigate | Auth/account/authorization, hash storage, exact revision, expiry and atomic one-shot consumption. |
| T-KPL01-29 | Tampering | undo replay/race | high | mitigate | Fresh mutation receipt plus row lock and stored no-change/result; independent-connection race tests. |
| T-KPL01-30 | Tampering | untrusted UI content | high | mitigate | Text-only render, no HTML, CSP and hostile fixtures across tasks/activity/conflicts. |
| T-KPL01-31 | Information disclosure | telemetry/evidence | high | mitigate | Field allow-list and negative capture tests for content, secrets, raw tokens and arbitrary IDs. |
| T-KPL01-32 | Denial | full-stack lists/long input | medium | mitigate | Bounded fields/pages/rate limits/indexes and long-input evidence. |
| T-KPL01-33 | Elevation | test fault routes | high | mitigate | Compile/runtime isolation and production route-absence/build inspection. |
| T-KPL01-SC | Tampering | third-party packages | high | mitigate | Exact pins approved by Plan 01-02; lockfile and built-dependency controls verified in full runner. |
</threat_model>

<verification>Run `./tooling/test-phase-1.sh`, require every automated lane green, then complete the consolidated end-of-phase human checks. Any high-severity threat failure blocks readiness.</verification>
<success_criteria>The user can authenticate and perform the complete locked browser lifecycle with exact recovery and fresh evidence; no deferred Phase 2–6 claim is made.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-07-SUMMARY.md` when done.</output>
