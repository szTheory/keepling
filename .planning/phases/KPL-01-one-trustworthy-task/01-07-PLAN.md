---
phase: KPL-01-one-trustworthy-task
plan: 07
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - apps/server/lib/keepling/accounts.ex
  - apps/server/lib/keepling/accounts/session.ex
  - apps/server/lib/keepling_web/auth.ex
  - apps/server/lib/keepling_web/controllers/auth_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/lib/mix/tasks/keepling.recover.ex
  - apps/server/priv/repo/migrations/20260830000210_expand_auth_lifecycle.exs
  - apps/server/test/keepling_web/auth_test.exs
  - apps/server/test/keepling/security_audit_test.exs
  - apps/server/test/keepling/telemetry_redaction_test.exs
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/generated/keepling.ts
autonomous: true
requirements: [SRV-01, SRV-03, QUAL-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Argon2id login, browser-consumed one-use recovery, reauthentication, logout, tracked sessions, expiry, revocation, and independent abuse limits implement D-46..D-55 after D-45 setup."
    - "Recovery races have one winner; credentials, raw tokens, IP/fingerprints, and arbitrary IDs never enter diagnostics."
  artifacts:
    - path: "apps/server/lib/keepling/accounts.ex"
      provides: "Credential, recovery, and tracked-session API over the closed account"
    - path: "apps/server/lib/keepling_web/controllers/auth_controller.ex"
      provides: "Versioned login/recovery/session transport mappings"
  key_links:
    - from: "apps/server/lib/keepling_web/controllers/auth_controller.ex"
      to: "apps/server/lib/keepling/accounts.ex"
      via: "closed DTOs and server-derived session principal"
      pattern: "Accounts"
---
<objective>Complete the server-side password, recovery, and tracked-session lifecycle after the D-45 setup capability has created the closed account (D-46..D-55).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-06-SUMMARY.md</context>
<scope_exception>The auth lifecycle changes 12 tightly coupled server/contract files because recovery, sessions, CSRF, OpenAPI source, generated DTOs, and focused security evidence are one externally observable protocol. Closed setup/timezone is already proven by Plan 01-06; reads are bounded to the named auth modules/tests and 01-PATTERNS.md auth/transport/contract sections.</scope_exception>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement login, one-use recovery, and tracked sessions</name>
  <reversibility rating="costly">Session/grant seams are versioned compatibility inputs for later native and MCP adapters.</reversibility>
  <files>apps/server/lib/keepling/accounts.ex, apps/server/lib/keepling/accounts/session.ex, apps/server/lib/keepling_web/auth.ex, apps/server/lib/keepling_web/controllers/auth_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/lib/mix/tasks/keepling.recover.ex, apps/server/priv/repo/migrations/20260830000210_expand_auth_lifecycle.exs, apps/server/test/keepling_web/auth_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts</files>
  <behavior>
    - "D-46 accepts long password-manager values, paste/AutoFill/reveal-compatible submissions, and no composition/rotation/security-question/CAPTCHA-first/permanent-lockout policy."
    - "D-47 operator recovery prints a one-use link; the browser endpoint consumes its token and sets a new password without CLI/env/log password transport."
    - "D-49/D-50 sessions expose current marker, editable label, client kind, creation time, coarse activity, and individual revocation."
  </behavior>
  <action>Build on the permanently closed D-45 setup/account and explicit timezone from Plan 01-06. Follow 01-PATTERNS.md's Authentication and Authorization no-implementation-analog guidance, Phoenix controller assignment, and contract boundary. Write independent-connection recovery races and expiry/revocation tests first. Implement D-47 operator recovery through `Mix.Tasks.Keepling.Recover`: issue a short-lived one-use browser link, store only its hash/lifecycle metadata, print generic surrounding output, and never accept a password through command arguments, environment variables, or logs. Implement measured Argon2id login, configurable 30-day idle/180-day absolute/15-minute recent-auth defaults, server-side session/CSRF rotation, and explicit list/label/revoke routes. Keep ordinary task writes outside recent-auth. Preserve the D-54 authorization-code+PKCE/scoped-grant port while leaving deferred native/passkey flows absent. Update OpenAPI, regenerate TypeScript in this task, and leave contract drift green before Plan 01-08 consumes the DTOs.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/auth_test.exs' &amp;&amp; pnpm contracts:check</automated></verify>
  <done>All locked account/session semantics and transport routes are executable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bound authentication abuse and diagnostic disclosure</name>
  <files>apps/server/lib/keepling/accounts.ex, apps/server/lib/keepling_web/controllers/auth_controller.ex, apps/server/test/keepling/security_audit_test.exs, apps/server/test/keepling/telemetry_redaction_test.exs</files>
  <behavior>
    - "Login/recovery have independent account/source buckets, bounded backoff, and generic responses; setup retains its independent Plan 01-06 bucket."
    - "Audit and telemetry contain only allow-listed low-cardinality fields."
  </behavior>
  <action>Follow 01-PATTERNS.md's Logging and Privacy and server-test no-implementation-analog guidance. Implement D-53 Hammer policies with measured parameters and privacy-safe security audit. Capture hostile password/token/task fixtures and assert no secret, content, raw IP/fingerprint, or arbitrary identifier is emitted. Verify all cookie-authenticated mutations enforce D-48 CSRF plus origin/host checks.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/security_audit_test.exs test/keepling/telemetry_redaction_test.exs'</automated></verify>
  <done>Authentication abuse and disclosure boundaries have focused evidence.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V4 API, V6 Authentication, V7 Session Management, V8 Authorization, V14 Data Protection, and V16 Logging/Error Handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-13 | Spoofing | login/session | high | mitigate | Argon2id, generic response, rotation, hash storage, expiry, and revocation tests. |
| T-KPL01-14 | Tampering | recovery | high | mitigate | Hash-stored short-lived one-use recovery tokens, independent race proof, and browser token consumption. |
| T-KPL01-15 | Information disclosure | audit/telemetry | high | mitigate | Allow-list and negative leakage tests. |
| T-KPL01-16 | Denial | auth endpoints | medium | mitigate | Independent bounded account/source limits. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Run auth, security-audit, and telemetry suites.</verification>
<success_criteria>The post-setup auth/session policy is reachable through versioned Phoenix routes without public registration or credential leakage.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-07-SUMMARY.md`.</output>
