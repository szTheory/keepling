---
phase: KPL-01-one-trustworthy-task
plan: 17
type: execute
wave: 15
depends_on: [01-16]
files_modified:
  - apps/server/lib/keepling_web/controllers/test_fault_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/test/keepling_web/test_fault_test.exs
  - apps/web/src/api/keepling.ts
  - apps/web/src/commands/submission.ts
  - apps/web/src/features/recovery/MutationRecoveryPanel.tsx
  - apps/web/src/features/tasks/TaskEditor.tsx
  - apps/web/src/features/tasks/LifecycleActions.tsx
  - apps/web/src/commands/submission.test.ts
  - apps/web/e2e/lifecycle-recovery.spec.ts
autonomous: true
requirements: [SRV-01, SRV-03, WEB-01, WEB-02, QUAL-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Timeout/disconnect/auth expiry preserves request bytes, draft, and original identity; lookup/same-ID retry distinguishes unknown from rejection (D-37, D-38, D-51)."
    - "Test-only before/after-commit faults exercise the real stack and are absent from production."
  artifacts:
    - path: "apps/web/src/commands/submission.ts"
      provides: "Exact submission/recovery state machine"
    - path: "apps/web/e2e/lifecycle-recovery.spec.ts"
      provides: "Real response-loss/auth-expiry evidence"
  key_links:
    - from: "apps/web/src/commands/submission.ts"
      to: "apps/web/src/api/keepling.ts"
      via: "mutation lookup/retry with unchanged bytes and identity"
      pattern: "mutationId"
---
<objective>Make uncertain delivery and authentication interruption honest across every browser mutation (D-11, D-37..D-38, D-51..D-52).</objective>
<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-PATTERNS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-16-SUMMARY.md</context>
<scope_exception>The 10 files are one uncertain-delivery boundary: browser submission state, facade/recovery surfaces, server fault route isolation, and real-stack loss evidence must agree on the original identity and exact request bytes. Reads are bounded to the six browser state files or four fault/E2E files plus the command-state/test guidance in 01-PATTERNS.md.</scope_exception>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement exact submission recovery state</name>
  <files>apps/web/src/api/keepling.ts, apps/web/src/commands/submission.ts, apps/web/src/features/recovery/MutationRecoveryPanel.tsx, apps/web/src/features/tasks/TaskEditor.tsx, apps/web/src/features/tasks/LifecycleActions.tsx, apps/web/src/commands/submission.test.ts</files>
  <behavior>
    - "States remain not_submitted, in_flight, unknown, authentication_required, acknowledged, rejected/conflict."
    - "Unknown shows exact Checking…/Check again; auth shows exact Sign in again…/Sign in and continue."
    - "No retry mints an identity or clears draft before exact terminal acknowledgement."
  </behavior>
  <action>Follow 01-PATTERNS.md's `apps/web/src/commands/**/*` `Implementation analog: none` guidance and Read Cache vs. Command State Machine pattern. Centralize D-37/D-51 submission orchestration separate from read caching, wire TaskEditor and LifecycleActions through it, and render persistent exact UI-SPEC recovery panels. Preserve fixed request bytes/identity through reauth, lookup, and retry; fence logout per D-52.</action>
  <verify><automated>pnpm --filter @keepling/web test --run src/commands/submission.test.ts</automated></verify>
  <done>Every browser mutation uses one honest recovery state machine.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove before/after-commit loss and production isolation</name>
  <files>apps/server/lib/keepling_web/controllers/test_fault_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/test_fault_test.exs, apps/web/e2e/lifecycle-recovery.spec.ts</files>
  <behavior>
    - "Before acceptance yields no receipt/effect and preserves browser identity."
    - "After commit/drop reconciles the stored result once with the same identity."
    - "Production compilation/router exposes no fault route."
  </behavior>
  <action>Follow 01-PATTERNS.md's Phoenix `Implementation analog: none` and server/browser testing no-implementation-analog guidance, including independent real PostgreSQL boundaries rather than mocked response loss. Add credentialed test-environment-only fault injection around controller/commit response and a production route-absence test. Exercise lifecycle, conflict, auth expiry before submit, auth expiry after commit/drop, duplicate activation, and same-ID recovery against real PostgreSQL.</action>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/test_fault_test.exs' &amp;&amp; pnpm --filter @keepling/web test:e2e --grep @lifecycle-recovery</automated></verify>
  <done>Uncertain delivery has real-boundary proof and fault controls cannot ship.</done>
</task>
</tasks>
<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V2 Business Logic, V3 Frontend, V4 API, V7 Session, V13 Configuration, and V16 Error Handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-36 | Repudiation | unknown delivery | high | mitigate | Fixed identity, terminal lookup/replay, persistent recovery state. |
| T-KPL01-37 | Elevation | fault controls | high | mitigate | Test compile/runtime gate, random credential, production route-absence test. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>
<verification>Run submission state, fault-isolation, and real response-loss suites.</verification>
<success_criteria>No browser path equates missing response with failure or ambiguously replays intent.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-17-SUMMARY.md`.</output>
