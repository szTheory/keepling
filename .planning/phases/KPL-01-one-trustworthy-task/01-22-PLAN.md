---
phase: KPL-01-one-trustworthy-task
plan: 22
type: execute
wave: 18
depends_on: [01-19]
files_modified:
  - apps/web/src/features/lists/TaskList.tsx
  - apps/web/src/features/lists/task-lists.test.tsx
  - apps/web/e2e/today-order-recovery.spec.ts
autonomous: true
gap_closure: true
requirements: [GTD-03, SRV-03, WEB-02, QUAL-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Any nonterminal Today move is a global order lock: every Earlier/Later control is disabled and `move` refuses replacement through in-flight, unknown, and authentication-required states (D-22, D-37)."
    - "The original Today mutation identity remains the only lookup/retry target until matching acknowledgement or terminal rejection (D-31, D-32, D-37, D-38)."
    - "Real after-commit response loss reconciles the accepted order once and cannot be displaced by another row action."
  artifacts:
    - path: "apps/web/src/features/lists/TaskList.tsx"
      provides: "Global Today exact-move lock and terminal-only release"
    - path: "apps/web/e2e/today-order-recovery.spec.ts"
      provides: "Real PostgreSQL after-commit-loss and attempted-replacement evidence"
  key_links:
    - from: "TaskList Today controls"
      to: "exactMove.current.snapshot"
      via: "global nonterminal lock used by button disabled state and move guard"
      pattern: "exactMove"
    - from: "TaskList recovery actions"
      to: "Today mutation receipt endpoint"
      via: "unchanged original mutation identity"
      pattern: "getTodayMoveMutation"
---

<objective>Make uncertain Today ordering globally single-flight and exactly recoverable so a second move can never replace an unresolved accepted move (D-22, D-31..D-32, D-37..D-38).</objective>

## Phase Goal

**As a** Keepling user, **I want to** manage one task end to end, **so that** I can trust every browser change.

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>

<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md
@.planning/phases/KPL-01-one-trustworthy-task/01-13-SUMMARY.md
@.planning/phases/KPL-01-one-trustworthy-task/01-17-SUMMARY.md</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Hold the original Today move across every nonterminal state</name>
  <files>apps/web/src/features/lists/TaskList.tsx, apps/web/src/features/lists/task-lists.test.tsx</files>
  <read_first>apps/web/src/features/lists/TaskList.tsx; apps/web/src/features/lists/task-lists.test.tsx; apps/web/src/commands/submission.ts; .planning/phases/KPL-01-one-trustworthy-task/01-REVIEW.md finding CR-02; .planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md sections “Lists and lifecycle” and “Mutation acknowledgement and recovery”</read_first>
  <behavior>
    - "A deferred, unknown, or authentication-required move disables Earlier/Later for all Today rows, not only the initiating row."
    - "Direct invocation of move while locked returns without allocating or assigning another exact submission."
    - "Acknowledgement or verified terminal rejection clears the lock; unknown and authentication-required retain it."
  </behavior>
  <action>Write component cases that defer the first move and try a second row/direction. Derive the global order-lock state from `exactMove.current` plus its exact snapshot, not from transient `movingTaskId`; use it in every button and as the first guard inside `move`. Per D-22/D-37, keep the first prepared request and mutation identity through unknown/authentication recovery and release only after matching acknowledgement or terminal rejection. Preserve UI-SPEC progress/recovery copy, semantic Earlier/Later controls, focus, and server-owned order revision behavior.</action>
  <acceptance_criteria>Across in-flight, unknown, and authentication-required states, all Today move controls are disabled, a programmatic second action is refused, and the first exact move remains the only retained request.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test --run src/features/lists/task-lists.test.tsx</automated></verify>
  <fails_when>`movingTaskId` alone controls the lock, any second action replaces `exactMove.current`, or a nonterminal outcome clears the original request.</fails_when>
  <done>Today ordering has one global unresolved exact mutation at a time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove an after-commit Today move cannot be replaced</name>
  <files>apps/web/e2e/today-order-recovery.spec.ts</files>
  <read_first>apps/web/e2e/lifecycle-recovery.spec.ts; apps/web/e2e/support/stack.ts; apps/server/lib/keepling_web/controllers/test_fault_controller.ex; apps/web/src/features/lists/TaskList.tsx; .planning/phases/KPL-01-one-trustworthy-task/01-VERIFICATION.md third gap</read_first>
  <behavior>
    - "A Today move commits through Phoenix/PostgreSQL and loses its response through the test-only after_commit boundary."
    - "A second row move attempt remains disabled/refused while the first result is unknown."
    - "Check again uses the first mutation identity and reconciles the accepted canonical order exactly once."
  </behavior>
  <action>Create a dedicated real-stack Playwright test with at least two movable Today rows. Apply the existing credentialed test-only after-commit fault to the first semantic move, attempt another move before reconciliation, and assert no second command is sent and the first identity remains attached to Check again. Verify the eventual server order and returned order revision match the first accepted move and stay duplicate-free.</action>
  <acceptance_criteria>The after-commit-loss test proves the first receipt is still recoverable, the second move never reaches the server, and the final Today order matches one accepted effect.</acceptance_criteria>
  <verify><automated>pnpm --filter @keepling/web test:e2e --grep @today-order-recovery</automated></verify>
  <fails_when>The test mocks persistence, another mutation identity is observed, the original receipt becomes unreachable, or final ordering contains a duplicate/stale client prediction.</fails_when>
  <done>Today’s exact recovery guarantee holds at the real response-loss boundary.</done>
</task>

</tasks>

<threat_model>
Applicable baseline: OWASP ASVS 5.0 Level 1, V2 Business Logic, V3 Web Frontend, V4 API, V8 Authorization, and V16 Error Handling.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-G22-01 | Tampering | Today order intent | high | mitigate | Global nonterminal lock, guarded move entry point, expected order revision, and terminal-only release. |
| T-KPL01-G22-02 | Repudiation | after-commit response loss | high | mitigate | Fixed mutation identity, durable receipt lookup, and real after-commit-loss evidence. |
| T-KPL01-G22-03 | Denial | stuck unresolved move | medium | mitigate | Persistent Check again/sign-in actions retain the exact request until a terminal result. |

Completion rule: any open or failed high-severity mitigation blocks plan and phase completion.
</threat_model>

<source_coverage>
GOAL trustworthy Today change: covered. REQ GTD-03/SRV-03/WEB-02/QUAL-01: covered. RESEARCH reorder-collision and response-loss matrix: covered. CONTEXT D-22, D-26..D-29, D-31..D-32, D-37..D-38, D-51: preserved or implemented; remaining decisions stay covered by completed Plans 01-01..01-19. Deferred manual order outside Today remains excluded.
</source_coverage>

<verification>Run focused list tests, the new real-stack Today recovery spec, and `./tooling/test-phase-1.sh --run`; any high threat or replacement identity blocks completion.</verification>
<success_criteria>An unresolved Today move globally locks ordering and always reconciles its original exact identity before another move can begin.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-22-SUMMARY.md`.</output>
