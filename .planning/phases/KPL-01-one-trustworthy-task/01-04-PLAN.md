---
phase: KPL-01-one-trustworthy-task
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - apps/server/lib/keepling/domain/task.ex
  - apps/server/lib/keepling/domain/organization.ex
  - apps/server/lib/keepling/application/commands.ex
  - apps/server/lib/keepling/adapters/postgres/command_store.ex
  - apps/server/priv/repo/migrations/20260830000300_add_task_details_organizations_activity.exs
  - apps/server/test/keepling/domain/edit_task_test.exs
  - apps/web/src/app/routes.tsx
  - apps/web/src/features/tasks/TaskEditor.tsx
  - apps/web/src/features/organizations/OrganizationFields.tsx
  - apps/web/src/features/activity/ActivityList.tsx
  - apps/web/src/features/tasks/task-editor.test.tsx
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/vectors/editing.json
autonomous: true
requirements: [GTD-02, SRV-02, SRV-03, WEB-01, WEB-02]
estimate: { tokens: 74000, raw_tokens: 74000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "A user explicitly saves or cancels title, notes, one project, flat tags, planned/deadline fields; Inbox Save and atomic Save & move out are distinct and no edit saves on blur (D-12, D-14..D-17)."
    - "Projects/tags use stable IDs and idempotent create/rename/archive/unarchive; archived names remain visible, project archive blocks active unfinished members, and unarchive reports active-name collision (D-18..D-20)."
    - "Every accepted edit/clarify/organization command atomically appends a closed activity fact with exact changed values and server-derived actor/client metadata, rendered as untrusted text with keyset Load earlier (D-56..D-63)."
    - "Validation preserves all entered fields, links summary to the first invalid field, and partial optional data remains absent rather than fabricated."
    - statement: "Editor/navigation/activity evidence covers 200-character titles, 10k notes, long names and action labels at 320/768/1024/1440 widths."
      verification: backstop
  artifacts:
    - { path: "apps/web/src/features/tasks/TaskEditor.tsx", provides: "Canonical explicit-save routed editor" }
    - { path: "apps/server/lib/keepling/domain/organization.ex", provides: "Project/tag semantic command rules" }
    - { path: "apps/web/src/features/activity/ActivityList.tsx", provides: "Safe exact user-visible activity" }
  key_links:
    - { from: "TaskEditor.tsx", to: "commands.ex", via: "closed touched-field/base-value semantic commands", pattern: "base_values" }
    - { from: "command_store.ex", to: "ActivityList.tsx", via: "atomic activity persistence exposed by paginated activity DTO", pattern: "task_activity" }
  flagged_assumptions:
    - "GTD-02 unclassified edge probe remains unresolved; golden vectors must expose any uncategorized field-normalization or validation behavior."
---

<objective>Deliver the complete clarify/edit/organization/activity slice through the shared command kernel (D-09, D-12..D-20, D-56..D-63).</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/KPL-01-one-trustworthy-task/01-CONTEXT.md
@.planning/phases/KPL-01-one-trustworthy-task/01-RESEARCH.md
@.planning/phases/KPL-01-one-trustworthy-task/01-UI-SPEC.md
@.planning/phases/KPL-01-one-trustworthy-task/01-03-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Save complete task details and explicit Inbox clarification</name>
  <read_first>01-CONTEXT.md D-09,D-12,D-14..D-17,D-31..D-38; RESEARCH pure-decision/narrow-merge patterns; existing capture kernel</read_first>
  <reversibility rating="costly">Explicit Inbox processing and touched-field command shapes become shared later-client contracts.</reversibility>
  <files>apps/server/lib/keepling/domain/task.ex, apps/server/lib/keepling/application/commands.ex, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/priv/repo/migrations/20260830000300_add_task_details_organizations_activity.exs, apps/server/test/keepling/domain/edit_task_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/vectors/editing.json, apps/web/src/app/routes.tsx, apps/web/src/features/tasks/TaskEditor.tsx, apps/web/src/features/tasks/task-editor.test.tsx</files>
  <behavior>
    - "Only touched fields plus base/requested values are sent; unrelated fields survive rebase and invariants rerun."
    - "Save leaves explicit Inbox state unchanged; Save & move atomically edits and clarifies; return_to_inbox is the only inverse."
    - "Validation preserves title, notes, project, tags, and dates and focuses the linked first invalid field."
  </behavior>
  <action>Add pure/vector/component tests first, then expand task snapshot/mapping and semantic `edit_task`, `clarify_task`, and `return_to_inbox` commands. Choose documented field bounds in contract constants—title 1..512 Unicode scalar values after trimming outer whitespace, notes 0..50,000 without markup interpretation—and preserve user text otherwise; flag these values in the summary as planner-selected because research left exact bounds open. Build the canonical routed editor used as wide detail pane and narrow full page, explicit Save/Cancel/Save & move actions, dirty Save/Discard/Stay blocker plus scoped beforeunload, Cmd/Ctrl-Enter submission, Escape cancellation, and no blur save per D-09,D-12,D-16. Keep the row until exact acknowledgement and preserve fields on every rejection.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/domain/edit_task_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @keepling/web test --run src/features/tasks/task-editor.test.tsx &amp;&amp; pnpm contracts:check</automated></verify>
  <acceptance_criteria>All supported fields round-trip, explicit Inbox state changes only through clarify/return, non-overlapping edits preserve other fields, validation retains/focuses input, and contract vectors stay deterministic.</acceptance_criteria>
  <done>A user can safely clarify and edit every Phase 1 task field through the real semantic boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manage projects and tags without name-addressed writes</name>
  <read_first>01-CONTEXT.md D-17..D-20; UI-SPEC organization fields and archive confirmation; task-created editor/command contracts</read_first>
  <files>apps/server/lib/keepling/domain/organization.ex, apps/server/lib/keepling/application/commands.ex, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/test/keepling/domain/organization_test.exs, apps/web/src/features/organizations/OrganizationFields.tsx, apps/web/src/features/organizations/organization-fields.test.tsx, packages/contracts/openapi/keepling.yaml, packages/contracts/vectors/organizations.json</files>
  <behavior>
    - "Create/rename/archive/unarchive replay idempotently by stable opaque ID; rename does not revision-bump assigned tasks."
    - "Archived assignments stay named but are absent from new pickers; project archive blocks with unfinished count; tag archive preserves assignments; unarchive collision changes nothing."
  </behavior>
  <action>D-19: ensure archived projects/tags disappear from new-assignment pickers but remain named on existing/history views; block project archive with active unfinished members, preserve tag assignments, and reject active-name collision on unarchive. Implement pure organization decisions and semantic commands through the existing receipt kernel, with normalized active-name uniqueness documented in vectors (Unicode NFC, trim outer whitespace, casefold for collision only while preserving display form). Add project combobox, multi-tag picker, management routes, archived labels, exact confirmations and blocked-count error. Assignments always submit IDs, never mutable names, and organization operations append their applicable closed activity/result without rewriting assigned snapshots.</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/domain/organization_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @keepling/web test --run src/features/organizations/organization-fields.test.tsx &amp;&amp; pnpm contracts:check</automated></verify>
  <acceptance_criteria>All D-17..D-20 commands, collisions, archive blockers, historical names, picker behavior, idempotency and no-task-revision-on-rename are proven.</acceptance_criteria>
  <done>Projects and tags can be managed and assigned without weakening task identity or history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose exact, safe, paginated task activity</name>
  <read_first>01-CONTEXT.md D-56..D-63; UI-SPEC Activity and Technical Detail; existing receipt/activity schema</read_first>
  <reversibility rating="costly">Activity types and field-delta contracts become durable user history and later export/MCP compatibility inputs.</reversibility>
  <files>apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/test/keepling/application/activity_test.exs, apps/web/src/features/activity/ActivityList.tsx, apps/web/src/features/activity/activity-list.test.tsx, packages/contracts/openapi/keepling.yaml, packages/contracts/vectors/activity.json</files>
  <behavior>
    - "Accepted captured/details-updated/clarified/returned activity stores exact changed canonical values only; authentication failures never appear."
    - "Newest-first keyset pages are account-bound and stale explicitly; exact account-timezone time is visible in time[datetime]."
    - "Hostile text remains text; long changes collapse; revision/mutation IDs live only in Technical details; undo handles never render."
  </behavior>
  <action>D-57: implement captured, details-updated, planned/unplanned, clarified/returned, completed/reopened, trashed/restored, and undo-applied activity types with acceptance time, from/to revisions, mutation/optional undone linkage, server-derived actor/principal/client kind, and closed changed-field data. Finalize closed versioned activity deltas and query DTO separate from receipts/conflicts/security audit/telemetry. Use an HMAC-authenticated opaque account-bound keyset cursor over `(accepted_at, activity_id, activity_view_revision)` and explicit stale response; retain activity with completion/Trash for account lifetime per D-63. Implement the compact list, Show change and Technical details disclosures, exact zoned `<time>`, Load earlier focus behavior, and hostile-content fixtures. Preserve the future server-derived agent/grant/client label fields without implementing deferred MCP UI (D-62).</action>
  <verify><automated>cd apps/server &amp;&amp; mix test test/keepling/application/activity_test.exs &amp;&amp; cd ../.. &amp;&amp; pnpm --filter @keepling/web test --run src/features/activity/activity-list.test.tsx &amp;&amp; pnpm contracts:check</automated></verify>
  <acceptance_criteria>Every accepted command has one correct activity fact, pagination is stable/account-bound/stale-aware, text cannot execute, identifiers/handles stay out of routine UI and telemetry, and retention survives lifecycle changes.</acceptance_criteria>
  <done>User-visible activity is trustworthy canonical history without becoming an event store or diagnostic log.</done>
</task>
</tasks>

## Artifacts this phase produces
- Expanded task/details/organization/activity migration; pure `Task.edit/clarify/return_to_inbox` and `Organization` decisions; semantic project/tag commands and contract vectors.
- Routed `TaskEditor`, `OrganizationFields`, and `ActivityList` with validation, dirty-navigation, archive, pagination, exact-time, hostile-text and technical-detail behavior.
- APIs for edit/clarify/return, project/tag create/rename/archive/unarchive, and per-task activity keyset pages.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| editor/organization UI → semantic API | Untrusted content, stable IDs, touched/base values and expected revisions. |
| stored activity → React | Content-bearing historical values return to the browser and must stay plain text. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL01-13 | Tampering | edit/org commands | high | mitigate | Closed schemas, server invariants, ID references, expected revision and receipt fingerprint. |
| T-KPL01-14 | Elevation | task/org/activity queries | high | mitigate | Session-derived account predicates on every aggregate, assignment and cursor lookup. |
| T-KPL01-15 | Tampering | activity rendering | high | mitigate | Canonical text only, React text nodes, no HTML, hostile-content tests and CSP inherited from shell. |
| T-KPL01-16 | Information disclosure | activity/technical details | medium | mitigate | Exact user history only on owned task; opaque handle never serialized; diagnostic separation/redaction tests. |
| T-KPL01-17 | Denial | long fields/pagination | medium | mitigate | Closed measured bounds, keyset page limits, indexes and stale cursor handling. |
| T-KPL01-SC | Tampering | UI packages | high | mitigate | Exact pins approved by Plan 01-02. |
</threat_model>
<verification>Run the three focused server/browser suites and contract drift; then exercise wide/narrow explicit save, organization archive, and hostile activity content.</verification>
<success_criteria>A user can clarify the complete supported task, manage stable-ID organization, and inspect exact safe activity without bypasses or draft loss.</success_criteria>
<output>Create `.planning/phases/KPL-01-one-trustworthy-task/01-04-SUMMARY.md` when done.</output>
