---
phase: KPL-03-mac-daily-loop
plan: 03
type: execute
wave: 5
depends_on: [03-09]
files_modified:
  - packages/web-ui/src/tasks/TaskEditor.tsx
  - packages/web-ui/src/tasks/ConflictResolver.tsx
  - packages/web-ui/src/recovery/SyncRecovery.tsx
  - apps/desktop/renderer/main.tsx
  - apps/desktop/renderer/desktop.css
  - apps/desktop/test/e2e/daily-loop.spec.ts
  - apps/desktop/test/application/state-matrix.test.tsx
autonomous: true
requirements: [MAC-01, MAC-02, MAC-04, QUAL-04]
estimate: {tokens: 50000, raw_tokens: 50000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Jon can use Inbox, Today, detail editing, complete/reopen, Trash/restore, and latest supported undo through the durable facade with immediate local visibility and exact later settlement."
    - "List focus is independent from selection; Up/Down, Return, Escape, Tab, dirty-work dialogs, row removal, and window recreation restore focus by stable task identity."
    - "Probe predicate (MAC-01): every required daily-loop operation has a named facade method, a reachable renderer action, a durable local acceptance assertion, and an exact terminal-state test."
    - "Probe predicate (MAC-02): native-compatible keyboard behavior preserves editable control ownership, IME/dead keys, key-repeat guards, ordinary Tab order, and text undo."
    - "Probe predicate (QUAL-04): populated, empty, opening, preparing, updating, offline, denied/authentication, stale, conflict, partial, retry, local-save failure, and unrecoverable states are deterministic and isolated across repeated or concurrent tests."
    - "UI consideration — Empty: authoritative zero-result lists use exact copy, forms retain labels and block invalid commit, healthy recovery says No Changes Need Your Attention, and empty action groups are omitted."
    - "UI consideration — Loading: stable surfaces, drafts, and accepted local content remain visible under named opening/preparing/delayed-updating states; no false empty state or perpetual spinner appears."
    - "UI consideration — Error: entered and durable content remains, contextual copy names the problem, and each state offers a specific safe action with adjacent validation and safe dialog focus."
    - "UI consideration — Populated: stable list/form geometry, independent scrolling, bounded recovery groups, keyboard navigation, and text-backed state labels remain available."
    - "UI consideration — Partial: valid local/nonconflicting content remains, absent optional fields remain absent, and conditional actions are disabled or omitted without hiding retained state."
    - "UI consideration — Zero/one/many: zero uses authoritative empty copy, one keeps normal geometry and singular wording, and many uses stable scrolling plus bounded counts."
    - {statement: "UI consideration — Overflow: the visual matrix verifies wrapped recovery/dialog copy, compact title clamping with accessible full value, internal pane/drawer/dialog scrolling, unclipped focus rings, and no window-level horizontal overflow.", verification: backstop}
    - {statement: "UI consideration — Long text: held-out cases verify editable/full views preserve content while compact views wrap or truncate without shrinking type or losing accessible meaning.", verification: backstop}
  artifacts:
    - {path: packages/web-ui/src/ClientFacade.ts, provides: "Platform-free named semantic operations and sequence subscription", exports: [ClientFacade]}
    - {path: packages/web-ui/src/workspace/Workspace.tsx, provides: "Responsive navigation/list/detail presentation"}
    - {path: apps/desktop/test/e2e/daily-loop.spec.ts, provides: "Keyboard-complete Mac daily-loop proof"}
    - {path: apps/desktop/test/application/state-matrix.test.tsx, provides: "Resolved UI consideration and QUAL-04 matrix"}
  key_links:
    - {from: apps/desktop/renderer/main.tsx, to: packages/web-ui/src/ClientFacade.ts, via: "desktop facade adapter; subscribe before initial snapshot"}
    - {from: packages/web-ui/src/ClientFacade.ts, to: apps/desktop/preload/index.ts, via: "named async semantic methods only"}
    - {from: packages/web-ui/src/tasks/TaskEditor.tsx, to: apps/desktop/main/application/DesktopApplication.ts, via: "immutable semantic save/lifecycle commands through facade"}
  prohibitions:
    - {statement: "MUST NOT turn the shared UI package into a browser/Electron universal backend or let it import fetch, IPC, persistence records, session lifecycle, or platform APIs."}
    - {statement: "MUST NOT hide an actionable durability, conflict, authentication, or local-store problem to preserve a visually calm surface."}
---

<objective>
Deliver the complete facade-driven Mac workspace loop and its resolved UI state matrix, implementing D-01, D-02, D-04 through D-08, D-12 through D-16, and D-39 through D-42.

Purpose: Turn the durable/sync core into the calm, keyboard-complete daily workspace Jon can actually use.
Output: A narrow shared presentation package, desktop renderer composition, exact focus/dirty-work behavior, and representative state/E2E proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/brand/BRAND-SEED.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md
@.planning/phases/KPL-03-mac-daily-loop/03-PATTERNS.md
@apps/web/src/app/AppShell.tsx
@apps/web/src/app/WorkspaceShell.tsx
@apps/web/src/features/capture/QuickCapture.tsx
@apps/web/src/features/tasks/ConflictResolver.tsx
@packages/design-tokens/tokens.json
</context>

## Artifacts this phase produces

- Symbols: `ClientFacade`, `Workspace`, `CaptureForm`, `TaskList`, `TaskEditor`, `ConflictResolver`, and `SyncRecovery`.
- Methods: snapshot/subscription, capture, edit, complete, reopen, trash, restore, undo, resolve conflict, inspect recovery, and navigation operations.
- Fields: stable focused/selected task IDs, semantic scroll anchor, dirty draft snapshot, and sequence number.
- Files: every path in `files_modified`, including desktop CSS and the state matrix.

<tasks>

<task type="auto" tdd="true">
  <name>Task 2: Expand the workspace through edit, lifecycle, Trash, restore, undo, conflicts, and exact focus recovery</name>
  <files>packages/web-ui/src/tasks/TaskEditor.tsx, packages/web-ui/src/tasks/ConflictResolver.tsx, packages/web-ui/src/recovery/SyncRecovery.tsx, apps/desktop/renderer/main.tsx, apps/desktop/renderer/desktop.css, apps/desktop/test/e2e/daily-loop.spec.ts</files>
  <read_first>packages/web-ui/src/ClientFacade.ts, apps/web/src/features/tasks/ConflictResolver.tsx, apps/web/src/features/recovery/RecoveryStrip.tsx, apps/web/e2e/phase1.spec.ts, .planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md, apps/desktop/main/application/presentation.ts</read_first>
  <behavior>
    - D-12/D-13 save uses Command-S/Command-Return, dirty transitions use Save Changes/Discard Changes/Keep Editing, text controls retain native undo, and focus after removal is next/previous/heading.
    - D-14 complete/reopen, Trash/restore, Today placement/order, and latest semantic undo are named actions outside editable controls with IME/repeat guards.
    - D-41 conflicts remain inline, preserve nonconflicting drafts, require explicit mine/current choices, and recovery copy names what is durable and the next safe action.
  </behavior>
  <action>Adapt the proven editor, lifecycle, conflict, and recovery components to `ClientFacade` and the exact UI-SPEC strings. Implement Inbox, Today, and Trash routes; edit all supported fields; complete/reopen; Trash/restore; Today move earlier/later; and persistent latest supported undo. Subscribe before fetching the first snapshot, refetch opaquely on any sequence gap per D-29, and restore selection/focus/scroll by stable identity on removal, remote changes, recreation, and narrow Back. Keep one polite debounced live region and exact dirty-work safe focus.</action>
  <acceptance_criteria>The Electron E2E completes the full loop only through user-visible roles/menu-equivalent actions, asserts local visibility before network, exact reconciliation, structured conflict, and deterministic focus after every row removal.</acceptance_criteria>
  <verify>
    <automated>pnpm test:desktop:e2e -- daily-loop</automated>
    <fails_when>non-zero exit, "0 tests" appears, any MAC-01 action is absent, focus is restored by DOM index, text Command-Z triggers semantic undo, or a conflict overwrites without choice</fails_when>
  </verify>
  <done>Jon can perform every scoped task action with complete keyboard and recovery behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze the populated, empty, loading, partial, overflow, long-text, retry, conflict, denied, and unrecoverable matrix</name>
  <files>apps/desktop/test/application/state-matrix.test.tsx, apps/desktop/renderer/desktop.css, packages/web-ui/src/workspace/Workspace.tsx, packages/web-ui/src/recovery/SyncRecovery.tsx</files>
  <read_first>.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md, apps/desktop/main/application/presentation.ts, packages/design-tokens/tokens.json, apps/web/src/app/ui-contract.test.ts</read_first>
  <behavior>
    - Every explicit and backstop UI consideration in frontmatter has a named fixture at the relevant window size/theme/zoom/state.
    - D-08/D-42 light/dark updates while open, forced/increased contrast, Differentiate Without Color, Reduce Transparency, Reduce Motion, and 200% reflow preserve non-color meaning and focus.
    - QUAL-04 repeated or concurrent matrix execution uses disposable state and deterministic clocks; no case depends on previous test order or a real profile.
  </behavior>
  <action>Build a table-driven renderer/component matrix for all exact state rows and empty/copy contracts. Add light/dark snapshots at 680×520, 1024×700, 1064×700, 1180×780, and 1440×900 plus 200% zoom and the accessibility media features named in D-42. Include zero/one/many, long task/note/recovery/dialog text, partial data, compact truncation with accessible full value, unclipped focus, internal scrolling, and no horizontal overflow. Treat backstop rows as evidence obligations: if the snapshot harness cannot demonstrate them, mark the verification human-needed rather than passing.</action>
  <acceptance_criteria>Every UI-SPEC consideration maps to a named case, all exact state strings appear, each breakpoint contract is asserted, and zero tests/cases cannot pass.</acceptance_criteria>
  <verify>
    <automated>pnpm test:desktop -- state-matrix</automated>
    <fails_when>non-zero exit, "0 tests" appears, the reported consideration count is below 10 aggregate rows, an exact state/copy fixture is missing, or any snapshot has window-level horizontal overflow</fails_when>
  </verify>
  <done>The complete resolved UI consideration set and QUAL-04 state matrix have executable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Shared React UI → platform facade | Presentation input must not acquire privileged transport/storage authority. |
| User content → renderer/accessibility | Task text is untrusted plain text and must not become markup or metadata leakage. |
| Main sequence → renderer mirror | Missing or reordered updates must trigger snapshot recovery, not inferred state. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-03-01 | Elevation of privilege | ClientFacade/shared UI | high | mitigate | Named semantic methods only; import guard forbids platform/persistence APIs. |
| T-KPL03-03-02 | Tampering | renderer sequence | high | mitigate | Subscribe before snapshot and refetch on monotonic sequence gap. |
| T-KPL03-03-03 | Information disclosure | titles/window/menu/a11y | high | mitigate | Plain-text rendering, coarse window title, privacy tests, no task-derived menus. |
| T-KPL03-03-04 | Denial of service | long/overflow content | medium | mitigate | Bounded values, internal scrolling, accessible truncation, matrix at all breakpoints. |
</threat_model>

<verification>Run browser regression, workspace tracer, daily-loop E2E, and non-vacuous state matrix.</verification>
<success_criteria>The complete daily loop is reachable, keyboard-safe, state-complete, and platform-adapter independent.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-03-SUMMARY.md` when done.</output>
