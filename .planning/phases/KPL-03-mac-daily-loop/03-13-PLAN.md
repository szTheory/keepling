---
phase: KPL-03-mac-daily-loop
plan: 13
type: execute
wave: 12
gap_closure: true
depends_on: [03-05, 03-10, 03-11, 03-06]
files_modified:
  - apps/desktop/main/index.ts
  - apps/desktop/preload/contracts.ts
  - apps/desktop/preload/index.ts
  - apps/desktop/renderer/main.tsx
  - apps/desktop/renderer/desktopClientFacade.ts
  - packages/web-ui/src/ClientFacade.ts
  - packages/web-ui/src/workspace/Workspace.tsx
  - apps/web/src/app/WorkspaceShell.tsx
  - apps/desktop/test/e2e/gap-closure.spec.ts
  - apps/desktop/test/ipc/hostile-bridge.test.ts
autonomous: true
requirements: [MAC-01, MAC-03, MAC-04, MAC-05, QUAL-04]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 4, confidence: medium}
must_haves:
  truths:
    - "O-15: the shipped bootstrap() constructs SafeStorageCredentialAdapter, so the credential path the app runs is the safeStorage-backed one 03-02 built and tested."
    - "O-12: removeLocalData is reachable end to end from the renderer through a named preload contract and a sender-validated IPC handler, and remains structurally unable to delete server data."
    - "O-11: D-06 renderer-semantic restoration persists and restores sidebar visibility, pane sizes, destination, surviving selected task, semantic scroll anchor, and recoverable editor draft; transient dialogs, progress, auth prompts and raw focus never restore."
    - "O-1: the apps/web production Inbox route either renders the shared packages/web-ui slice with the useSharedWorkspace flag deleted, or a recorded decision states web keeps its routed content and the flag is permanent documented API."
    - "Every new IPC surface is covered by the hostile-bridge suite and fails closed without privileged effect."
  artifacts:
    - {path: apps/desktop/test/e2e/gap-closure.spec.ts, provides: "Shipped-entry-point proof for each closed gap"}
  key_links:
    - {from: apps/desktop/main/index.ts, to: apps/desktop/main/adapters/credentials.ts, via: "bootstrap constructs SafeStorageCredentialAdapter"}
    - {from: apps/desktop/preload/contracts.ts, to: apps/desktop/main/recovery/remove-local-data.ts, via: "named schema-validated removal contract"}
  prohibitions:
    - {statement: "MUST NOT weaken 03-10's protocol, CSP, permission, sender-trust or bounded-parsing hardening; the ipc lane must still pass unchanged."}
    - {statement: "MUST NOT expose generic IPC, raw Electron events, or any path letting a renderer supply namespace fields or trigger server-side deletion."}
    - {statement: "MUST NOT prove any closed gap against test/fixtures/wired-app-harness.ts; shipped-entry-point evidence only."}
---

<objective>
Close GAP-1: four features are built and tested but unreachable in the shipped app. Wire them through one plan that owns the contended files, so the phase's automated evidence describes what the app actually does.

Purpose: A requirement whose user-facing path does not exist is not met, however green its unit tests are. O-15 additionally means the shipped credential path is not the tested one.
Output: A wired bootstrap, a reachable removal contract, real D-06 restoration, a resolved web-slice decision, and shipped-entry-point proof for each.
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-03-mac-daily-loop/.continue-here.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: O-15 — construct SafeStorageCredentialAdapter in the shipped bootstrap</name>
  <files>apps/desktop/main/index.ts, apps/desktop/test/e2e/gap-closure.spec.ts</files>
  <read_first>apps/desktop/main/adapters/credentials.ts, apps/desktop/main/index.ts, .planning/phases/KPL-03-mac-daily-loop/03-02-SUMMARY.md</read_first>
  <behavior>
    - bootstrap() constructs SafeStorageCredentialAdapter and supplies it wherever credentials are read or written.
    - First establish what bootstrap() constructs TODAY and record it; the replaced path must be named in the SUMMARY.
    - Sign-out still fences local intent and clears credentials BEFORE best-effort remote revocation.
  </behavior>
  <action>Determine the current credential path, then wire the safeStorage-backed adapter into bootstrap(). Prove against the shipped entry point that credentials round-trip through it and are cleared on sign-out.</action>
  <verify><automated>pnpm typecheck:desktop &amp;&amp; pnpm test:desktop:ipc &amp;&amp; pnpm test:desktop:e2e</automated><fails_when>non-zero exit, "0 tests" appears, ipc lane count drops below 46, or evidence uses wired-app-harness</fails_when></verify>
  <done>The shipped app uses the tested credential adapter, proven at the real entry point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: O-12 — make removeLocalData reachable and hostile-proof</name>
  <files>apps/desktop/preload/contracts.ts, apps/desktop/preload/index.ts, apps/desktop/main/index.ts, apps/desktop/test/ipc/hostile-bridge.test.ts</files>
  <read_first>apps/desktop/main/recovery/remove-local-data.ts, apps/desktop/preload/contracts.ts, apps/desktop/main/index.ts, .planning/phases/KPL-03-mac-daily-loop/03-05-SUMMARY.md, .planning/phases/KPL-03-mac-daily-loop/03-10-SUMMARY.md</read_first>
  <behavior>
    - A named preload contract and sender-validated IPC handler expose DesktopApplication.removeLocalData().
    - The second-confirmation fence is preserved and cannot be bypassed from the renderer.
    - No sync or network port becomes reachable; server deletion stays structurally impossible.
    - New hostile cases assert absence of privileged effect, not merely rejection.
  </behavior>
  <action>Add the strict zod contract on both sides plus the main-side handler behind assertTrustedSender and bounded parsing, following 03-10's established pattern exactly. Extend hostile-bridge.test.ts to cover the new surface.</action>
  <verify><automated>pnpm typecheck:desktop &amp;&amp; pnpm test:desktop:ipc &amp;&amp; pnpm test:desktop</automated><fails_when>non-zero exit, "0 tests" appears, ipc count does not grow, the confirmation fence is bypassable, or any server-delete path becomes reachable</fails_when></verify>
  <done>A user can remove local data; a hostile renderer cannot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: O-11 — implement D-06 renderer-semantic restoration</name>
  <files>apps/desktop/renderer/main.tsx, apps/desktop/renderer/desktopClientFacade.ts, packages/web-ui/src/ClientFacade.ts, packages/web-ui/src/workspace/Workspace.tsx</files>
  <read_first>apps/desktop/main/lifecycle.ts, packages/web-ui/src/workspace/Workspace.tsx, packages/web-ui/src/ClientFacade.ts, .planning/phases/KPL-03-mac-daily-loop/03-11-SUMMARY.md, .planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md</read_first>
  <behavior>
    - D-06: persist and restore sidebar visibility, pane sizes, destination, surviving selected task, semantic scroll anchor, and a recoverable editor draft.
    - Never restore transient dialogs, progress indicators, authentication prompts, or stale raw focus targets.
    - A selected task that no longer exists degrades safely rather than restoring a dangling selection.
    - D-03 holds: a restored draft makes NO durability claim.
    - packages/web-ui stays presentation-only; the import-boundary guard must still pass.
  </behavior>
  <action>Extend ClientFacade with named semantic restoration operations and persist the snapshot through the existing main-owned window-state port. Restore on renderer mount and prove across a real relaunch.</action>
  <verify><automated>pnpm typecheck:desktop &amp;&amp; pnpm typecheck:web &amp;&amp; pnpm test:desktop &amp;&amp; pnpm test:desktop:e2e</automated><fails_when>non-zero exit, "0 tests" appears, a platform import enters packages/web-ui, a transient state restores, or a dangling selection restores</fails_when></verify>
  <done>Reopening the app returns the workspace to where the person left it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: O-1 — resolve the web shared-workspace flag</name>
  <files>apps/web/src/app/WorkspaceShell.tsx</files>
  <read_first>apps/web/src/app/WorkspaceShell.tsx, apps/web/src/adapters/browserClientFacade.ts, .planning/phases/KPL-03-mac-daily-loop/03-09-SUMMARY.md</read_first>
  <behavior>
    - Preferred: the production Inbox route renders the shared slice and useSharedWorkspace is deleted.
    - Acceptable: web keeps its routed content, and the flag becomes documented permanent API with the reason recorded.
    - Either way no opt-in remains that only a test sets.
    - Existing web behavior must not regress: 15 files / 153 tests stay green.
  </behavior>
  <action>Attempt the flip. If the shared slice cannot yet serve the production route without regression, do not force it — record the decision explicitly and document the flag.</action>
  <verify><automated>pnpm --dir apps/web test --run &amp;&amp; pnpm typecheck:web</automated><fails_when>non-zero exit, "0 tests" appears, web behavior regresses, or an unset test-only opt-in remains undocumented</fails_when></verify>
  <done>No delivered code is reachable only from a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer → main | New removal contract must not widen renderer authority. |
| Persisted UI state → renderer | Restored state is untrusted input and must be validated. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-13-01 | Elevation of privilege | removal IPC contract | high | mitigate | Strict both-side schemas, sender allowlist, preserved confirmation fence. |
| T-KPL03-13-02 | Tampering | restored workspace snapshot | medium | mitigate | Validate and clamp restored state; drop dangling references. |
| T-KPL03-13-03 | Information disclosure | credential adapter swap | high | mitigate | Prove safeStorage-backed round trip and sign-out clearing at the shipped entry point. |
</threat_model>

<verification>Run the full desktop phase gate (node tooling/verify-desktop-phase.mjs) after all tasks; it must still report lanes=8 failed=0 with ipc >= 46.</verification>
<success_criteria>GAP-1 is closed: no feature delivered by phase KPL-03 is reachable only from a test, and the shipped credential path is the tested one.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-13-SUMMARY.md` when done.</output>
