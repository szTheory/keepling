---
phase: KPL-03-mac-daily-loop
plan: 05
type: execute
wave: 4
depends_on: [03-02, 03-04]
files_modified:
  - apps/desktop/main/index.ts
  - apps/desktop/main/application/DesktopApplication.ts
  - apps/desktop/main/recovery/remove-local-data.ts
  - apps/desktop/main/protocol.ts
  - apps/desktop/preload/contracts.ts
  - apps/desktop/preload/index.ts
  - apps/desktop/store-worker/index.ts
  - apps/desktop/store-worker/local-store.ts
  - apps/desktop/test/ipc/hostile-bridge.test.ts
  - apps/desktop/test/store/migrations-faults.test.ts
  - apps/desktop/test/e2e/sync-recovery.spec.ts
autonomous: true
requirements: [MAC-03, MAC-04, MAC-05, QUAL-04]
estimate: {tokens: 45000, raw_tokens: 45000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Renderer compromise or reload cannot obtain raw database, filesystem, credential, network, Electron event, generic IPC, or unrestricted platform authority."
    - "Migration checksum drift, integrity failure, permission/read-only/disk-full/SQLITE_BUSY/corruption opens a narrow recovery shell, preserves the old store, and never renders a false empty list or silently resets data."
    - "Remove data from this Mac is distinct from sign out and server deletion, shows bounded pending/conflicted counts, closes the store before whole-unit removal, requires a second Remove Anyway step for local-only intent, and verifies absence without claiming cryptographic erasure."
    - "Probe predicate (MAC-05 concurrency): renderer crash, IPC sequence gaps, Quick Entry/main writes, and remove-versus-sync/store ownership serialize without two writers or partial whole-unit removal."
  artifacts:
    - {path: apps/desktop/test/ipc/hostile-bridge.test.ts, provides: "Both-side schema/sender/clone/sequence privilege proof"}
    - {path: apps/desktop/test/store/migrations-faults.test.ts, provides: "Real SQLite migration/WAL/busy/disk/read-only/corruption proof"}
    - {path: apps/desktop/main/recovery/remove-local-data.ts, provides: "Fenced, confirmed, close-first namespace removal", exports: [removeLocalNamespaceData]}
    - {path: apps/desktop/main/protocol.ts, provides: "Local packaged-content protocol and navigation/permission policy"}
  key_links:
    - {from: apps/desktop/preload/index.ts, to: apps/desktop/main/application/DesktopApplication.ts, via: "strict request/response parsing plus sender allowlist"}
    - {from: apps/desktop/main/recovery/remove-local-data.ts, to: apps/desktop/store-worker/local-store.ts, via: "fence, close, remove DB/WAL/SHM unit, verify absence"}
    - {from: apps/desktop/store-worker/index.ts, to: apps/desktop/test/store/migrations-faults.test.ts, via: "validated worker protocol and deterministic fault port"}
  prohibitions:
    - {statement: "MUST NOT silently replace, reset, delete, copy, or open an in-memory substitute for a failed or corrupted local store."}
    - {statement: "MUST NOT describe local removal as server deletion, remote wipe, secure erase, or cryptographic erasure."}
    - {statement: "MUST NOT expose task text, drafts, commands, credentials, tokens, cursors, fingerprints, raw identifiers, server URLs, database paths, or task-derived titles through diagnostics or renderer capabilities."}
---

<objective>
Close hostile process, storage-failure, recovery-shell, and local-removal boundaries per D-22 through D-29, D-34, D-37, D-38, D-41, D-44, and D-48.

Purpose: Ensure the always-open client fails visibly and recoverably without escalating renderer privilege or deleting the only durable local intent.
Output: Hostile IPC proof, packaged local-content policy, real SQLite fault/migration suite, recovery shell orchestration, and safe namespace removal.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md
@.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md
@apps/desktop/main/application/DesktopApplication.ts
@apps/desktop/preload/contracts.ts
@apps/desktop/store-worker/local-store.ts
</context>

## Artifacts this phase produces

- Symbols: `removeLocalNamespaceData`, packaged-content `registerKeeplingProtocol`, strict worker/IPC request and response unions, and recovery-shell outcomes.
- Fields: closed store failure codes, bounded pending/conflict counts, removal phase/result, and presentation sequence gap recovery.
- Files: every path in `files_modified`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject hostile renderer, sender, schema, navigation, permission, and sequence-gap behavior</name>
  <files>apps/desktop/main/index.ts, apps/desktop/main/protocol.ts, apps/desktop/preload/contracts.ts, apps/desktop/preload/index.ts, apps/desktop/test/ipc/hostile-bridge.test.ts</files>
  <read_first>apps/desktop/main/index.ts, apps/desktop/preload/contracts.ts, apps/desktop/preload/index.ts, .planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md</read_first>
  <behavior>
    - D-27/D-28 every request/response is strict, clone-safe, parsed on both sides, sender-bound, and named; unknown operation/field/sender/noncloneable data fails closed.
    - Packaged renderer has sandbox/context isolation, no Node integration, restrictive CSP/custom local protocol, denied unexpected navigation/window/permission, and an allowlisted external-link path.
    - D-29 renderer subscribes before snapshot; sequence gaps and reload/crash refetch an opaque snapshot without losing committed mutations.
  </behavior>
  <action>Harden BrowserWindow/webPreferences and register a privileged custom local protocol that loads only packaged assets. Centralize sender validation and strict request/response schemas; remove any generic channel or raw Electron callback surface. Add hostile tests for extra/deep fields, malformed unions, unknown methods, forged sender/frame, noncloneable responses, navigation/window creation/permissions, CSP/local content, missing/out-of-order/duplicate sequence, reload, and renderer crash after store COMMIT before UI acknowledgement.</action>
  <acceptance_criteria>Every hostile case fails closed with a bounded error, no privileged effect, and unchanged durable state; reload/crash reconstructs the committed task from a fresh snapshot.</acceptance_criteria>
  <verify>
    <automated>pnpm test:desktop:ipc</automated>
    <fails_when>non-zero exit, "0 tests" appears, any hostile request reaches a port, unexpected content/navigation/permission succeeds, or a sequence gap applies an incremental update without snapshot refetch</fails_when>
  </verify>
  <done>The renderer remains a disposable, least-privilege mirror under hostile inputs and crashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail safely across migrations, WAL ownership, busy, disk, permission, read-only, transaction interruption, and corruption</name>
  <files>apps/desktop/store-worker/index.ts, apps/desktop/store-worker/local-store.ts, apps/desktop/main/application/DesktopApplication.ts, apps/desktop/test/store/migrations-faults.test.ts, apps/desktop/test/e2e/sync-recovery.spec.ts</files>
  <read_first>apps/desktop/store-worker/index.ts, apps/desktop/store-worker/local-store.ts, apps/desktop/migrations/0001_initial.sql, apps/desktop/migrations/manifest.json, .planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md</read_first>
  <behavior>
    - D-22 startup validates namespace, migration checksums, foreign keys/invariants/integrity, then exposes local projection before network; failure enters recovery shell without new store/reset.
    - D-34/D-38 one worker writer owns WAL/FULL/checkpoint/busy behavior; database, WAL, SHM remain one unit; live copying/unlinking is absent.
    - Recovery shell emits exact UI-SPEC copy/actions for permission, read-only, disk-full, migration, integrity, and corruption cases.
  </behavior>
  <action>Add retained fresh-create and forward-migration fixtures, checksum drift, failed migration rollback, foreign-key/invariant failure, finite SQLITE_BUSY, disk-full, read-only, permission, quit during transaction, WAL checkpoint, backup, and corruption cases against the real embedded adapter. The worker must preserve prior files on every failure and publish a closed failure code to `DesktopApplication`, which exposes only Retry Opening/Show Recovery Options and never a false workspace empty state. Assert no main/window module imports node:sqlite.</action>
  <acceptance_criteria>Every injected failure either commits completely or rolls back completely, preserves the old durable unit, renders the exact recovery state, and can be retried after the condition is repaired.</acceptance_criteria>
  <verify>
    <automated>pnpm test:desktop -- migrations-faults &amp;&amp; pnpm test:desktop:e2e -- sync-recovery</automated>
    <fails_when>non-zero exit, "0 tests" appears, any fixture auto-resets/opens memory/loses WAL state, a partial transaction earns success, or the workspace shows an authoritative empty list after store-open failure</fails_when>
  </verify>
  <done>Every required local-store failure is explicit, non-destructive, retryable where possible, and proven on real SQLite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fence and remove one namespace from this Mac without server deletion or silent local-intent loss</name>
  <files>apps/desktop/main/recovery/remove-local-data.ts, apps/desktop/main/application/DesktopApplication.ts, apps/desktop/store-worker/local-store.ts, apps/desktop/test/e2e/sync-recovery.spec.ts</files>
  <read_first>.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md, apps/desktop/main/application/DesktopApplication.ts, apps/desktop/main/adapters/credentials.ts, apps/desktop/store-worker/local-store.ts</read_first>
  <behavior>
    - D-24 first confirmation names local scope and server non-deletion; pending/conflicted counts offer Sync First and second Remove Local Changes Anyway confirmation with Keep Data initial focus.
    - Namespace mismatch requires Inspect and Export before separately confirmed removal; it never merges or sends prior data.
    - Removal fences sync, closes worker/store, removes credentials/database/WAL/SHM/temp/cache/index/metadata for only that namespace, verifies absence, and honestly avoids cryptographic-erasure language.
  </behavior>
  <action>Implement a serialized removal state machine called only through `DesktopApplication.removeLocalData`. Snapshot bounded counts, fence the namespace against new work, resolve dirty drafts, offer Sync First only when valid, then require the second exact confirmation when local-only intent exists. Close the store before deleting the explicit namespace-scoped file inventory, remove credentials afterward, verify every target is absent, and leave unrelated namespaces untouched. Test remove-versus-sync and remove-versus-Quick-Entry races, failed file removal, restart after partial external filesystem failure, and no server delete request.</action>
  <acceptance_criteria>No local-only intent can be removed with one confirmation; successful removal leaves no named namespace artifact or credential and sends no server-deletion command; failed removal remains inspectable and retryable.</acceptance_criteria>
  <verify>
    <automated>pnpm test:desktop:e2e -- remove-local-data</automated>
    <fails_when>non-zero exit, "0 tests" appears, one-step removal succeeds with pending/conflicted intent, the store is live during unlink, another namespace changes, any target remains after a success claim, or a server delete is invoked</fails_when>
  </verify>
  <done>Local removal is separately scoped, double-confirmed when necessary, close-first, verified, and honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer/frame → privileged IPC | Compromised or unexpected frames must not invoke filesystem/store/credential authority. |
| Filesystem/runtime → SQLite unit | Partial migrations, busy/disk faults, and live WAL handling can destroy durable intent. |
| User removal intent → namespace artifacts | A local action must not delete server or another namespace data. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-05-01 | Elevation of privilege | IPC/protocol | high | mitigate | Strict schemas, sender validation, sandbox/CSP/local protocol, permission/navigation denial. |
| T-KPL03-05-02 | Tampering | migrations/WAL | high | mitigate | Checksums, transactional migrations, one worker writer, close/backup whole unit, no reset. |
| T-KPL03-05-03 | Information disclosure | namespace removal | high | mitigate | Exact namespace inventory, fence/close first, unrelated namespace assertions. |
| T-KPL03-05-04 | Denial of service | store faults | medium | mitigate | Finite busy handling, closed recovery shell, retry after external repair. |
</threat_model>

<verification>Run IPC, real-store fault, recovery, and removal lanes with non-zero named case counts.</verification>
<success_criteria>Privilege escalation, store faults, and destructive local recovery fail closed without silent loss or false claims.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-05-SUMMARY.md` when done.</output>
