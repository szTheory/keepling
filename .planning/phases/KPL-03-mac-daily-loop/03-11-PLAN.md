---
phase: KPL-03-mac-daily-loop
plan: 11
type: execute
wave: 8
depends_on: [03-04]
files_modified:
  - apps/desktop/main/lifecycle.ts
  - apps/desktop/main/windows/main-window.ts
  - apps/desktop/test/e2e/lifecycle.spec.ts
autonomous: true
requirements: [MAC-02, MAC-03, MAC-05]
estimate: {tokens: 19000, raw_tokens: 19000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "One resident single-instance owner survives main-window close, keeps Quick Entry/store/sync available, and recreates the renderer from durable state on Dock activation or second launch."
    - "Validated restoration includes only D-06 semantic state and clamps bounds on screen; transient dialogs/progress/auth/raw focus never restore."
    - "D-21 quit is bounded and never waits for server acknowledgement or decides whether accepted intent exists."
  artifacts:
    - {path: apps/desktop/main/lifecycle.ts, provides: "Single-instance resident close/activate/quit owner", exports: [DesktopLifecycle]}
    - {path: apps/desktop/test/e2e/lifecycle.spec.ts, provides: "Close, activate, restoration, quit, and relaunch proof"}
  key_links:
    - {from: apps/desktop/main/lifecycle.ts, to: apps/desktop/main/windows/main-window.ts, via: "recreate disposable window from durable semantic snapshot"}
  prohibitions:
    - {statement: "MUST NOT make graceful quit, server acknowledgement, wake, timer, socket, or background execution a durability prerequisite."}
---

<objective>
Prove the resident single-instance lifecycle and validated restoration separately from menus and Quick Entry.

Purpose: Keep lifecycle interruption/recreation proof within a focused file and context budget.
Output: `DesktopLifecycle`, validated main-window restoration, and deterministic E2E evidence.
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md
@apps/desktop/main/windows/main-window.ts
@apps/desktop/main/application/DesktopApplication.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement single-instance residency, validated restoration, and bounded quit</name>
  <files>apps/desktop/main/lifecycle.ts, apps/desktop/main/windows/main-window.ts</files>
  <read_first>.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md, apps/desktop/main/index.ts, apps/desktop/main/windows/main-window.ts, apps/desktop/main/application/DesktopApplication.ts</read_first>
  <behavior>
    - D-20 second launch activates the existing instance; main close resolves dirty work and leaves store/sync/Quick Entry resident; Dock activation recreates the window.
    - D-06 bounds are clamped and only fullscreen/sidebar/panes/destination/surviving selection/semantic anchor/durable draft restore.
    - D-21 quit fences new network work, performs bounded cancellation/checkpoint/close, and exits without server acknowledgement.
  </behavior>
  <action>Own Electron lifecycle in `DesktopLifecycle`: request the single-instance lock, separate close from quit, recreate the disposable renderer on activate, and serialize validated restoration through the main-owned application/store. Resolve dirty editor and Quick Entry with exact safe-default dialogs. On quit set a stopping fence, reject new sync passes, await only bounded worker/network cancellation and SQLite checkpoint/close, then exit.</action>
  <verify><automated>pnpm test:desktop:e2e -- lifecycle-core</automated><fails_when>non-zero exit, "0 tests" appears, second process owns the profile, close terminates the resident owner, transient state restores, or quit waits for server acknowledgement</fails_when></verify>
  <done>The lifecycle owner has one deterministic close/activate/quit path independent of network success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove interruption, bounds, recreation, and committed-intent retention</name>
  <files>apps/desktop/test/e2e/lifecycle.spec.ts</files>
  <read_first>apps/desktop/main/lifecycle.ts, apps/desktop/main/windows/main-window.ts, apps/desktop/main/application/DesktopApplication.ts, .planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md</read_first>
  <behavior>
    - Invalid displays, fullscreen/sidebar/panes/destination/selection/anchor/draft, renderer recreation, second launch, Command-H, close-versus-quit, pending network, and transaction interruption are explicit cases.
    - Stable task identity restores focus and semantic scroll; stale raw focus does not.
    - Every post-COMMIT mutation survives quit/relaunch without a graceful-shutdown dependency.
  </behavior>
  <action>Build the isolated-profile lifecycle suite with deterministic display, network, transaction, and process-kill ports. Assert exact restoration allowlist/denylist, one resident owner/window, bounded exit, and durable reconstruction around pre-COMMIT rollback versus post-COMMIT retention.</action>
  <verify><automated>pnpm test:desktop:e2e -- lifecycle</automated><fails_when>non-zero exit, "0 tests" appears, invalid bounds stay off-screen, raw/transient state restores, close kills capture, or committed intent disappears</fails_when></verify>
  <done>Keepling behaves as one resident Mac application whose lifecycle is never a commit protocol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| OS lifecycle → durable store | Timing must not decide accepted intent. |
| Stored window state → recreated renderer | Private, transient, stale, or off-screen state must not restore. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-11-01 | Denial of service | single-instance/quit | medium | mitigate | One lock, bounded cancellation, deterministic recreation. |
| T-KPL03-11-02 | Information disclosure | restoration metadata | high | mitigate | Semantic allowlist and coarse titles only. |
| T-KPL03-11-03 | Tampering | quit transaction | high | mitigate | COMMIT is the only local-success boundary; rollback/relaunch tests. |
</threat_model>

<verification>Run lifecycle core and full isolated-profile E2E suites with positive case counts.</verification>
<success_criteria>Close, Dock activation, second launch, and quit preserve the resident workflow and every accepted mutation.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-11-SUMMARY.md` when done.</output>
