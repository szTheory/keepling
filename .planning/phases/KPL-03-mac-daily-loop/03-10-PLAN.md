---
phase: KPL-03-mac-daily-loop
plan: 10
type: execute
wave: 7
depends_on: [03-03]
files_modified:
  - apps/desktop/main/index.ts
  - apps/desktop/main/protocol.ts
  - apps/desktop/preload/contracts.ts
  - apps/desktop/preload/index.ts
  - apps/desktop/test/ipc/hostile-bridge.test.ts
autonomous: true
requirements: [MAC-03, MAC-04, MAC-05, QUAL-04]
estimate: {tokens: 21000, raw_tokens: 21000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Hostile renderer requests, frames, fields, operations, clone values, navigation, windows, permissions, and sequences fail closed without privileged effects."
    - "Renderer reload/crash after COMMIT reconstructs from an opaque snapshot and never loses accepted intent."
  artifacts:
    - {path: apps/desktop/main/protocol.ts, provides: "Restrictive packaged local-content/navigation/permission policy"}
    - {path: apps/desktop/test/ipc/hostile-bridge.test.ts, provides: "Hostile two-sided bridge and sequence proof"}
  key_links:
    - {from: apps/desktop/preload/index.ts, to: apps/desktop/main/index.ts, via: "strict both-side schemas plus sender allowlist"}
  prohibitions:
    - {statement: "MUST NOT expose generic IPC, raw Electron events, filesystem/database/credential/fetch authority, or unexpected packaged navigation."}
---

<objective>
Seal the hostile renderer and packaged-content boundary before destructive store recovery work.

Purpose: Isolate privilege/security proof from storage corruption and namespace removal so each plan stays executable within scope.
Output: Hardened BrowserWindow/protocol/preload registration and non-vacuous hostile IPC suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md
@apps/desktop/main/application/DesktopApplication.ts
@apps/desktop/preload/contracts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden packaged content, BrowserWindow privileges, and named preload contracts</name>
  <files>apps/desktop/main/index.ts, apps/desktop/main/protocol.ts, apps/desktop/preload/contracts.ts, apps/desktop/preload/index.ts</files>
  <read_first>apps/desktop/main/index.ts, apps/desktop/preload/index.ts, .planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md</read_first>
  <behavior>
    - D-27/D-28 strict clone-safe requests and responses are parsed on both sides, sender/frame-bound, and named.
    - Packaged renderer uses sandbox/context isolation, no Node integration, restrictive CSP/local protocol, denied unexpected navigation/window/permission, and allowlisted external links.
    - D-29 subscription precedes snapshot and gaps trigger opaque refetch.
  </behavior>
  <action>Register a privileged custom local protocol loading only packaged assets; harden webPreferences, navigation, window creation, permission, and external-link policy. Centralize strict request/response schemas plus sender/frame validation and remove any generic channel or raw callback surface. Preserve the exact named semantic capabilities and sequence contract already consumed by `ClientFacade`.</action>
  <verify><automated>pnpm test:desktop:ipc -- protocol-contract</automated><fails_when>non-zero exit, "0 tests" appears, an unknown/extra request reaches a port, or unexpected content/navigation/permission succeeds</fails_when></verify>
  <done>The renderer has only the intended packaged content and named semantic capabilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove hostile schemas, senders, cloning, sequence gaps, reload, and crash</name>
  <files>apps/desktop/test/ipc/hostile-bridge.test.ts</files>
  <read_first>apps/desktop/main/protocol.ts, apps/desktop/preload/contracts.ts, apps/desktop/preload/index.ts, apps/desktop/main/application/DesktopApplication.ts</read_first>
  <behavior>
    - Extra/deep fields, malformed unions, unknown methods, forged sender/frame, and noncloneable values fail closed with bounded codes.
    - Missing/out-of-order/duplicate sequences refetch rather than apply unsafe increments.
    - Reload/crash after local COMMIT before UI acknowledgement reconstructs the committed task from a fresh snapshot.
  </behavior>
  <action>Create deterministic hostile fixtures for every D-28/D-29 case, including durable-state assertions before and after each attempt. Count and name every case; assert no privileged port invocation on validation failure and no committed mutation loss on renderer failure.</action>
  <verify><automated>pnpm test:desktop:ipc</automated><fails_when>non-zero exit, "0 tests" appears, a hostile input has a privileged effect, a gap applies incrementally, or reload/crash loses the committed task</fails_when></verify>
  <done>The renderer remains a disposable least-privilege mirror under hostile inputs and crashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Renderer/frame → privileged main | Compromised content must not invoke ambient authority. |
| Packaged protocol → renderer | Navigation or resource substitution must fail closed. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-10-01 | Elevation of privilege | preload/main IPC | high | mitigate | Strict schemas, sender validation, named capabilities. |
| T-KPL03-10-02 | Spoofing | local protocol/navigation | high | mitigate | Packaged asset allowlist and denied unexpected navigation/window/permission. |
| T-KPL03-10-03 | Tampering | sequence mirror | high | mitigate | Monotonic sequence and opaque snapshot recovery. |
</threat_model>

<verification>Run the full IPC lane and require positive named hostile-case counts.</verification>
<success_criteria>Hostile renderer and content behavior cannot escalate privilege or alter durable intent.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-10-SUMMARY.md` when done.</output>
