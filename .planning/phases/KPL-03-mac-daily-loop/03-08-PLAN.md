---
phase: KPL-03-mac-daily-loop
plan: 08
type: execute
wave: 10
depends_on: [03-03, 03-04, 03-05, 03-11]
files_modified:
  - apps/desktop/test/performance/runtime.spec.ts
  - tooling/measure-desktop-performance.mjs
  - apps/desktop/performance-budgets.json
  - docs/testing/desktop-performance.md
autonomous: true
requirements: [MAC-01, MAC-02, MAC-03, MAC-05, QUAL-03]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "D-43 cold/warm launch, Quick Entry open-to-focus, local commit, list scrolling, idle CPU/memory, wake/reconnect work, WAL size/checkpoint cost, and packaged footprint all produce named digest-bound measurements."
    - "No numeric release threshold is fabricated: the first representative run records baselines and a named regression budget with rationale; later runs fail closed on missing or exceeded budgets."
    - "Measurements prove database/network work does not block the Electron main or renderer thread and contain no private task content, raw identifiers, URLs, or paths."
  artifacts:
    - {path: tooling/measure-desktop-performance.mjs, provides: "D-43 measurement orchestrator and evidence schema"}
    - {path: apps/desktop/performance-budgets.json, provides: "Named baseline and regression-budget decisions"}
    - {path: docs/testing/desktop-performance.md, provides: "Reproduction and interpretation contract"}
  key_links:
    - {from: tooling/measure-desktop-performance.mjs, to: apps/desktop/test/performance/runtime.spec.ts, via: "exact packaged executable, deterministic fixture, and sampled OS/runtime metrics"}
    - {from: tooling/measure-desktop-performance.mjs, to: apps/desktop/performance-budgets.json, via: "metric-name baseline/budget comparison with recorded rationale"}
  prohibitions:
    - {statement: "MUST NOT invent numeric release thresholds, optimize before recording a baseline, or emit private task/profile/path data in performance evidence."}
---

<objective>
Make the complete D-43 performance contract executable and regression-sensitive against exact packaged bytes.

Purpose: Establish honest baseline evidence for local responsiveness and runtime cost before optimization or release claims.
Output: Packaged runtime scenarios, measurement tooling, baseline/budget decision record, and reproducibility guide.
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md
@.planning/phases/KPL-03-mac-daily-loop/03-VALIDATION.md
@tooling/smoke-desktop-packaged.mjs
</context>

## Artifacts this phase produces

- Metrics: `cold_launch_to_local_interactive_ms`, `warm_launch_to_local_interactive_ms`, `quick_entry_open_to_focus_ms`, `local_commit_ms`, `list_scroll_frame_p95_ms`, `idle_cpu_percent`, `idle_memory_mb`, `wake_reconnect_main_thread_work_ms`, `wake_reconnect_renderer_thread_work_ms`, `wal_bytes`, `checkpoint_ms`, and `packaged_app_bytes`/ZIP bytes.
- Evidence fields: source and artifact digest, hardware/macOS/runtime/schema versions, fixture size, sample count, distribution summary, baseline, regression budget, rationale, and pass/fail.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure every D-43 product and runtime path against one exact packaged digest</name>
  <files>apps/desktop/test/performance/runtime.spec.ts, tooling/measure-desktop-performance.mjs</files>
  <read_first>.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md, tooling/smoke-desktop-packaged.mjs, apps/desktop/test/packaged/offline-capture.spec.ts, apps/desktop/test/e2e/lifecycle.spec.ts</read_first>
  <behavior>
    - D-43 records cold and warm launch to local-interactive separately, Quick Entry invocation to focused title, and semantic mutation invocation to post-COMMIT local_saved.
    - D-43 records list-scroll frame distribution on a deterministic many-task fixture, quiescent idle CPU/RSS, wake/reconnect main/renderer work, WAL bytes plus checkpoint duration, and `.app`/ZIP footprint.
    - Each result binds to the exact artifact digest and records environment, fixture, sample count, p50/p95/max where meaningful, while privacy validation rejects content, raw identifiers, URLs, and profile/database paths.
  </behavior>
  <action>Launch only the already-built copied `.app` from the package manifest. Use deterministic privacy-safe fixtures and enough repeated samples to report distributions rather than one timing. Instrument local-interactive, focused Quick Entry, post-COMMIT local_saved, scroll frames, idle resource samples, wake/reconnect thread work, WAL/checkpoint, and artifact sizes as the named fields above. Assert no meaningful database/network work runs on main or renderer during commit/reconnect. Emit JSON evidence plus a human summary, both bound to source/artifact digest and closed environment metadata.</action>
  <acceptance_criteria>All twelve named D-43 metrics exist with non-zero sample counts, units, artifact digest, environment/fixture metadata, and privacy pass; thread-work assertions show store/network ownership outside UI threads.</acceptance_criteria>
  <verify><automated>node tooling/measure-desktop-performance.mjs --manifest .artifacts/desktop/package-manifest.json --record-baseline</automated><fails_when>non-zero exit, any named metric/sample/unit/digest is absent, a development/source executable is used, private evidence appears, or database/network work blocks main/renderer</fails_when></verify>
  <done>The complete D-43 measurement set has fresh reproducible baseline evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Record named regression-budget decisions and fail later measurements on drift</name>
  <files>apps/desktop/performance-budgets.json, docs/testing/desktop-performance.md, tooling/measure-desktop-performance.mjs</files>
  <read_first>tooling/measure-desktop-performance.mjs, .planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md</read_first>
  <action>For every D-43 metric, persist the measured baseline, statistic used for comparison, a named absolute or relative regression budget, environment applicability, sample floor, and rationale based on observed variance. Do not invent a product threshold absent from CONTEXT; label these as regression budgets against the recorded baseline. Document reproduction, noise handling, baseline replacement approval, and the rule that a runtime/storage/artifact change reruns the exact packaged lane. Make normal verification refuse missing budgets, stale artifact/input digests, insufficient samples, and exceeded budgets.</action>
  <acceptance_criteria>Every metric has one reviewed baseline and regression-budget decision with rationale; a synthetic regression beyond each budget makes the verifier fail, while unchanged evidence passes reproducibly.</acceptance_criteria>
  <verify><automated>node tooling/measure-desktop-performance.mjs --manifest .artifacts/desktop/package-manifest.json --check-budgets --self-test-regression</automated><fails_when>any metric lacks a baseline/budget/rationale, a stale digest passes, or the injected regression remains green</fails_when></verify>
  <done>D-43 evidence is regression-sensitive without fabricating unsupported release thresholds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Packaged process → measurements | Development or stale bytes can produce misleading baselines. |
| OS/runtime samples → evidence | Paths/content/identifiers must not leak. |
| Baseline → release gate | Noise or invented thresholds can create false pass/fail claims. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-08-01 | Spoofing | artifact under test | high | mitigate | Exact external executable and manifest digest validation. |
| T-KPL03-08-02 | Information disclosure | performance evidence | high | mitigate | Closed fields and automated privacy rejection. |
| T-KPL03-08-03 | Repudiation | regression budgets | medium | mitigate | Baseline, statistic, budget, environment, sample floor, and rationale per metric. |
</threat_model>

<verification>Record the exact packaged baseline, then run budget comparison and injected-regression self-test.</verification>
<success_criteria>Every D-43 measurement is recorded, reproducible, privacy-safe, and governed by a named regression budget rather than an invented release threshold.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-08-SUMMARY.md` when done.</output>
