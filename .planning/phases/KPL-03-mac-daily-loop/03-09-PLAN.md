---
phase: KPL-03-mac-daily-loop
plan: 09
type: execute
wave: 5
depends_on: [03-02]
files_modified:
  - packages/web-ui/package.json
  - packages/web-ui/src/ClientFacade.ts
  - packages/web-ui/src/workspace/Workspace.tsx
  - packages/web-ui/src/capture/CaptureForm.tsx
  - packages/web-ui/src/tasks/TaskList.tsx
  - apps/web/src/app/AppShell.tsx
  - apps/web/src/app/WorkspaceShell.tsx
autonomous: true
requirements: [MAC-01, MAC-02, MAC-04, QUAL-04]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The shared React seam contains presentation only and invokes named semantic operations through `ClientFacade`; browser and Electron authority remain separate."
    - "One Inbox capture/list/detail slice behaves identically through the web adapter and the main-owned desktop adapter without importing fetch, IPC, lifecycle, or persistence into shared UI."
    - "D-04/D-05 responsive workspace, stable focus/selection identity, independent scrolling, and inherited D-40 meaning are established before full workspace expansion."
  artifacts:
    - {path: packages/web-ui/src/ClientFacade.ts, provides: "Platform-free semantic presentation contract", exports: [ClientFacade]}
    - {path: packages/web-ui/src/workspace/Workspace.tsx, provides: "Responsive navigation/list/detail presentation"}
  key_links:
    - {from: packages/web-ui/src/ClientFacade.ts, to: apps/web/src/app/AppShell.tsx, via: "browser-owned adapter"}
    - {from: packages/web-ui/src/ClientFacade.ts, to: apps/desktop/preload/contracts.ts, via: "desktop adapter supplied later without shared import"}
  prohibitions:
    - {statement: "MUST NOT import browser session/submission/history/fetch, Electron/preload, lifecycle, SQLite, or persistence records into packages/web-ui."}
---

<objective>
Freeze the presentation-only React extraction boundary and prove one shared capture/list/detail slice before the complete workspace expands.

Purpose: Resolve the research extraction question and remove the temporal dependency between ClientFacade creation and native menu work.
Output: `packages/web-ui`, `ClientFacade`, one responsive workspace slice, and web-regression/import-boundary proof.
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/architecture/REPOSITORY.md
@docs/brand/BRAND-SEED.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md
@.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md
@apps/web/src/app/AppShell.tsx
@apps/web/src/app/WorkspaceShell.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the presentation-only ClientFacade and import boundary</name>
  <files>packages/web-ui/package.json, packages/web-ui/src/ClientFacade.ts, apps/web/src/app/AppShell.tsx</files>
  <read_first>apps/web/src/app/AppShell.tsx, apps/web/src/api/keepling.ts, apps/web/src/commands/submission.ts, docs/architecture/REPOSITORY.md, .planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md</read_first>
  <behavior>
    - D-26/D-27 the facade exposes snapshot/subscription and named semantic task/navigation/recovery operations only.
    - D-30 renderer view models remain distinct from generated wire DTOs, browser models, and SQLite records.
    - Import guards reject browser transport/session/history and Electron/preload/storage/lifecycle modules from the shared package.
  </behavior>
  <action>Create strict presentation models plus named async facade operations and sequence subscription. Adapt the browser composition through a browser-owned facade adapter while retaining its existing exact-submission/session behavior. Add import-boundary tests that freeze presentation-only ownership and permit deliberate small adapter duplication when a platform concern would otherwise leak.</action>
  <verify><automated>pnpm --dir apps/web test --run &amp;&amp; pnpm test:desktop -- client-facade-boundary</automated><fails_when>non-zero exit, "0 tests" appears, a platform/persistence import enters web-ui, or browser exact-submission behavior changes</fails_when></verify>
  <done>The platform-free boundary is executable and browser behavior remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract one responsive Inbox capture/list/detail slice</name>
  <files>packages/web-ui/src/workspace/Workspace.tsx, packages/web-ui/src/capture/CaptureForm.tsx, packages/web-ui/src/tasks/TaskList.tsx, apps/web/src/app/WorkspaceShell.tsx</files>
  <read_first>apps/web/src/app/WorkspaceShell.tsx, apps/web/src/features/capture/QuickCapture.tsx, packages/web-ui/src/ClientFacade.ts, packages/design-tokens/tokens.json, .planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md</read_first>
  <behavior>
    - D-04/D-05 workspace uses navigation/list/detail at 1064px+, drawer at 1024–1063px, and one routed surface below 1024px with focus independent from selection.
    - D-40 semantic tokens, plain-text bounds, copy voice, and focus safety retain meaning.
    - The same shared slice renders through web and a deterministic desktop-facade fixture.
  </behavior>
  <action>Move only proven capture, task-list, and workspace presentation behind the facade. Implement the UI-SPEC geometry, divider keyboard behavior, independent scroll regions, stable task identities, system-theme semantic tokens, and plain-text bounds. Keep platform routing/window behavior in each adapter.</action>
  <verify><automated>pnpm --dir apps/web test --run &amp;&amp; pnpm typecheck:desktop &amp;&amp; pnpm test:desktop -- workspace-tracer</automated><fails_when>non-zero exit, "0 tests" appears, web behavior regresses, focus/selection uses DOM position, or desktop capture bypasses ClientFacade</fails_when></verify>
  <done>One shared presentation slice works through distinct web and Electron adapters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Shared UI → platform adapters | Presentation must not acquire privileged or browser-specific authority. |
| User text → renderer | Task content remains bounded plain text. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-09-01 | Elevation of privilege | ClientFacade | high | mitigate | Named semantic methods and import guard. |
| T-KPL03-09-02 | Information disclosure | shared presentation | high | mitigate | Plain-text rendering and no native metadata ownership. |
</threat_model>

<verification>Run web regression, facade import-boundary, desktop typecheck, and workspace tracer commands.</verification>
<success_criteria>The extraction boundary is resolved as presentation-only, and Plan 03-03 can expand from an existing `ClientFacade` without cross-platform authority leakage.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-09-SUMMARY.md` when done.</output>
