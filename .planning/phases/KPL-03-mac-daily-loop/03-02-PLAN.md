---
phase: KPL-03-mac-daily-loop
plan: 02
type: execute
wave: 3
depends_on: [03-01]
files_modified:
  - apps/desktop/main/application/DesktopApplication.ts
  - apps/desktop/main/application/presentation.ts
  - apps/desktop/main/adapters/sync.ts
  - apps/desktop/main/adapters/credentials.ts
  - apps/desktop/store-worker/local-store.ts
  - apps/desktop/preload/contracts.ts
  - apps/desktop/test/application/sync-vectors.test.ts
  - apps/desktop/test/application/recovery-presentation.test.ts
  - apps/desktop/test/e2e/real-stack-sync.spec.ts
autonomous: true
requirements: [MAC-03, MAC-04, SRV-02]
estimate: {tokens: 44000, raw_tokens: 44000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Offline and actionable synchronization uses one main-owned projection: healthy is quiet; delayed, failure, conflict, authentication, namespace, and store states carry exact human text, bounded counts, coarse times, and named recovery actions."
    - "After reconnect or resume, at least one bounded pull occurs before pushes are interleaved; exact immutable commands survive response loss and relaunch without duplicated semantic effects."
    - "A different account/server/generation namespace never drains, merges, or rewrites prior local intent, and sign-out fences locally before remote revocation."
    - "Probe predicates (MAC-04): every closed state is renderer-reachable without logs and cannot be inferred from navigator online state, sockets, timers, or another lane's success."
    - "Probe predicate (SRV-02 concurrency): concurrent and duplicate Electron delivery uses the released semantic endpoints, server revisions, exact receipts, and conflicts; desktop code never reimplements canonical merge or invariant authority."
    - "UI consideration — offline/actionable synchronization: healthy state is quiet; delayed and actionable states use one authoritative projection, explicit text, bounded counts, and exact recovery actions without claiming completeness."
  artifacts:
    - {path: apps/desktop/main/application/presentation.ts, provides: "Closed renderer synchronization and recovery projection", exports: [DesktopPresentation, deriveDesktopPresentation]}
    - {path: apps/desktop/main/adapters/sync.ts, provides: "Generated-contract compatibility/bootstrap/feed/push/lookup adapter", exports: [KeeplingSyncAdapter]}
    - {path: apps/desktop/main/adapters/credentials.ts, provides: "Async safeStorage-backed credential seam", exports: [SafeStorageCredentialAdapter]}
    - {path: apps/desktop/test/e2e/real-stack-sync.spec.ts, provides: "Phoenix/PostgreSQL Electron adapter proof"}
  key_links:
    - {from: apps/desktop/main/adapters/sync.ts, to: packages/contracts/openapi/keepling.yaml, via: "generated client operations and explicit wire-to-client mappers"}
    - {from: apps/desktop/main/application/DesktopApplication.ts, to: apps/desktop/main/application/presentation.ts, via: "all sync/store outcomes reduce into one sequence-tagged projection"}
    - {from: apps/desktop/main/adapters/credentials.ts, to: apps/desktop/main/application/DesktopApplication.ts, via: "async CredentialPort; raw values never enter renderer projection"}
  prohibitions:
    - {statement: "MUST NOT silently merge, drain, remove, or send local intent under a different account, server, restore epoch, or synchronization generation."}
    - {statement: "MUST NOT claim complete synchronization from connectivity, an empty page, one successful lane, or absence of an error."}
    - {statement: "MUST NOT use routine synchronization notifications, celebration, pressure, or attention-grabbing status to manipulate continued use."}
---

<objective>
Expand the packaged tracer into real exact synchronization, namespace fencing, and calm recovery presentation per D-01 through D-03, D-15 through D-19, D-23, D-25, D-27, D-30, D-31, D-44, and D-48.

Purpose: Make offline work reconcilable against the real Phase 2 server without moving canonical authority into Electron.
Output: Generated-contract sync adapter, credential port, exact durable settlement, one authoritative recovery projection, and real-stack proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md
@.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-01-SUMMARY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-05-SUMMARY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-11-SUMMARY.md
@packages/contracts/openapi/keepling.yaml
@packages/contracts/vectors/sync.json
@apps/web/src/api/keepling.ts
</context>

## Artifacts this phase produces

- Symbols: `KeeplingSyncAdapter`, `SafeStorageCredentialAdapter`, `DesktopPresentation`, `deriveDesktopPresentation`, bounded `runSyncPass`, and namespace fence operations.
- Fields: monotonic `presentationSequence`, bounded pending/conflict/auth counts, coarse `lastSuccessfulContact`, and closed recovery action codes.
- Files: every path in `files_modified`; no new public wire fields.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive the Phase 2 vectors through the durable desktop store and bounded scheduler</name>
  <files>apps/desktop/main/application/DesktopApplication.ts, apps/desktop/store-worker/local-store.ts, apps/desktop/test/application/sync-vectors.test.ts</files>
  <read_first>apps/desktop/main/application/DesktopApplication.ts, apps/desktop/store-worker/local-store.ts, packages/contracts/vectors/sync.json, packages/contracts/schemas/sync-state-machine.schema.json, apps/server/lib/keepling/application/sync/reference_model.ex, .planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md</read_first>
  <behavior>
    - D-18 each hint requests one bounded pass: pull up to 50, then interleave ready pushes up to 25 with capped exponential backoff and jitter; timers/sockets never decide correctness.
    - D-36 exact matching terminal settlement commits shadow/conflict, journal terminalization, projection replay, and exact outbox removal atomically; mismatch and unknown acknowledgement preserve state.
    - D-23 fencing selects a store by issuer, origin, stable server instance, account subject, and synchronization generation; prior namespaces remain isolated and inspectable.
  </behavior>
  <action>Implement storage-neutral scheduling and reducer behavior in `DesktopApplication` against the existing `LocalStorePort`, then add the required worker operations without copying Elixir persistence records. Consume every Phase 2 vector with fixed clocks/IDs and assert cross-runtime equality for accepted, already-satisfied, rejection, stale, conflict, tombstone, cursor, response-loss, reconnect, relaunch, auth fence, and namespace changes. Preserve exact serialized command bytes on every retry and implement finite, injected backoff constants.</action>
  <acceptance_criteria>All named sync vectors execute non-vacuously; only accepted/already-satisfied dependencies unblock; mismatched acknowledgements and namespace switches cannot remove or send prior intent.</acceptance_criteria>
  <verify>
    <automated>pnpm test:desktop -- sync-vectors &amp;&amp; ./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/sync/reference_model_test.exs'</automated>
    <fails_when>non-zero exit, "0 tests" appears, vector counts differ, a mismatched acknowledgement mutates state, or a fenced namespace produces a ready push</fails_when>
  </verify>
  <done>The durable desktop state machine matches the Phase 2 reference under retries, conflicts, relaunch, and fencing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect native authorization and released sync operations without cloning server authority</name>
  <reversibility rating="costly">D-25 and D-31 establish adapter seams used by later native clients and packaged credential evidence.</reversibility>
  <files>apps/desktop/main/adapters/sync.ts, apps/desktop/main/adapters/credentials.ts, apps/desktop/main/application/DesktopApplication.ts, apps/desktop/test/e2e/real-stack-sync.spec.ts</files>
  <read_first>packages/contracts/openapi/keepling.yaml, apps/web/src/api/keepling.ts, apps/desktop/main/application/ports.ts, apps/desktop/main/application/DesktopApplication.ts, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-05-SUMMARY.md, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-11-SUMMARY.md</read_first>
  <behavior>
    - D-31 adapter invokes compatibility, native authorization/refresh, bootstrap, ordered feed, command delivery, and mutation lookup operations from generated contracts; canonical acceptance/rebase/conflict remains server-owned.
    - D-25 credentials cross only the async `CredentialPort`, use packaged `safeStorage`, rotate/revoke through server contracts, and never enter renderer state or diagnostics.
    - D-23 logout fences the active namespace before best-effort revocation and retains its durable intent.
  </behavior>
  <action>Create explicit wire-to-storage-neutral mappers and a bounded HTTPS transport that accepts only server-derived grant namespace data. Persist credential ciphertext only through `SafeStorageCredentialAdapter`; surface the unsigned continuity limitation in Settings data rather than claiming stable Keychain identity. The real-stack E2E uses a disposable server/account/grant, exercises capture, response loss, lookup, duplicate replay, remote conflict, auth revocation, and reconnect, and asserts the same server acknowledgement/conflict contracts used by web/API.</action>
  <acceptance_criteria>The Electron adapter passes generated contract drift, compatibility negotiation, native grant rotation/revocation, exact receipt replay, and structured conflict proof against Phoenix/PostgreSQL.</acceptance_criteria>
  <verify>
    <automated>pnpm contracts:check &amp;&amp; ./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/sync_controller_test.exs' &amp;&amp; pnpm test:desktop:e2e -- real-stack</automated>
    <fails_when>non-zero exit, generated-contract drift appears, "0 tests" appears, desktop reconstructs namespace authority from request input, or duplicate delivery creates a second semantic effect</fails_when>
  </verify>
  <done>SRV-02 has executable Electron-adapter proof through the released semantic server boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish one closed synchronization and recovery projection to every renderer surface</name>
  <files>apps/desktop/main/application/presentation.ts, apps/desktop/main/application/DesktopApplication.ts, apps/desktop/preload/contracts.ts, apps/desktop/test/application/recovery-presentation.test.ts</files>
  <read_first>.planning/phases/KPL-03-mac-daily-loop/03-UI-SPEC.md, apps/web/src/commands/submission.ts, apps/web/src/features/recovery/RecoveryStrip.tsx, packages/contracts/vectors/sync.json, apps/desktop/preload/contracts.ts</read_first>
  <behavior>
    - D-15/D-16 one projection owns opening, preparing, delayed updating, offline, retryable, local-saved, uncertain, rejection, conflict, authentication, namespace mismatch, local-save failure, and store-unavailable states.
    - D-17 exact user language and actions are emitted with bounded counts/coarse times; routine success is quiet and no state says Everything synced.
    - D-19 no OS notification path exists for routine synchronization.
  </behavior>
  <action>Define a strict discriminated union for the exact UI-SPEC states and recovery action codes. Derive it only from main-owned store/scheduler/auth outcomes, add a three-second injected grace period, and publish monotonic sequence-tagged snapshots through the existing preload contract. Keep diagnostic payloads to D-44 allowlisted codes, bounded counts, coarse timing, artifact/schema versions, and process roles; negative tests include task text, drafts, commands, credentials, tokens, cursors, fingerprints, raw IDs, URLs, paths, and titles.</action>
  <acceptance_criteria>Every state/copy/action row has a deterministic fixture, healthy state is quiet, and all primary surfaces receive byte-equivalent summaries for the same main-owned snapshot.</acceptance_criteria>
  <verify>
    <automated>pnpm test:desktop -- recovery-presentation &amp;&amp; pnpm test:desktop -- privacy</automated>
    <fails_when>non-zero exit, "0 tests" appears, any exact state row is absent, shell/row/panel outputs disagree, or forbidden diagnostic content is detected</fails_when>
  </verify>
  <done>Jon can inspect every actionable sync state without logs or misleading completeness claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Server response → desktop namespace | Untrusted network data must not authorize or settle another namespace/mutation. |
| Credential adapter → application | Secret material must remain outside renderer and diagnostics. |
| Main projection → renderer | Closed human state must not leak transport/storage internals or infer false truth. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-02-01 | Spoofing | native grant namespace | high | mitigate | Accept only server-derived five-part authority; fence before revoke/switch. |
| T-KPL03-02-02 | Tampering | acknowledgement settlement | high | mitigate | Identity plus fingerprint verification in one atomic worker transaction. |
| T-KPL03-02-03 | Information disclosure | credentials/diagnostics | high | mitigate | Async safeStorage port and allowlisted negative privacy tests. |
| T-KPL03-02-04 | Denial of service | scheduler/feed | medium | mitigate | Pull 50/push 25 bounds, capped backoff/jitter, finite passes. |
</threat_model>

<verification>Run vector, real-stack, presentation, privacy, and contract commands; all must execute positive case counts.</verification>
<success_criteria>Offline intent reconciles exactly and every exception remains calmly inspectable through one authoritative projection.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-02-SUMMARY.md` when done.</output>
