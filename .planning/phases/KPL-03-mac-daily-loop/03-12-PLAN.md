---
phase: KPL-03-mac-daily-loop
plan: 12
type: execute
wave: 2
depends_on: [03-07]
files_modified:
  - apps/desktop/vitest.config.ts
  - apps/desktop/playwright.config.ts
  - tooling/package-desktop.mjs
  - tooling/smoke-desktop-packaged.mjs
  - tooling/verify-desktop-harness.mjs
autonomous: true
requirements: [MAC-01, MAC-02, MAC-03, MAC-04, MAC-05, QUAL-03, QUAL-04, SRV-02]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Every desktop test lane resolves to named non-watch discovery and refuses zero-case success before production work invokes it."
    - "Renderer, Electron, and packaged tests always use disposable user-data/profile directories and cannot reach Jon's real Keepling data."
    - "Package-once records exact inputs and executable identity; smoke consumes only an already-built copied application outside source and cannot rebuild."
  artifacts:
    - {path: apps/desktop/vitest.config.ts, provides: "Desktop pure/component/worker/IPC discovery"}
    - {path: apps/desktop/playwright.config.ts, provides: "Isolated Electron E2E and packaged projects"}
    - {path: tooling/package-desktop.mjs, provides: "Package-once manifest contract"}
    - {path: tooling/smoke-desktop-packaged.mjs, provides: "External exact-executable smoke contract"}
    - {path: tooling/verify-desktop-harness.mjs, provides: "Dependency-free Wave-0 completeness gate"}
  key_links:
    - {from: apps/desktop/playwright.config.ts, to: apps/desktop/forge.config.ts, via: "manifest-selected executable with disposable profile"}
    - {from: tooling/package-desktop.mjs, to: tooling/smoke-desktop-packaged.mjs, via: "immutable manifest; smoke has no build path"}
  prohibitions:
    - {statement: "MUST NOT use a development renderer, source-tree executable, watch mode, zero-test success, or Jon's real profile as desktop proof."}
---

<objective>
Complete Wave 0 with the isolated test, package-once, and exact external-smoke harness required by D-45 through D-47.

Purpose: Make Plan 03-01's provenance checkpoint and packaged tracer executable without overloading the build-foundation plan.
Output: Vitest/Playwright discovery, profile isolation, package manifest, smoke consumer, and an anti-vacuous static verifier.
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-PATTERNS.md
@.planning/phases/KPL-03-mac-daily-loop/03-VALIDATION.md
@apps/web/vitest.config.ts
@apps/web/playwright.config.ts
</context>

## Artifacts this phase produces

- Configs: named Vitest projects and isolated Electron/packaged Playwright projects.
- Harnesses: package-once manifest writer, external exact-executable smoke consumer, and static completeness verifier.
- Files: every path in `files_modified`.

<tasks>

<task type="auto">
  <name>Task 1: Create the dependency-free Wave-0 completeness verifier</name>
  <files>tooling/verify-desktop-harness.mjs</files>
  <read_first>.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md, .planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md, tooling/test-phase-2.sh, apps/desktop/forge.config.ts, apps/desktop/tsconfig.json</read_first>
  <action>Create a dependency-free static verifier for D-45 through D-47. It must inspect required root scripts, process configs/entries, test discovery, disposable-profile guards, package-manifest fields, external-executable enforcement, no-build smoke behavior, watch-mode absence, and positive-case-count requirements. Support focused `--build-config` and `--test-config` checks plus `--all`, with missing not-yet-created harness inputs reported as failures rather than skipped success.</action>
  <verify><automated>node --check tooling/verify-desktop-harness.mjs</automated><fails_when>the verifier has invalid syntax, silently ignores missing required inputs, or lacks focused and aggregate modes</fails_when></verify>
  <done>The checker exists before any later task relies on it.</done>
</task>

<task type="auto">
  <name>Task 2: Create isolated test discovery plus package-once and external-smoke contracts</name>
  <files>apps/desktop/vitest.config.ts, apps/desktop/playwright.config.ts, tooling/package-desktop.mjs, tooling/smoke-desktop-packaged.mjs</files>
  <read_first>apps/web/vitest.config.ts, apps/web/playwright.config.ts, apps/desktop/package.json, apps/desktop/tsconfig.json, apps/desktop/forge.config.ts, tooling/verify-desktop-harness.mjs</read_first>
  <action>Define named Vitest projects for pure/component/worker/IPC discovery and Playwright projects for Electron E2E and packaged executable cases per D-45/D-46. Every launch allocates a disposable profile under the system temporary directory and rejects the normal Keepling application-data path; configure finite serial stateful execution, failure evidence, and explicit non-watch globs. Implement D-47 package-once manifest fields for source revision, input digest, application/ZIP digests, embedded versions, and executable path. The smoke consumer accepts only an already-built copied `.app` outside source, requires `app.isPackaged`, and contains no packaging or development-server path.</action>
  <verify><automated>node tooling/verify-desktop-harness.mjs --all</automated><fails_when>any Wave-0 contract is absent, smoke can build or launch in source, profile isolation is missing, watch mode is enabled, or a lane can pass without executed cases</fails_when></verify>
  <done>The full Wave-0 harness is statically executable before package installation and production tracer work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Test runner → user profile | E2E must never mutate real personal data. |
| Package producer → smoke consumer | Smoke must consume exact bytes without rebuilding. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-12-01 | Information disclosure | test profiles | high | mitigate | Disposable temp profiles and normal-path rejection. |
| T-KPL03-12-02 | Tampering | package/smoke | high | mitigate | Immutable manifest, external executable, and no-build smoke consumer. |
| T-KPL03-12-03 | Repudiation | test discovery | medium | mitigate | Positive case counts and static rejection of zero-case/watch configurations. |
</threat_model>

<verification>Run `node tooling/verify-desktop-harness.mjs --all`; it must name every command, config, isolation guard, and package/smoke invariant.</verification>
<success_criteria>Plan 03-01 can install approved dependencies and execute a real packaged tracer against a complete, isolated, anti-vacuous harness.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-12-SUMMARY.md` when done.</output>
