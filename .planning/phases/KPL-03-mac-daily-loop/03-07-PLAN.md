---
phase: KPL-03-mac-daily-loop
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - apps/desktop/package.json
  - apps/desktop/forge.config.ts
  - apps/desktop/tsconfig.json
  - apps/desktop/vite.main.config.ts
  - apps/desktop/vite.preload.config.ts
  - apps/desktop/vite.renderer.config.ts
  - apps/desktop/vite.worker.config.ts
  - apps/desktop/renderer/index.html
  - apps/desktop/vitest.config.ts
  - apps/desktop/playwright.config.ts
  - tooling/package-desktop.mjs
  - tooling/smoke-desktop-packaged.mjs
  - tooling/verify-desktop-harness.mjs
autonomous: true
requirements: [MAC-01, MAC-02, MAC-03, MAC-04, MAC-05, QUAL-03, QUAL-04, SRV-02]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Every locked desktop command resolves to a concrete direct Vite/TypeScript, Vitest, Playwright, package-once, or exact-smoke entry before a production task invokes it."
    - "Renderer, Electron, and packaged tests always use disposable user-data/profile directories and cannot reach Jon's real Keepling data."
    - "The package harness has a real renderer HTML entry, four explicit process build inputs, and an external-executable smoke path with no development-server fallback."
  artifacts:
    - {path: apps/desktop/tsconfig.json, provides: "Grounded main/preload/renderer/worker TypeScript build inputs"}
    - {path: apps/desktop/vitest.config.ts, provides: "Desktop pure/component/worker/IPC test discovery"}
    - {path: apps/desktop/playwright.config.ts, provides: "Isolated Electron E2E and packaged projects"}
    - {path: apps/desktop/renderer/index.html, provides: "Concrete local renderer entry"}
    - {path: tooling/verify-desktop-harness.mjs, provides: "Dependency-free Wave-0 completeness gate"}
  key_links:
    - {from: package.json, to: apps/desktop/package.json, via: "locked desktop commands use pnpm --dir apps/desktop scripts"}
    - {from: apps/desktop/forge.config.ts, to: apps/desktop/vite.main.config.ts, via: "Forge consumes direct built process entries without the experimental Forge Vite plugin"}
    - {from: tooling/smoke-desktop-packaged.mjs, to: apps/desktop/playwright.config.ts, via: "manifest-selected external executable and disposable profile"}
  prohibitions:
    - {statement: "MUST NOT use a development renderer, source-tree executable, watch mode, zero-test success, or Jon's real profile as desktop proof."}
---

<objective>
Create the complete Wave-0 desktop build and test harness required by D-45 through D-47 before the packaged tracer consumes any desktop command.

Purpose: Make every later verification executable while keeping this plan limited to configuration, discovery, isolation, and package/smoke plumbing.
Output: Direct process builds, renderer HTML, grounded TypeScript inputs, Vitest/Playwright configs, locked root commands, and package-once/external-smoke entrypoints.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-03-mac-daily-loop/03-CONTEXT.md
@.planning/phases/KPL-03-mac-daily-loop/03-RESEARCH.md
@.planning/phases/KPL-03-mac-daily-loop/03-PATTERNS.md
@.planning/phases/KPL-03-mac-daily-loop/03-VALIDATION.md
</context>

## Artifacts this phase produces

- Commands: `dev:desktop`, `typecheck:desktop`, `test:desktop`, `test:desktop:ipc`, `test:desktop:e2e`, `package:desktop`, and `smoke:desktop:packaged`.
- Configs: explicit main, preload, renderer, and worker Vite entries; desktop TypeScript inputs; Vitest and Playwright projects; Forge resources.
- Harnesses: package-once manifest writer, external exact-executable smoke consumer, and static Wave-0 completeness verifier.

<tasks>

<task type="auto">
  <name>Task 1: Ground root commands, desktop package metadata, Forge inputs, and TypeScript process boundaries</name>
  <files>package.json, apps/desktop/package.json, apps/desktop/forge.config.ts, apps/desktop/tsconfig.json</files>
  <read_first>package.json, apps/web/package.json, apps/desktop/README.md, .planning/phases/KPL-03-mac-daily-loop/03-PATTERNS.md, .planning/phases/KPL-03-mac-daily-loop/03-VALIDATION.md</read_first>
  <action>Add all seven D-45 root commands using grounded `pnpm --dir apps/desktop` scripts. Declare the exact approved Electron/Forge/Zod pins without installing or changing `pnpm-lock.yaml`; Plan 03-01's blocking provenance checkpoint remains the install gate. Configure Forge ZIP packaging and explicit packaged resources for main, preload, renderer, worker, migrations, and local assets. Define TypeScript inputs and output roots for all four process roles with no browser/Electron cross-import shortcuts.</action>
  <verify><automated>node -e "const p=require('./package.json'); for (const n of ['dev:desktop','typecheck:desktop','test:desktop','test:desktop:ipc','test:desktop:e2e','package:desktop','smoke:desktop:packaged']) if (!p.scripts[n]) process.exit(1)"</automated><fails_when>any command is absent, a command is ungrounded, Forge references an undeclared entry/resource, or this task changes pnpm-lock.yaml</fails_when></verify>
  <done>Every required command and process/resource boundary is declared before dependency installation.</done>
</task>

<task type="auto">
  <name>Task 2: Create direct Vite process builds and the renderer HTML entry</name>
  <files>apps/desktop/vite.main.config.ts, apps/desktop/vite.preload.config.ts, apps/desktop/vite.renderer.config.ts, apps/desktop/vite.worker.config.ts, apps/desktop/renderer/index.html</files>
  <read_first>apps/web/vite.config.ts, apps/desktop/tsconfig.json, apps/desktop/forge.config.ts, .planning/phases/KPL-03-mac-daily-loop/03-PATTERNS.md</read_first>
  <action>Create four direct Vite configs with explicit entry/output/externalization rules and a local renderer HTML document pointing at `renderer/main.tsx`. Main, preload, and worker outputs must remain Node/Electron-appropriate; renderer output must contain no Node globals and must support restrictive packaged CSP/local assets. Keep renderer HMR confined to `dev:desktop`; package builds must never require a dev server.</action>
  <verify><automated>node tooling/verify-desktop-harness.mjs --build-config</automated><fails_when>any entry/output is unresolved, renderer HTML is absent, a packaged config embeds a dev-server URL, or process roles share an unsafe output/input</fails_when></verify>
  <done>All four process bundles and the renderer document have deterministic executable inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Create Vitest, Playwright, package-once, external-smoke, and anti-vacuous harness contracts</name>
  <files>apps/desktop/vitest.config.ts, apps/desktop/playwright.config.ts, tooling/package-desktop.mjs, tooling/smoke-desktop-packaged.mjs, tooling/verify-desktop-harness.mjs</files>
  <read_first>.planning/phases/KPL-03-mac-daily-loop/03-VALIDATION.md, .planning/phases/KPL-03-mac-daily-loop/03-PATTERNS.md, apps/web/vitest.config.ts, apps/web/playwright.config.ts, tooling/test-phase-2.sh</read_first>
  <action>Define named Vitest projects for pure/component/worker/IPC discovery and Playwright projects for Electron E2E and packaged executable cases. Every launch must allocate a disposable profile under the system temporary directory and reject the normal Keepling application-data path. Implement package-once manifest fields for source/input/artifact digests and executable path, plus a smoke consumer that accepts only an already-built copied `.app` outside source. The static verifier must reject missing configs/scripts, unresolved test globs, watch mode, zero-case configuration, rebuild calls from smoke, and absent isolation guards without requiring dependencies to be installed.</action>
  <verify><automated>node tooling/verify-desktop-harness.mjs --all</automated><fails_when>any VALIDATION.md Wave-0 config/command is absent, a test can use the real profile, smoke can rebuild or launch in source, or a lane can report green with zero cases</fails_when></verify>
  <done>The complete desktop harness exists and is statically executable before Plan 03-01 installs approved dependencies and runs the packaged tracer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Build config → packaged bytes | Miswired entries/resources can substitute development behavior. |
| Test runner → user profile | E2E must never mutate real personal data. |
| Package job → smoke job | Smoke must consume exact bytes without rebuilding. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL03-07-01 | Tampering | build entries | high | mitigate | Explicit process configs, resource manifest, static completeness gate. |
| T-KPL03-07-02 | Information disclosure | test profiles | high | mitigate | Disposable temp profiles and real-path rejection. |
| T-KPL03-07-03 | Repudiation | package/smoke | high | mitigate | Package-once manifest and no-build smoke consumer. |
| T-KPL03-07-SC | Tampering | npm dependencies | high | mitigate | Manifests only in Wave 0; Plan 03-01 human provenance gate precedes lockfile/install. |
</threat_model>

<verification>Run `node tooling/verify-desktop-harness.mjs --all`; it must name every config, command, isolation guard, and package/smoke invariant.</verification>
<success_criteria>Plan 03-01 can execute every desktop verification command against a complete harness, with dependency installation still blocked on explicit provenance approval.</success_criteria>
<output>Create `.planning/phases/KPL-03-mac-daily-loop/03-07-SUMMARY.md` when done.</output>
