---
phase: KPL-06-portability-and-trust-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/phases/KPL-05-safe-agent-access/05-VERIFICATION.md
autonomous: true
requirements: [QUAL-02, QUAL-03, QUAL-04, QUAL-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md QUAL-02 is unchecked, and its disclosure states the current fact: CI now executes and fails, so the lane has never produced a passing run to cite."
    - "No open row in .planning/WINDOWS.md has an empty, missing, or literal 'Unowned' owner field; every open row names a phase number or BACKLOG."
    - "The O-22 keyboard-navigation data-loss defect has its own WINDOWS.md row with an owner, so no data-loss-class defect is untracked."
    - "ROADMAP.md Phase 6 success criterion 3 carries a dated narrowing recording that the hermetic host-replacement rehearsal is the gate and the credentialed live cutover stays a non-passing protected outer lane."
    - "ROADMAP.md Phase 6 success criterion 5 carries the dated D-53 replacement wording, so the criterion names a measurement rather than an assumption."
    - "REQUIREMENTS.md's traceability table has one row per requirement ID, so no tool can check a grouped row against its own stated precondition."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/WINDOWS.md
  key_links:
    - "WINDOWS.md row owners <- read by the Phase 6 verifier; an empty owner must make verification fail (D-35)."
    - "REQUIREMENTS.md per-ID traceability rows <- read by `phase complete`; grouped rows are what let SRV-02 be checked against its own precondition twice."
  prohibitions:
    - "A correction in this plan must never be executed in the direction that flatters the project: a claim may be weakened or unchecked, but no claim may be strengthened, re-checked, or its disclosure softened by this plan."
---

<objective>
Correct the record before any new Phase 6 work runs. 06-CONTEXT.md D-41 and D-54(1) make this the
first act of the phase: the project's own ledger currently states things in the project's favour
that source contradicts, and every later plan in this phase would otherwise build evidence on top
of a false baseline.

This plan deliberately contains **no feature work and no tracer**. The phase tracer is 06-02, which
depends on this plan. Per D-54(1) the corrections must land before any new plan runs, so this plan
precedes the tracer by explicit instruction in the phase context, not by omission.

Purpose: make the baseline honest so the Phase 6 release claim can be checked against it.
Output: corrected REQUIREMENTS.md, ROADMAP.md, WINDOWS.md, STATE.md, and 05-VERIFICATION.md.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/KPL-06-portability-and-trust-release/06-CONTEXT.md
</context>

## Artifacts this phase produces

This plan creates or renames no code symbols. It edits planning documents only. The new named
things it introduces are: a `WINDOWS.md` row for **O-22**; the dated amendment headings **"SC3
narrowing (2026-09-11)"** and **"SC5 replacement (2026-09-11)"** in `ROADMAP.md`; and one
traceability table row per requirement ID in `REQUIREMENTS.md`.

<tasks>

<task type="auto">
  <name>Task 1: Uncheck QUAL-02 and reject the invalid Phase 5 disposition</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/KPL-05-safe-agent-access/05-VERIFICATION.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (lines 55-80 and the traceability table around line 118-126)
    - `.planning/phases/KPL-06-portability-and-trust-release/06-CONTEXT.md` (D-34, D-35, D-41)
    - `.planning/phases/KPL-05-safe-agent-access/05-VERIFICATION.md` (the disposition at line 49)
  </read_first>
  <action>
    Apply D-41 corrections 1 and 2.

    (1) In `.planning/REQUIREMENTS.md`, change QUAL-02's checkbox from `[x]` to `[ ]` and append a
    dated correction to its existing HTML-comment disclosure. The existing disclosure asserts the
    repository has no git remote and therefore no workflow has ever executed. That is now false in
    the project's favour: CI executes and fails 17 of 17 jobs across two workflows. The appended
    correction must state the date, state that CI now runs and fails, name Phase 6 plan 06-02 as the
    owner of the repair, and state plainly that QUAL-02 may not be cited as evidence until a lane
    inventory produces a passing run with positive case counts.

    (2) In `.planning/phases/KPL-05-safe-agent-access/05-VERIFICATION.md`, at the SRV-02 disposition
    around line 49, replace the owner value with `Phase 6` and append a dated note recording that a
    dispositionless owner is an invalid value under D-35 and that this correction is what makes the
    Phase 5 record legal rather than re-opening Phase 5.

    Do not touch QUAL-03, QUAL-04, or QUAL-05 checkboxes in this task — their final verification is
    assigned to 06-13, and this plan's prohibition forbids strengthening any claim.
  </action>
  <verify>
    <automated>grep -n '^- \[ \] \*\*QUAL-02\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>no matching line (QUAL-02 still carries `[x]`), or grep exits non-zero</fails_when>
    <automated>grep -c 'Unowned' .planning/phases/KPL-05-safe-agent-access/05-VERIFICATION.md || true</automated>
    <fails_when>a count greater than zero that is not inside the new dated note explaining why the value is illegal; a bare surviving disposition field holding that value is the failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a line matching `^- \[ \] \*\*QUAL-02\*\*`.
    - QUAL-02's disclosure comment contains the substring `2026-09-11` and the substring `06-02`.
    - `05-VERIFICATION.md`'s SRV-02 disposition names `Phase 6` as the owner.
    - `git diff --stat .planning/REQUIREMENTS.md` shows QUAL-03, QUAL-04 and QUAL-05 checkbox
      characters unchanged.
  </acceptance_criteria>
  <done>QUAL-02 is unchecked with a dated, factually-current disclosure, and the Phase 5 SRV-02 disposition names Phase 6 as owner.</done>
</task>

<task type="auto">
  <name>Task 2: Make the open-item ledger complete and owned</name>
  <files>.planning/WINDOWS.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/WINDOWS.md` (the whole file — every open row, and rows 43, 57, 58, 59, 60, 61, 63, 68, 69, 71, 73, 74, 75, 76 specifically)
    - `.planning/STATE.md` (the Blockers section, lines ~360-375)
    - `apps/desktop/main/application/DesktopApplication.ts` lines 460-520 (the O-45 closure that makes row 59 stale)
    - `apps/desktop/main/index.ts` lines 900-925 (the wiring that makes O-21 stale)
    - `apps/desktop/renderer/DesktopShell.tsx` lines 40-70 (the O-22 defect being filed)
  </read_first>
  <action>
    Apply D-41 corrections 2b, 3 and 4, and enforce D-35's anti-unowned clause across the ledger.

    File a new `WINDOWS.md` row for **O-22**: keyboard command dispatch in
    `apps/desktop/renderer/DesktopShell.tsx` calls `facade.setRoute` directly for `new-task`,
    `go-inbox` and `go-today`, bypassing `attemptNavigation` in
    `packages/web-ui/src/workspace/Workspace.tsx`, so a dirty editor's unsaved edits are discarded
    without the guard dialog. Class: data-loss. Owner: `Phase 6`. Reference plan `06-09`.

    Set the owner of row 69 (the SRV-02 cross-adapter legs) to `Phase 6`, reference plan `06-05`,
    and correct the cited driver-model path: the row cites `apps/desktop/test/packaged/real-stack-sync.spec.ts`,
    which does not exist; the file is `apps/desktop/test/real-stack/real-stack-sync.spec.ts`.

    Resolve row 59 as closed, citing `DesktopApplication.ts` lines 466-517 as the source evidence
    (O-45 and O-51 are closed in shipped code while the row still records them open).

    Delete the stale blocker prose at `.planning/STATE.md` for O-21 (closed at
    `apps/desktop/main/index.ts:911-918`) and for O-51, replacing each with a one-line dated note
    that the blocker was verified closed against source on this date.

    Then sweep every remaining OPEN row: any row whose owner field is empty, missing, or the literal
    unowned placeholder gets a real owner — a phase number or `BACKLOG`. Specifically set row 43
    (the credentialed Hetzner/DNS cutover) to owner `BACKLOG/999.2`, and row 63 (the G7
    locked-device write) to owner `BACKLOG`. Every status you carry forward must be re-verified
    against source before you keep it — 06-CONTEXT.md records six ledger rows that were stale in a
    single sitting, so a recorded status is not evidence.
  </action>
  <verify>
    <automated>awk '/^\|/ && !/^\|[- ]*\|/' .planning/WINDOWS.md | grep -iE '\|[[:space:]]*(unowned|tbd|—|-)?[[:space:]]*\|[[:space:]]*$' | grep -v '^|[[:space:]]*#' | wc -l</automated>
    <fails_when>any count other than 0; a non-zero count means at least one ledger row still ends in an empty or placeholder owner cell</fails_when>
    <automated>grep -n 'O-22' .planning/WINDOWS.md</automated>
    <fails_when>no match (the data-loss defect is still untracked), or grep exits non-zero</fails_when>
    <automated>grep -rn 'test/packaged/real-stack-sync.spec.ts' .planning/WINDOWS.md | wc -l</automated>
    <fails_when>any count other than 0; a surviving reference means the ledger still cites a path that does not exist</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/WINDOWS.md` contains a row whose text includes `O-22` and whose owner cell reads `Phase 6`.
    - Row 69's owner cell reads `Phase 6` and its cited driver path is `apps/desktop/test/real-stack/real-stack-sync.spec.ts`.
    - Row 43's owner cell reads `BACKLOG/999.2`; row 63's owner cell reads `BACKLOG`.
    - Row 59 is marked closed with a source citation naming `DesktopApplication.ts`.
    - `git diff .planning/STATE.md` removes the O-21 and O-51 blocker prose and adds a dated
      verified-closed note for each.
  </acceptance_criteria>
  <done>Every open ledger row names a real owner, O-22 is tracked, and the three verified-stale entries are corrected against source.</done>
</task>

<task type="auto">
  <name>Task 3: Amend SC3 and SC5 and split the grouped traceability rows</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 356-370 (the Phase 6 entry and its five success criteria)
    - `.planning/REQUIREMENTS.md` lines 110-130 (the traceability table, including the grouped rows)
    - `.planning/phases/KPL-06-portability-and-trust-release/06-CONTEXT.md` (D-40, D-53, D-41(5), D-41(6))
  </read_first>
  <action>
    Apply D-41 corrections 5 and 6 and the D-40/D-53 amendments.

    In `.planning/ROADMAP.md` under Phase 6, add a dated amendment block beneath the success-criteria
    list. It carries two entries.

    **SC3 narrowing (2026-09-11), per D-40:** the gate for host replacement is the hermetic rehearsal
    produced by `tooling/verify-host-replacement.sh` and `tooling/test-host-replacement-sequence.sh`.
    The credentialed live Hetzner/DNS cutover remains a protected outer lane that keeps exiting
    non-zero, so a skip is never counted as evidence; DATA-03 and OPS-02 stay unchecked, which is the
    honest outcome, and `WINDOWS.md` row 43 carries owner `BACKLOG/999.2`.

    **SC5 replacement (2026-09-11), per D-53:** replace success criterion 5's text verbatim with the
    D-53 wording — the three defect classes are measured rather than assumed, via (a) the read-only
    reconciliation oracle against the real Mac+iPhone install for at least 14 calendar days, at
    least 300 samples, at least 10 non-thin usage days, zero unexplained violations of invariants
    I1 through I8, and a published per-day usage census; (b) the same oracle as the assertion layer
    over at least 72 accumulated machine-hours across at least 200 seeds of adversarial two-client
    soak against the real stack with every chaos operator exercised at least 50 times; and (c) an
    open-defect register listing no open item classed data-loss, silent-overwrite, or
    recovery-severity. Evidence is `.artifacts/trust-soak/trust-soak-evidence.json`, digest-bound,
    stating its own detection floor and disclosed blind spot. Owner dogfood feedback stays informal,
    unstructured, non-evidentiary and non-gating.

    In `.planning/REQUIREMENTS.md`, split every grouped traceability row into one row per
    requirement ID. The rows currently grouping `SRV-04..06, DATA-02..03, OPS-01..05, QUAL-02,
    QUAL-05`, the `MAC-01, MAC-02, MAC-04, QUAL-04` row, and the `DATA-01, QUAL-03..05` row all
    expand. Each new row carries that single ID, its owning phase, and its own status verdict —
    carry forward the existing verdict text per ID and do not upgrade any status while splitting.
  </action>
  <verify>
    <automated>grep -c 'SC5 replacement (2026-09-11)' .planning/ROADMAP.md</automated>
    <fails_when>a count of 0; the dated SC5 amendment is absent</fails_when>
    <automated>grep -c 'SC3 narrowing (2026-09-11)' .planning/ROADMAP.md</automated>
    <fails_when>a count of 0; the dated SC3 amendment is absent</fails_when>
    <automated>grep -E '^\| [A-Z]+-[0-9]+' .planning/REQUIREMENTS.md | grep -cE '\.\.|,'</automated>
    <fails_when>any count other than 0; a non-zero count means a traceability row still groups more than one requirement ID</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains both dated amendment headings under Phase 6.
    - The SC5 amendment text contains the literals `14 calendar days`, `300 samples`, `10 non-thin`,
      `72 accumulated machine-hours`, `200 seeds` and `.artifacts/trust-soak/trust-soak-evidence.json`.
    - The SC3 amendment names both `tooling/verify-host-replacement.sh` and
      `tooling/test-host-replacement-sequence.sh` and states DATA-03 and OPS-02 stay unchecked.
    - No line in `.planning/REQUIREMENTS.md`'s traceability table matches a requirement-ID column
      containing a comma or a range ellipsis.
  </acceptance_criteria>
  <done>SC3 and SC5 carry dated amendments matching D-40 and D-53, and the traceability table has one row per requirement ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → future verifier | `WINDOWS.md` owner fields and `REQUIREMENTS.md` checkboxes are read by `phase complete` and by the Phase 6 verifier as authority for what may pass. |
| planning record → public release claim | `ROADMAP.md` success criteria become the wording of the public trust claim in 06-11's policy documents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Repudiation | `.planning/REQUIREMENTS.md` QUAL-02 disclosure | high | mitigate | The uncheck is accompanied by a dated disclosure naming the current CI state and the owning plan, so the record cannot later be read as an unexplained regression or quietly re-checked. |
| T-06-01-02 | Tampering | `.planning/WINDOWS.md` owner fields | high | mitigate | Task 2's automated gate counts rows with empty/placeholder owner cells and fails on any non-zero count, so an unowned row cannot be reintroduced silently. |
| T-06-01-03 | Elevation of Privilege | `REQUIREMENTS.md` grouped traceability rows | high | mitigate | Splitting to one row per ID removes the mechanism by which `phase complete` checked SRV-02 against its own stated precondition; the gate asserts zero remaining grouped rows. |
| T-06-01-04 | Information Disclosure | `ROADMAP.md` SC5 wording | medium | mitigate | The D-53 wording publishes the detection floor and the disclosed blind spot alongside the claim, so the public criterion cannot overstate what the measurement proves. |
| T-06-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan; RESEARCH.md's Package Legitimacy Audit records no `[SUS]` or `[SLOP]` packages for the phase, and no install task exists here to gate. |
</threat_model>

<verification>
Run all three task gates in sequence. Then confirm no claim was strengthened:
`git diff .planning/REQUIREMENTS.md` must contain no change turning `[ ]` into `[x]`.
</verification>

<success_criteria>
QUAL-02 is unchecked with a current disclosure; every open ledger row names an owner; O-22 is
tracked; the three stale entries are corrected against source; SC3 and SC5 carry dated amendments;
the traceability table has one row per requirement ID.
</success_criteria>

<output>
Create `.planning/phases/KPL-06-portability-and-trust-release/06-01-SUMMARY.md` when done.
</output>
