---
phase: KPL-02-synchronization-and-replaceable-server
plan: 09
type: execute
wave: 9
depends_on: [02-07, 02-08]
files_modified:
  - infra/tofu/hetzner/versions.tf
  - infra/tofu/hetzner/main.tf
  - infra/tofu/hetzner/variables.tf
  - infra/tofu/hetzner/outputs.tf
  - infra/tofu/hetzner/cloud-init.yml
  - infra/tofu/hetzner/replace_host.tftest.hcl
  - infra/tofu/hetzner/.terraform.lock.hcl
  - infra/tofu/hetzner/selection.json
  - infra/dns/cloudflare.sh
  - tooling/verify-host-replacement.sh
autonomous: false
requirements: [DATA-03, OPS-02, OPS-03]
user_setup:
  - service: hetzner
    why: "Credentialed fresh-host provisioning and replacement acceptance"
    env_vars: [{name: HCLOUD_TOKEN, source: "Hetzner Cloud project API token with least required scope"}, {name: KEEPLING_SSH_PUBLIC_KEY_FILE, source: "Readable public-key file for the exact replacement-run hcloud_ssh_key resource"}]
  - service: backup-repositories
    why: "Encrypted primary and independent-mirror recovery proof"
    env_vars: [{name: KEEPLING_BACKUP_PRIMARY_CREDENTIAL_FILE, source: "Primary repository credential file"}, {name: KEEPLING_BACKUP_MIRROR_CREDENTIAL_FILE, source: "Independent provider/account credential file"}, {name: KEEPLING_BACKUP_CIPHER_FILE, source: "Separately recoverable encryption key file"}]
  - service: dns
    why: "Controlled DNS cutover and rollback rehearsal"
    env_vars: [{name: CLOUDFLARE_API_TOKEN_FILE, source: "Cloudflare API token file scoped to DNS read/edit for the selected zone"}, {name: KEEPLING_DNS_ZONE_ID, source: "Cloudflare zone identifier"}, {name: KEEPLING_DNS_RECORD_NAME, source: "Exact rehearsal record name"}]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "OpenTofu creates the reference Hetzner host, firewall, private network, an owned SSH key, bootstrap inputs, and sensitive locked state from tracked source."
    - "A complete credentialed rehearsal provisions an empty replacement, restores and rotates epoch, proves user behavior, changes DNS, and records RPO/RTO evidence before retiring the old host."
    - "DATA-03 adjacency/empty/ordering edges: source equal to target is refused; only an empty target proceeds; DNS cutover occurs strictly after verification and rollback remains available until propagation proof."
    - "FLAGGED ASSUMPTION (OPS-02 concurrency/unclassified): state locking plus a single owned replacement run is the required concurrency contract; verifier review must confirm this interpretation against the requirement prose."
  artifacts:
    - {path: infra/tofu/hetzner/main.tf, provides: "Declarative replaceable host/network/firewall"}
    - {path: infra/dns/cloudflare.sh, provides: "Cloudflare authoritative read/update/rollback adapter"}
    - {path: tooling/verify-host-replacement.sh, provides: "Dry-run and credentialed replacement/DNS evidence"}
  key_links:
    - {from: tooling/verify-host-replacement.sh, to: infra/tofu/hetzner/main.tf, via: "plan/apply/destroy with exact tested digest and restore contract"}
    - {from: infra/tofu/hetzner/main.tf, to: tooling/verify-host-replacement.sh, via: "owned hcloud_ssh_key identity retained for exact read/delete cleanup and unowned-key refusal"}
  prohibitions:
    - {statement: "MUST NOT store canonical product data, plaintext secrets, or irreplaceable recovery material in OpenTofu state, cloud-init, provider snapshots, or a snowflake host."}
    - {statement: "MUST NOT change DNS or destroy the old host before the fresh candidate passes epoch, login, read, write, undo, and rollback proof."}
---
<objective>Provision and prove the complete replaceable-host contract per D-37, D-39, D-46, D-47, and D-48.</objective>
<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md
@infra/compose/compose.yml
@infra/backup/durable-state-manifest.yml
@tooling/verify-restore.sh
@tooling/verify-deploy.sh</context>
## Artifacts this phase produces
- Pinned OpenTofu/hcloud module and lockfile, bounded cloud-init, provider tests, and `verify-host-replacement.sh` evidence runner.
<tasks>
<task type="auto">
  <name>Task 1: Provision the complete Hetzner host boundary from pinned source</name>
  <reversibility rating="costly">The chosen provider adapter is outward and replaceable, but state/backend/resource addresses require coordinated migration.</reversibility>
  <files>infra/tofu/hetzner/versions.tf, infra/tofu/hetzner/main.tf, infra/tofu/hetzner/variables.tf, infra/tofu/hetzner/outputs.tf, infra/tofu/hetzner/cloud-init.yml</files>
  <read_first>infra/README.md, infra/compose/compose.yml, infra/backup/durable-state-manifest.yml, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md</read_first>
  <action>Pin OpenTofu 1.12.6 and `hetznercloud/hcloud` 1.68.0; declare location/server-type and SSH public-key inputs, and create an owned `hcloud_ssh_key` resource with run-ownership labels that the server references by exact resource identity. Declare the firewall exposing only required edge/admin access, private network/subnet/attachment, immutable image/digest inputs, sensitive remote-state backend contract, and bounded idempotent cloud-init that installs no canonical data. Retain only the provider resource ID/name needed for exact read/delete cleanup, mark outputs sensitive where appropriate, and never place a private key or credential in configuration, output, or cloud-init. Validate target architecture/storage inputs and avoid removed `datacenter` semantics.</action>
  <acceptance_criteria>Formatting/validation pass and provider tests prove the required host/network/firewall/bootstrap graph plus owned `hcloud_ssh_key` create/read/delete identity, with sensitive outputs and no embedded credentials, private key, or canonical state.</acceptance_criteria>
  <verify><automated>tofu -chdir=infra/tofu/hetzner fmt -check -recursive &amp;&amp; tofu -chdir=infra/tofu/hetzner init -backend=false &amp;&amp; tofu -chdir=infra/tofu/hetzner validate &amp;&amp; tofu -chdir=infra/tofu/hetzner test</automated><fails_when>non-zero exit, provider/runtime pin drifts, the owned SSH-key lifecycle/resource reference is absent, PostgreSQL is public, or credentials/private key/canonical state enter configuration</fails_when></verify>
  <done>The reference host is fully declarative and replaceable.</done>
</task>
<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Supply credentialed recovery, provider, and DNS authority</name>
  <read_first>.planning/phases/KPL-02-synchronization-and-replaceable-server/02-09-PLAN.md, .planning/phases/KPL-02-synchronization-and-replaceable-server/COVERAGE.md, infra/backup/durable-state-manifest.yml</read_first>
  <action>Provide the least-privilege Hetzner token, readable SSH public-key file, primary/mirror backup credential files, separate cipher file, Cloudflare token file, zone ID, and exact record name from `user_setup`; do not paste secret values into chat, Git, logs, or plan artifacts. The executor resumes by checking only variable/file presence and never printing credential contents.</action>
  <instructions>Export `HCLOUD_TOKEN`, `KEEPLING_SSH_PUBLIC_KEY_FILE`, `KEEPLING_DNS_ZONE_ID`, and `KEEPLING_DNS_RECORD_NAME`; point the SSH variable to a readable public-key file and every credential/cipher variable, including `CLOUDFLARE_API_TOKEN_FILE`, to an already-created readable secret file outside the repository. Reply `credentials ready` without sharing credential contents.</instructions>
  <acceptance_criteria>All named authority is available only through environment/file references and remains absent from tracked files and command output.</acceptance_criteria>
  <verify><automated>test -n "${HCLOUD_TOKEN:-}" &amp;&amp; test -r "${KEEPLING_SSH_PUBLIC_KEY_FILE:-}" &amp;&amp; test -r "${KEEPLING_BACKUP_PRIMARY_CREDENTIAL_FILE:-}" &amp;&amp; test -r "${KEEPLING_BACKUP_MIRROR_CREDENTIAL_FILE:-}" &amp;&amp; test -r "${KEEPLING_BACKUP_CIPHER_FILE:-}" &amp;&amp; test -r "${CLOUDFLARE_API_TOKEN_FILE:-}" &amp;&amp; test -n "${KEEPLING_DNS_ZONE_ID:-}" &amp;&amp; test -n "${KEEPLING_DNS_RECORD_NAME:-}"</automated><fails_when>authority or the public-key input is missing, or a secret is committed/printed</fails_when></verify>
  <verification>The executor runs the same read-only presence/readability checks and `git grep` checks for credential values without echoing secrets.</verification>
  <resume-signal>Reply `credentials ready` after the variables reference readable secret files.</resume-signal>
  <done>Credentialed acceptance can run without fabricating external proof.</done>
</task>
<task type="auto">
  <name>Task 3: Replace the host, restore, verify, rehearse DNS, and retain rollback</name>
  <precondition>HCLOUD_TOKEN, KEEPLING_SSH_PUBLIC_KEY_FILE, and all credential files from user_setup are present and readable</precondition>
  <files>infra/tofu/hetzner/replace_host.tftest.hcl, infra/tofu/hetzner/.terraform.lock.hcl, infra/tofu/hetzner/selection.json, infra/dns/cloudflare.sh, tooling/verify-host-replacement.sh</files>
  <read_first>infra/tofu/hetzner/main.tf, infra/tofu/hetzner/outputs.tf, tooling/verify-restore.sh, tooling/verify-deploy.sh, .planning/phases/KPL-02-synchronization-and-replaceable-server/COVERAGE.md</read_first>
  <action>Create dry-run provider tests covering the owned `hcloud_ssh_key` resource, a Cloudflare DNS v4 outward adapter, and a credentialed runner. First query the current Hetzner catalog and benchmark representative storage/restore throughput for available `nbg1` x86 candidates, write the non-secret chosen location/type and evidence to `infra/tofu/hetzner/selection.json`, and refuse apply unless the selection meets the four-hour objective. Then create the labeled SSH key from the supplied public key, retain its exact provider identity in the private run workspace, read it back, attach only that key to a fresh empty VM, deploy the exact tested digest, restore from primary and independent mirror, rotate epoch, and prove readiness plus recovery/login/read/write/undo. Before DNS mutation, call `GET /zones/{zone_id}/dns_records?type=A&amp;name={record_name}`, require exactly one result, and retain its record ID, content, proxied state, and TTL in the private run workspace. Stage the candidate IPv4 with `PUT /zones/{zone_id}/dns_records/{record_id}`, re-read the Cloudflare record, verify authoritative nameserver and recursive resolver propagation, exercise rollback with the same update endpoint using the retained original fields, re-read, and verify rollback propagation. Keep the old host until cutover proof is recorded. During cleanup, re-read the SSH key by retained identity, require the run-ownership labels to match, delete exactly that owned key, prove it is absent, and refuse deletion when a fixture key is unowned or its identity/labels differ. Redact API bodies/tokens, refuse ambiguous or multiple record matches and equal/live source-target pairs, and destroy only explicitly owned test resources.</action>
  <acceptance_criteria>Dry-run is deterministic; credentialed evidence proves owned SSH-key create/read/delete and refusal to delete an unowned key, names only bounded digests/times/results, meets the five-minute/four-hour objectives, and proves no snowflake repair or hidden durable location.</acceptance_criteria>
  <verify><automated>tofu -chdir=infra/tofu/hetzner test &amp;&amp; ./tooling/verify-host-replacement.sh --dry-run &amp;&amp; ./infra/dns/cloudflare.sh self-test &amp;&amp; ./tooling/verify-host-replacement.sh --credentialed</automated><fails_when>non-zero exit, SSH-key create/read/delete proof or unowned-key refusal is absent, target is not empty, authoritative record read/original TTL retention/staged update/propagation/rollback is omitted, objectives are unmet/unmeasured, or owned resources leak</fails_when></verify>
  <done>DATA-03, OPS-02, and live OPS-03 acceptance have complete source-driven evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| OpenTofu/provider credentials → Hetzner | State and API operations are sensitive and externally authoritative. |
| Verified candidate → DNS | Cutover can redirect all production traffic. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-09-01 | Tampering | provider/state inputs | high | mitigate | provider lock checksums, remote encrypted locked state, immutable image inputs. |
| T-KPL02-09-02 | Elevation of privilege | cloud credentials | high | mitigate | least privilege, file/env injection, no outputs/logging. |
| T-KPL02-09-03 | Denial of service | DNS cutover | high | mitigate | candidate proof first, propagation check, retained rollback host. |
| T-KPL02-09-04 | Tampering | SSH-key cleanup | high | mitigate | retained exact provider identity, run-ownership labels, read-before-delete, and refusal for unowned keys. |
| T-KPL02-09-SC | Tampering | hcloud provider | high | mitigate | official signed 1.68.0 pin and cross-platform lock checksums. |
</threat_model>
<verification>Run local tofu tests, then the blocking credentialed replacement rehearsal.</verification>
<success_criteria>DATA-03/OPS-02 prove a fresh host can replace the old one from tracked source and recovery material.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-09-SUMMARY.md` when done.</output>
