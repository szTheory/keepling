---
phase: KPL-02-synchronization-and-replaceable-server
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - infra/images/server/Dockerfile
  - infra/images/server/image-metadata.schema.json
  - infra/compose/compose.yml
  - infra/caddy/Caddyfile
  - tooling/verify-image.sh
  - tooling/verify-compose.sh
  - tooling/verify-deploy.sh
  - tooling/run-local-stack.sh
autonomous: true
requirements: [OPS-01, OPS-03]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "A digest-pinned Caddy/Phoenix/PostgreSQL topology starts locally with private PostgreSQL, explicit health, graceful stop, and only named host-recoverable durable state."
    - "Deploy promotes the exact tested digest, runs compatible migrations, waits for readiness, then proves login/read/write/undo through honest short interruption."
    - "FLAGGED ASSUMPTION (OPS-01/OPS-03 unclassified): exact OCI base-image digests are selected only after the packaged multi-architecture test records them."
  artifacts:
    - {path: infra/compose/compose.yml, provides: "Reference single-host topology"}
    - {path: tooling/verify-deploy.sh, provides: "Digest-to-user-smoke deploy proof"}
  key_links:
    - {from: infra/compose/compose.yml, to: apps/server/lib/keepling_web/controllers/health_controller.ex, via: "liveness/readiness health checks"}
  prohibitions:
    - {statement: "MUST NOT expose PostgreSQL publicly or place irreplaceable state in app containers, image layers, or build directories."}
    - {statement: "MUST NOT deploy mutable tags, promise zero downtime, or roll back an image whose declared schema/protocol range rejects the migrated database."}
---
<objective>Package and deploy the replaceable single-host topology per D-44, D-45, and D-47, with the infrastructure evidence portion of D-48.</objective>
<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md
@tooling/run-local-stack.sh
@tooling/runtime-preflight.sh
@infra/README.md</context>
## Artifacts this phase produces
- `infra/images/server/Dockerfile`, `image-metadata.schema.json`, `infra/compose/compose.yml`, `infra/caddy/Caddyfile`, and image/Compose/deploy verifiers.
<tasks>
<task type="auto">
  <name>Task 1: Build the tested Phoenix release image and run one health-to-command path</name>
  <files>infra/images/server/Dockerfile, infra/images/server/image-metadata.schema.json, tooling/verify-image.sh, tooling/run-local-stack.sh</files>
  <read_first>apps/server/mix.exs, apps/server/config/runtime.exs, apps/server/lib/keepling/release.ex, tooling/run-local-stack.sh, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md</read_first>
  <action>Build a multi-stage Phoenix release with exact repository runtimes, non-root runtime, no source-time Mix dependency, OCI labels for revision/digest/protocol/schema ranges, and a production check that test-only controls are absent. Capture the immutable manifest digest and execute liveness, readiness, and one `keepling ops status --json` path from the packaged artifact.</action>
  <acceptance_criteria>The same digest that passes image inspection starts the release, becomes ready against disposable PostgreSQL, and returns bounded status.</acceptance_criteria>
  <verify><automated>./tooling/verify-image.sh</automated><fails_when>non-zero exit, mutable/tag-only identity is accepted, the image runs as root, or packaged health/status fails</fails_when></verify>
  <done>The exact server artifact is packaged, inspectable, and runnable.</done>
</task>
<task type="auto">
  <name>Task 2: Compose Caddy, app, and private PostgreSQL with explicit ownership</name>
  <files>infra/compose/compose.yml, infra/caddy/Caddyfile, tooling/verify-compose.sh, tooling/run-local-stack.sh</files>
  <read_first>infra/README.md, tooling/run-local-stack.sh, apps/server/lib/keepling_web/controllers/health_controller.ex, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md</read_first>
  <action>Define digest inputs, named PostgreSQL volume, internal database network, Caddy-only public edge, health-based dependencies, bounded stop grace, read-only configs, and explicit secrets/files supplied from outside containers. Extend the local stack wrapper and tests for readiness, unavailable DB/app, graceful shutdown, volume survival, no published 5432, and stable edge `503` plus `Retry-After` during app replacement.</action>
  <acceptance_criteria>Local Compose reaches semantic readiness, survives app recreation with database state intact, keeps Caddy available, and has no hidden durable path.</acceptance_criteria>
  <verify><automated>docker compose -f infra/compose/compose.yml config &amp;&amp; ./tooling/verify-compose.sh</automated><fails_when>non-zero exit, port 5432 is published, state disappears on app recreation, or health/503 behavior is wrong</fails_when></verify>
  <done>The supported topology is source-owned and locally recoverable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Deploy exact digest through migration, readiness, smoke, and rollback eligibility</name>
  <reversibility rating="costly">Deployment and rollback metadata are release evidence consumed by host replacement and compatibility gates.</reversibility>
  <files>tooling/verify-deploy.sh, infra/compose/compose.yml, infra/caddy/Caddyfile, infra/images/server/image-metadata.schema.json</files>
  <read_first>tooling/keepling-ops, tooling/test-compatibility.sh, infra/compose/compose.yml, apps/server/lib/keepling/release.ex</read_first>
  <behavior>
    - D-44 expands/migrates, recreates only app, waits for readiness, and proves login/read/write/undo while Caddy returns bounded retry guidance during interruption.
    - D-45 rollback accepts only a previous tested digest whose schema/protocol range includes the migrated target; otherwise returns forward-fix guidance.
  </behavior>
  <action>Create local black-box deploy tests for exact digest promotion, explicit migration, dependency outage, interrupted request exact-retry behavior, user-level smoke, and compatible/incompatible rollback candidates. Never rebuild on target or use restore as routine rollback.</action>
  <acceptance_criteria>The local deployment rehearses the entire digest→migration→readiness→smoke path and rejects known incompatible rollback metadata.</acceptance_criteria>
  <verify><automated>./tooling/verify-deploy.sh --local</automated><fails_when>non-zero exit, a mutable input deploys, smoke omits undo, or an incompatible rollback is permitted</fails_when></verify>
  <done>OPS-01 and OPS-03 are proven on the packaged topology.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| OCI registry/input → deployment | Artifact identity and metadata can be substituted. |
| Public edge → private app/database | Only intended HTTP exposure is allowed. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-07-01 | Tampering | OCI promotion | high | mitigate | immutable manifest digest and tested metadata schema. |
| T-KPL02-07-02 | Information disclosure | Compose network/secrets | high | mitigate | private DB network, external secret files, no image-layer credentials. |
| T-KPL02-07-03 | Denial of service | app replacement | medium | mitigate | Caddy-held edge, stable 503/Retry-After, graceful stop. |
| T-KPL02-07-SC | Tampering | OCI dependencies | high | mitigate | exact packaged-tested image digests recorded after multi-arch proof. |
</threat_model>
<verification>Run image, Compose, and local deploy verifiers.</verification>
<success_criteria>OPS-01/OPS-03 are executable from tracked source with immutable evidence.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-07-SUMMARY.md` when done.</output>
