---
phase: KPL-02-synchronization-and-replaceable-server
plan: 05
type: execute
wave: 5
depends_on: [02-02, 02-03, 02-04, 02-11]
files_modified:
  - apps/server/lib/keepling_web/controllers/sync_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/lib/keepling_web/telemetry.ex
  - apps/server/test/keepling_web/sync_controller_test.exs
  - apps/server/test/keepling/telemetry_redaction_test.exs
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/generated/keepling.ts
  - packages/contracts/vectors/redaction.json
autonomous: true
requirements: [SRV-04, SRV-05, QUAL-05]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Authenticated sync pull/bootstrap expose bounded pages and stable reset/conflict problems without bypassing the application port."
    - "Semantic mutation, synchronization, compatibility, and recovery states are closed and distinguish local durability from server acceptance."
    - "Hostile content, credentials, tokens, cursors, and arbitrary identifiers are absent from sync logs, metrics, traces, errors, and generated diagnostic fixtures."
  artifacts:
    - {path: apps/server/lib/keepling_web/controllers/sync_controller.ex, provides: "Thin authenticated feed/bootstrap HTTP adapter"}
    - {path: packages/contracts/vectors/redaction.json, provides: "Cross-surface privacy sentinels"}
  key_links:
    - {from: apps/server/lib/keepling_web/controllers/sync_controller.ex, to: apps/server/lib/keepling/application/sync.ex, via: "closed application results"}
    - {from: apps/server/lib/keepling/accounts/device_grant.ex, to: apps/server/lib/keepling_web/controllers/sync_controller.ex, via: "authenticated issuer/origin/server/subject/generation context"}
  prohibitions:
    - {statement: "MUST NOT claim Everything synced from one successful lane or expose backend identifiers and restore plumbing in ordinary product copy."}
    - {statement: "MUST NOT emit raw task titles, notes, prompts, credentials, tokens, cursors, mutation/activity/task IDs, or provider bodies in diagnostics."}
---
<objective>Expose the server synchronization contract and privacy-safe trust states per D-15, D-49, D-50, D-51, D-52, D-53, and D-54.</objective>
<execution_context>@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md
@apps/server/lib/keepling_web/controllers/task_view_controller.ex
@apps/server/lib/keepling_web/router.ex
@apps/server/lib/keepling_web/telemetry.ex</context>
## Artifacts this phase produces
- `SyncController.pull/2` and `bootstrap/2`, authenticated routes, closed OpenAPI sync/state schemas, and `redaction.json`.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route bounded feed and bootstrap through the authenticated application seam</name>
  <files>apps/server/lib/keepling_web/controllers/sync_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/sync_controller_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts</files>
  <read_first>apps/server/lib/keepling_web/controllers/task_view_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/lib/keepling/accounts/device_grant.ex, apps/server/lib/keepling/application/sync.ex, packages/contracts/openapi/keepling.yaml</read_first>
  <behavior>
    - Pull/bootstrap derive issuer/origin, stable server instance, account subject, and synchronization generation from the authenticated DeviceGrant; client namespace claims never authorize.
    - Every cursor-reset reason and structured conflict maps to a stable versioned Problem Details code/recovery action.
    - D-15/D-16 accepted, duplicate, rejection, reuse, stale, conflict/resolution, acknowledgement/feed reorder, loss, bootstrap, tombstone, restore epoch, revocation/replay fencing, late acknowledgement, and account/server switching vectors remain represented.
  </behavior>
  <action>Add thin authenticated GET sync-page and bootstrap routes that construct sync authorization exclusively from `Keepling.Accounts.DeviceGrant` and pass that namespace to `Keepling.Application.Sync`; validate only closed query/header inputs and map closed results. Extend OpenAPI and generated TypeScript for feed envelopes, tombstones, reset/bootstrap, coverage cursor, and semantic state enums without changing the existing versioned command-route meaning. Add adversarial end-to-end controller cases proving refresh replay/revocation increments fence old cursors, late acknowledgements cannot settle fenced intent, same-account reauthentication resumes exact state, and account/server switching cannot read or drain the prior namespace.</action>
  <acceptance_criteria>Authenticated current-train clients can pull and bootstrap; cross-account/tampered/unsupported inputs receive exact non-empty recovery problems.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/sync_controller_test.exs' &amp;&amp; pnpm contracts:check</automated><fails_when>non-zero exit, a route bypasses authentication/application ports, or invalid cursor returns empty success</fails_when></verify>
  <done>The wire contract exposes the proven sync semantics without persistence leakage.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Lock semantic trust states and scan every diagnostic surface</name>
  <reversibility rating="costly">D-49 state names become shared React/Electron/Swift contract vocabulary.</reversibility>
  <files>apps/server/lib/keepling_web/telemetry.ex, apps/server/test/keepling/telemetry_redaction_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts, packages/contracts/vectors/redaction.json</files>
  <read_first>apps/server/lib/keepling_web/telemetry.ex, apps/server/test/keepling/telemetry_redaction_test.exs, packages/contracts/openapi/keepling.yaml, docs/brand/BRAND-SEED.md</read_first>
  <behavior>
    - D-49 exact mutation/sync/compatibility/recovery enum sets are generated and unknown values fail closed.
    - D-50/D-51 state facts identify durable location, consequence, and next action while routine success remains quiet.
    - D-52/D-53/D-54 vectors encode non-color cues, keyboard/focus/announcement requirements, sanitized technical details, and plain non-blaming copy without a platform UI.
  </behavior>
  <action>Extend closed schema/vector definitions and the allow-listed telemetry assertions with hostile title/note/prompt/token/cursor/raw-ID/provider-error sentinels across success and exception paths. Require exact metadata key sets and bounded cardinality. Keep final platform layout/copy refinement out of this phase while preserving semantic accessibility requirements.</action>
  <acceptance_criteria>Generated contracts contain the exact locked states, unknown states fail, and no hostile sentinel occurs in serialized logs/metrics/traces/errors/vector diagnostics.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/telemetry_redaction_test.exs' &amp;&amp; pnpm contracts:check</automated><fails_when>non-zero exit, an enum drifts, a sentinel appears, or diagnostic metadata becomes unbounded</fails_when></verify>
  <done>Phase 2 trust language and privacy invariants are cross-runtime executable contracts.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| HTTP client → sync adapter | Inputs and cursor claims are untrusted. |
| Domain/provider failures → diagnostics | Exceptions may contain private or high-cardinality values. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-05-01 | Spoofing | sync routes | high | mitigate | authenticated account context and independent cursor namespace check. |
| T-KPL02-05-02 | Information disclosure | telemetry/errors | high | mitigate | allow-listed fields and hostile artifact scans. |
| T-KPL02-05-03 | Denial of service | feed pages | medium | mitigate | strict bounded limits and stable retry/reset results. |
| T-KPL02-05-SC | Tampering | dependencies | low | accept | No install occurs. |
</threat_model>
<verification>Run sync controller, redaction, and contract drift checks.</verification>
<success_criteria>SRV-04/SRV-05 are reachable and QUAL-05 protects the full sync transport surface.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-05-SUMMARY.md` when done.</output>
