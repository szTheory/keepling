---
phase: KPL-02-synchronization-and-replaceable-server
plan: 02
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - apps/server/priv/repo/migrations/20260901000100_add_sync_feed.exs
  - apps/server/lib/keepling/adapters/postgres/command_store.ex
  - apps/server/lib/keepling/adapters/postgres/sync_feed.ex
  - apps/server/lib/keepling/application/sync.ex
  - apps/server/lib/keepling/application/sync/cursor.ex
  - apps/server/test/keepling/adapters/postgres/sync_feed_test.exs
  - apps/server/test/keepling/application/sync/cursor_test.exs
  - apps/server/test/keepling/application/sync/bootstrap_test.exs
  - apps/server/test/support/sync_scenario.ex
autonomous: true
requirements: [SRV-04, SRV-05]
estimate: {tokens: 38000, raw_tokens: 38000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Every first delivery atomically commits its terminal receipt, canonical effect or rejection, activity/conflict, and ordered account-scoped change envelopes; replay appends nothing."
    - "Opaque cursors and feed/bootstrap authorization bind the authenticated issuer/origin, stable server instance, account subject, grant synchronization generation, restore epoch, and protocol position; mismatches fail or quarantine explicitly."
    - "Bootstrap is gap-free under concurrent writes and Trash remains a canonical snapshot rather than a tombstone."
  artifacts:
    - {path: apps/server/lib/keepling/adapters/postgres/sync_feed.ex, provides: "Atomic feed persistence and reads"}
    - {path: apps/server/lib/keepling/application/sync/cursor.ex, provides: "Authenticated cursor codec"}
  key_links:
    - {from: apps/server/lib/keepling/adapters/postgres/command_store.ex, to: apps/server/lib/keepling/adapters/postgres/sync_feed.ex, via: "same Repo.transact first-delivery branch"}
    - {from: apps/server/lib/keepling/accounts/device_grant.ex, to: apps/server/lib/keepling/application/sync/cursor.ex, via: "server-derived authenticated namespace and synchronization generation"}
  prohibitions:
    - {statement: "MUST NOT use PostgreSQL sequence allocation, wall-clock time, socket order, or response order as durable feed order."}
    - {statement: "MUST NOT interpret Trash as deletion or advance feed coverage from an acknowledgement without an explicit coverage cursor."}
---

<objective>
Deliver the atomic PostgreSQL feed, authenticated cursor, and race-safe bootstrap required by D-07, D-08, D-09, D-10, D-11, D-12, D-13, and D-14 while preserving Phase 1 conflict semantics under D-15.

Purpose: Make canonical server history pullable and recoverable without sockets or silent gaps.
Output: Additive feed schema, atomic writer/read adapter, cursor codec, and bootstrap proof.
</objective>

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-PATTERNS.md
@apps/server/lib/keepling/accounts/device_grant.ex
@apps/server/lib/keepling/adapters/postgres/command_store.ex
@apps/server/priv/repo/migrations/20260830000100_create_core_task_command_tables.exs
@apps/server/test/support/concurrency_case.ex
</context>

## Artifacts this phase produces
- `Keepling.Adapters.Postgres.SyncFeed`, `Keepling.Application.Sync`, and `Keepling.Application.Sync.Cursor`.
- `sync_accounts`, `sync_changes`, and `sync_epochs` additive storage through migration `20260901000100_add_sync_feed.exs`.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Commit one ordered feed position with every terminal command outcome</name>
  <reversibility rating="costly">D-08 feed positions become durable cursor and compatibility invariants.</reversibility>
  <files>apps/server/priv/repo/migrations/20260901000100_add_sync_feed.exs, apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/lib/keepling/adapters/postgres/sync_feed.ex, apps/server/test/keepling/adapters/postgres/sync_feed_test.exs</files>
  <read_first>apps/server/lib/keepling/adapters/postgres/command_store.ex, apps/server/priv/repo/migrations/20260830000100_create_core_task_command_tables.exs, apps/server/priv/repo/migrations/20260830000800_add_persisted_conflicts.exs, apps/server/test/support/concurrency_case.ex</read_first>
  <behavior>
    - D-07 first delivery locks the account clock then sorted resources and commits terminal outcome plus all feed rows in one transaction.
    - D-08 one transaction owns one sequence and stable item ordinals; rollback exposes no position and exact replay adds no row.
    - SRV-05 persisted conflict is emitted as a feed envelope while unrelated concurrent lanes commit and remain ordered.
  </behavior>
  <action>Create additive account clock/epoch/change tables with compound account keys, unique `(account_id, sequence, ordinal)`, and explicit constraints. Extend the existing `first_delivery_or_replay` transaction before receipt finalization to acquire locks in the documented order and append stable envelopes for snapshots, persisted conflicts, collection tombstones, and undo metadata. Use two unboxed PostgreSQL backends plus a barrier to prove convergence and no deadlock-induced duplication.</action>
  <acceptance_criteria>Accepted, rejected, conflict, duplicate, rollback, and concurrent multi-envelope cases expose a total per-account order with no partially committed receipt/feed pair.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/adapters/postgres/sync_feed_test.exs'</automated>
    <fails_when>non-zero exit, duplicate replay appends a row, a receipt commits without its feed rows, or concurrent backends produce a gap/regression</fails_when>
  </verify>
  <done>Canonical command outcomes and feed envelopes share one PostgreSQL commit boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authenticate feed positions and return closed reset reasons</name>
  <files>apps/server/lib/keepling/application/sync/cursor.ex, apps/server/test/keepling/application/sync/cursor_test.exs, apps/server/lib/keepling/adapters/postgres/sync_feed.ex</files>
  <read_first>apps/server/lib/keepling/application/task_views.ex, apps/server/lib/keepling/adapters/postgres/sync_feed.ex, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md</read_first>
  <behavior>
    - D-10/D-16 cursor MAC and authorization bind issuer, origin, stable server instance, account subject, grant synchronization generation, restore sync epoch, protocol/codec train, sequence, and ordinal from Plan 02-03's authenticated device-grant namespace.
    - D-09 pulled coverage and entity revision/generation are monotonic while acknowledgements do not implicitly advance the cursor.
    - D-14 low-water/expiry returns reset-required metadata and safe bootstrap remains available; initial limits are 200 envelopes per pull, 30 days of offline grace beyond the active compatibility window, and 1,000 rows per pruning batch, with destructive pruning disabled until measurements and reset vectors pass.
  </behavior>
  <action>Consume the authenticated namespace emitted by `Keepling.Accounts.DeviceGrant`, then implement deterministic HMAC cursor encoding with injected keyring/time and safe decoding into closed results for valid, tampered, wrong issuer/origin/server/subject/generation, wrong restore epoch, unsupported codec/protocol, expired, and below-low-water. Authorize feed reads independently against the same server-derived tuple, keep cursor data opaque at the controller boundary, freeze the initial retention/page defaults, and test older entity envelopes cannot regress a newer acknowledgement snapshot.</action>
  <acceptance_criteria>No invalid cursor produces empty success; every reset condition has one stable recovery result and no account/server cross-use succeeds.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/sync/cursor_test.exs'</automated>
    <fails_when>non-zero exit, tampering/cross-account use decodes, invalid cursor becomes an empty page, or stale data regresses an entity</fails_when>
  </verify>
  <done>The durable feed position is opaque, authenticated, namespace-bound, and explicitly recoverable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bootstrap at high water without losing concurrent writes or local intent</name>
  <files>apps/server/lib/keepling/application/sync.ex, apps/server/lib/keepling/adapters/postgres/sync_feed.ex, apps/server/test/keepling/application/sync/bootstrap_test.exs, apps/server/test/support/sync_scenario.ex</files>
  <read_first>apps/server/lib/keepling/application/sync.ex, apps/server/lib/keepling/adapters/postgres/sync_feed.ex, apps/server/lib/keepling/adapters/postgres/task_views.ex, apps/server/test/support/sync_scenario.ex</read_first>
  <behavior>
    - D-11 captures high water only after D-16 authorization matches issuer/origin, server instance, account subject, and synchronization generation; it keyset-enumerates canonical entities by stable identity, then pulls strictly after high water.
    - D-12 a restored epoch mismatch forces reset/bootstrap before readiness can accept traffic.
    - D-13 trashed tasks enumerate as snapshots and only canonical collection removal emits tombstones.
  </behavior>
  <action>Add the application port and PostgreSQL adapter for bounded bootstrap pages with a distinct cursor codec, accepting only Plan 02-03's server-derived authenticated namespace. Force a mutation between pages and prove it appears in the post-high-water feed. Feed the bootstrap pages into the Plan 01 reducer and prove only the matching namespace's canonical shadow/cursor are replaced while immutable local intents replay unchanged; issuer/origin, server, subject, or generation mismatch quarantines instead of enumerating or replaying.</action>
  <acceptance_criteria>Empty, single-entity, multi-page, concurrent-write, Trash, tombstone, reset, and pending-local cases converge without omissions or duplicate semantic effects.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/sync/bootstrap_test.exs test/keepling/adapters/postgres/sync_feed_test.exs'</automated>
    <fails_when>non-zero exit, a mid-bootstrap write is missing, a local intent changes, or Trash is encoded as a tombstone</fails_when>
  </verify>
  <done>Full bootstrap and feed catch-up form one gap-free recovery path.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Authenticated client → cursor/feed API | Cursor content and client namespace claims are untrusted. |
| Concurrent command transactions → account feed | Lock order must prevent gaps, duplicates, and cross-account writes. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-02-01 | Spoofing/Tampering | sync cursor | high | mitigate | HMAC plus independent account/server/epoch comparison. |
| T-KPL02-02-02 | Tampering | feed ordering | high | mitigate | locked account clock, compound constraints, two-backend tests. |
| T-KPL02-02-03 | Denial of service | bootstrap/feed reads | medium | mitigate | bounded page limits and explicit low-water reset. |
| T-KPL02-02-SC | Tampering | dependencies | low | accept | No package-manager install occurs in this plan. |
</threat_model>
<verification>Run both sync test directories and the Phase 1 idempotency/conflict regression command.</verification>
<success_criteria>SRV-04 and SRV-05 are proven at transaction, cursor, bootstrap, and concurrency boundaries.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-02-SUMMARY.md` when done.</output>
