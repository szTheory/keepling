---
phase: KPL-02-synchronization-and-replaceable-server
plan: 08
type: execute
wave: 8
depends_on: [02-06, 02-07]
files_modified:
  - infra/backup/pgbackrest.conf.template
  - infra/backup/schedule.yml
  - infra/backup/durable-state-manifest.yml
  - apps/server/lib/keepling/application/ops/restore.ex
  - apps/server/test/keepling/application/ops/restore_test.exs
  - tooling/verify-backup.sh
  - tooling/verify-restore.sh
  - packages/contracts/vectors/recovery.json
autonomous: true
requirements: [DATA-02, OPS-04, OPS-05]
estimate: {tokens: 38000, raw_tokens: 38000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Physical/WAL and portable logical backups meet the exact cadence/retention contract and remain encrypted off host with independent credentials."
    - "A backup becomes healthy only after an isolated empty-target restore rotates the sync epoch and proves schema, history invariants, recovery/login, read, write, and undo."
    - "DATA-02 idempotency/concurrency edge: rerunning a completed verification is read-only/idempotent; parallel restore attempts cannot target the same destination."
  artifacts:
    - {path: infra/backup/pgbackrest.conf.template, provides: "Pinned encrypted physical/WAL policy"}
    - {path: tooling/verify-restore.sh, provides: "Logical/latest-WAL/historical PITR semantic verifier"}
    - {path: packages/contracts/vectors/recovery.json, provides: "Fixed restore/epoch/refusal recovery cases"}
  key_links:
    - {from: tooling/verify-restore.sh, to: apps/server/lib/keepling/application/ops/restore.ex, via: "closed isolated restore state machine"}
  prohibitions:
    - {statement: "MUST NOT call a backup healthy from archive creation/check output without disposable application-level restore proof."}
    - {statement: "MUST NOT restore into a live/nonempty/ambiguous target, expose side effects before proof, or reuse the pre-restore synchronization epoch."}
---
<objective>Deliver backup, PITR, isolated restore, and recovery-health semantics per D-34, D-35, D-36, D-37, D-38, D-39, and D-46.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md
@apps/server/lib/keepling/application/ops.ex
@tooling/test-phase-1.sh</context>
## Artifacts this phase produces
- pgBackRest/logical policy templates and durable-state manifest.
- `Keepling.Application.Ops.Restore` and backup/restore verifiers for newest logical, latest WAL, and randomized historical PITR.
<tasks>
<task type="auto">
  <name>Task 1: Package the exact backup, WAL, encryption, mirror, and retention policy</name>
  <reversibility rating="costly">D-35/D-36 define the recovery window and operational storage contract.</reversibility>
  <files>infra/backup/pgbackrest.conf.template, infra/backup/schedule.yml, infra/backup/durable-state-manifest.yml, tooling/verify-backup.sh</files>
  <read_first>.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md, infra/compose/compose.yml, apps/server/config/runtime.exs, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md</read_first>
  <action>Pin pgBackRest 2.59.1 and PostgreSQL 18.6 compatibility; configure client-side-encrypted Backblaze B2 as primary and AWS S3 with Object Lock in an independent account as mirror; encode one-minute archive boundary, weekly full/daily differential, 14-day physical/WAL, daily logical 30-day, weekly logical 12-week retention, versioning/object lock, and daily mirror. Keep backup/restore credentials outside app container and separate. Inventory PostgreSQL volume, both object repositories, OpenTofu state, DNS inputs, keys, and root-owned secret manifest as every durable location.</action>
  <acceptance_criteria>Config validation proves the exact cadence/retention/encryption/mirror policy and no recovery credential or irreplaceable state is inside the app image/container.</acceptance_criteria>
  <verify><automated>./tooling/verify-backup.sh --fixture local</automated><fails_when>non-zero exit, a cadence/retention differs, encryption/object-lock/mirror is absent, or app receives recovery credentials</fails_when></verify>
  <done>The recovery-material policy is pinned, source-owned, and complete.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Refuse unsafe restores and finalize a fresh epoch before readiness</name>
  <files>apps/server/lib/keepling/application/ops/restore.ex, apps/server/test/keepling/application/ops/restore_test.exs, tooling/verify-restore.sh, packages/contracts/vectors/recovery.json</files>
  <read_first>apps/server/lib/keepling/application/ops.ex, apps/server/lib/keepling/adapters/postgres/ops_store.ex, apps/server/lib/keepling/application/sync.ex, tooling/keepling-ops</read_first>
  <behavior>
    - D-46 refuses live/nonempty destinations, ambiguous source/target, missing WAL, unverified selection, mutable image, and expired compatibility unless an explicit recorded recovery override applies.
    - D-12/D-46 rotate a fresh random epoch transactionally before readiness and block production side effects until verification.
  </behavior>
  <action>Implement a closed restore state machine and lease/target guard inside the inward operations layer, with fixed vectors for every refusal, failure, epoch, and verified outcome. Persist source digest, recovery target, verifier version, schema/protocol/image facts, timestamps, epoch-finalized state, and closed outcome code without task content. Make repeated completed verification idempotent and competing same-target runs mutually exclusive.</action>
  <acceptance_criteria>Every unsafe case fails before mutation; a successful isolated restore has a new epoch and cannot become ready until its proof is finalized.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/ops/restore_test.exs'</automated><fails_when>non-zero exit, unsafe target mutates, concurrent same-target restores proceed, or readiness precedes epoch finalization</fails_when></verify>
  <done>Restore safety and epoch rotation are application-owned and transactionally proven.</done>
</task>
<task type="auto">
  <name>Task 3: Prove newest logical, latest WAL, and historical PITR at user level</name>
  <files>tooling/verify-restore.sh, tooling/verify-backup.sh, infra/backup/schedule.yml</files>
  <read_first>tooling/verify-deploy.sh, tooling/keepling-ops, infra/backup/schedule.yml, packages/contracts/vectors/recovery.json</read_first>
  <action>Build isolated disposable targets and fixtures for daily newest logical/latest recoverable WAL and weekly seeded randomized historical PITR. Verify repository manifest/checksum, schema, privacy-safe task/history counts/digests, operator recovery/login, read, safe write, and undo; persist measured recovery point/duration and restore proof. Make the verifier fail on a missing WAL segment, wrong key, corrupt manifest, stale epoch, or omitted semantic smoke.</action>
  <acceptance_criteria>All three restore classes pass from clean targets, known corrupt cases fail, RPO evidence is at most five minutes, and measured full-host workflow remains within the four-hour objective or fails acceptance honestly.</acceptance_criteria>
  <verify><automated>./tooling/verify-restore.sh --fixture newest-logical &amp;&amp; ./tooling/verify-restore.sh --fixture latest-wal &amp;&amp; ./tooling/verify-restore.sh --fixture historical-pitr</automated><fails_when>non-zero exit, semantic smoke is incomplete, corrupt fixtures pass, epoch is reused, or measured objective is omitted</fails_when></verify>
  <done>DATA-02 is backed by executable logical and PITR recovery evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Encrypted repositories → restore target | Backup bytes, selection, keys, and WAL completeness are untrusted until verified. |
| Restore target → production exposure | Candidate must remain isolated until epoch and semantic proof complete. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-08-01 | Tampering | restore selection/target | high | mitigate | explicit immutable source/empty target and refusal state machine. |
| T-KPL02-08-02 | Information disclosure | backup repositories | high | mitigate | client-side encryption and separate least-privilege credentials. |
| T-KPL02-08-03 | Denial of service | WAL/archive backlog | high | mitigate | lag/free-space status and deploy-preflight block without readiness coupling. |
| T-KPL02-08-SC | Tampering | pgBackRest package | high | mitigate | research-approved exact 2.59.1 packaged-test pin. |
</threat_model>
<verification>Run backup validation and all three disposable restore lanes.</verification>
<success_criteria>DATA-02 and recovery portions of OPS-04/OPS-05 have fresh executable evidence.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-08-SUMMARY.md` when done.</output>
