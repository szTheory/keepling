---
phase: KPL-02-synchronization-and-replaceable-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/mix.exs
  - apps/server/mix.lock
  - apps/server/lib/keepling/application/sync/reference_model.ex
  - apps/server/test/keepling/application/sync/reference_model_test.exs
  - apps/server/test/support/sync_scenario.ex
  - packages/contracts/schemas/sync-state-machine.schema.json
  - packages/contracts/vectors/sync.json
  - tooling/check-contracts.mjs
autonomous: true
requirements: [SRV-04, SRV-05]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "A deterministic reference client accepts one immutable local mutation, pulls while it is pending, and settles it only from an exact identity-and-fingerprint acknowledgement."
    - "Overlapping resource-key lanes preserve FIFO while disjoint lanes progress; failed dependencies block descendants without stopping unrelated work."
    - "FLAGGED ASSUMPTION (SRV-04/SRV-05 unclassified): the closed sync vectors are the authoritative Phase 2 interpretation and require verifier review against the requirement prose."
  artifacts:
    - {path: packages/contracts/schemas/sync-state-machine.schema.json, provides: "Storage-neutral state-machine contract"}
    - {path: packages/contracts/vectors/sync.json, provides: "Fixed-clock adversarial sync cases"}
    - {path: apps/server/lib/keepling/application/sync/reference_model.ex, provides: "Persistence-neutral reference reducer"}
  key_links:
    - {from: packages/contracts/vectors/sync.json, to: apps/server/lib/keepling/application/sync/reference_model.ex, via: "schema-validated vector runner"}
  prohibitions:
    - {statement: "MUST NOT report local success before projection, immutable command, fingerprint, journal, dependencies, and outbox are one durable transaction."}
    - {statement: "MUST NOT rewrite an immutable command or mint a replacement identity during retry, bootstrap, conflict, or authentication recovery."}
---

<objective>
Prove the production reference synchronization semantics end to end before server-feed expansion, implementing D-01, D-02, D-03, D-04, D-05, and D-06.

Purpose: Give later Electron and Swift stores one executable, storage-neutral truth for durable local acceptance and reconciliation.
Output: A schema-checked reference reducer, adversarial vectors, and deterministic property/scenario support.
</objective>

<execution_context>
@/Users/jon/.claude/gsd-core/workflows/execute-plan.md
@/Users/jon/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-PATTERNS.md
@packages/contracts/vectors/conflicts.json
@apps/server/test/support/concurrency_case.ex
</context>

## Phase Goal

Keepling has a formally tested offline synchronization contract and an always-on, recoverable reference deployment.

## Artifacts this phase produces

- `Keepling.Application.Sync.ReferenceModel` with `local_accept/2`, `pull/2`, `ready_pushes/1`, `acknowledge/2`, `fence/2`, and `bootstrap/2`.
- `packages/contracts/schemas/sync-state-machine.schema.json`, `packages/contracts/vectors/sync.json`, and `Keepling.SyncScenario`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Walk one durable mutation through local save, pending pull, push, and exact acknowledgement</name>
  <reversibility rating="one-way">D-03 fixes the durable local-success boundary inherited by every offline client; the user already locked it, so this records rather than reopens the choice.</reversibility>
  <files>apps/server/lib/keepling/application/sync/reference_model.ex, apps/server/test/keepling/application/sync/reference_model_test.exs, apps/server/test/support/sync_scenario.ex, packages/contracts/schemas/sync-state-machine.schema.json, packages/contracts/vectors/sync.json</files>
  <read_first>.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md, packages/contracts/vectors/conflicts.json, apps/server/lib/keepling/application/task_views.ex, apps/server/test/support/concurrency_case.ex</read_first>
  <behavior>
    - D-03 local acceptance atomically records optimistic projection, exact serialized bytes/fingerprint, journal, dependencies, and outbox before returning `local_saved`.
    - D-04 pull advances canonical shadow while a mutation is pending, then deterministically replays the unchanged intent into visible state.
    - D-06 only an acknowledgement matching mutation identity and fingerprint removes the outbox row and journals its terminal result.
  </behavior>
  <action>Write the failing fixed-clock tracer case first, then implement the pure map-based reference reducer through the single local-save → pending pull → ready push → accepted acknowledgement path. Keep canonical shadow, optimistic projection, journal, dependency graph, and outbox as distinct values. Reject mismatched acknowledgements without changing coverage or intent. The module remains independent of Ecto, Phoenix, generated DTOs, SQLite, and Swift representations.</action>
  <acceptance_criteria>The tracer ends with the acknowledged canonical snapshot, an empty outbox, a retained terminal journal row, and the pulled cursor unchanged by the acknowledgement.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/sync/reference_model_test.exs --only tracer'</automated>
    <fails_when>non-zero exit, a mismatched acknowledgement removes intent, or local_saved is emitted from partial state</fails_when>
  </verify>
  <done>One production-quality reference path proves durable local acceptance and exact settlement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand FIFO lanes, durable dependencies, relaunch, and authentication fencing</name>
  <reversibility rating="costly">D-01 and D-02 become cross-runtime durable queue semantics and vector compatibility inputs.</reversibility>
  <files>apps/server/lib/keepling/application/sync/reference_model.ex, apps/server/test/keepling/application/sync/reference_model_test.exs, apps/server/test/support/sync_scenario.ex, packages/contracts/vectors/sync.json</files>
  <read_first>apps/server/lib/keepling/application/sync/reference_model.ex, apps/server/test/keepling/application/sync/reference_model_test.exs, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md</read_first>
  <behavior>
    - D-01 sorted overlapping resource keys exclude one another and preserve FIFO; disjoint keys remain ready.
    - D-02 only `accepted` and `already_satisfied` satisfy a dependency; conflict/rejection block descendants and cycles/orphans become explicit invalid state.
    - D-05 relaunch starts from durable state, performs a bounded pull, interleaves bounded pulls/pushes, and retains exact state through authentication loss.
  </behavior>
  <action>Add targeted scenarios for task-only lanes, Today/order multi-scope exclusion, organization prerequisites, capture-before-edit, blocked descendants, cycle/orphan rejection, response loss before and after commit, reconnect, relaunch, and authentication pause. Implement canonical bytewise resource-key ordering and bounded scheduling without an account-wide stop or semantic DAG optimizer.</action>
  <acceptance_criteria>Every ready-set and terminal transition is deterministic from durable input, and a conflict in one task lane does not prevent an unrelated task lane from progressing.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/sync/reference_model_test.exs'</automated>
    <fails_when>non-zero exit, nondeterministic ready sets, a failed dependency runs, or unrelated lanes stop</fails_when>
  </verify>
  <done>The reference reducer covers lane, dependency, reconnect, relaunch, and authentication semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the vector corpus non-vacuous and property checked</name>
  <files>apps/server/mix.exs, apps/server/mix.lock, packages/contracts/schemas/sync-state-machine.schema.json, packages/contracts/vectors/sync.json, tooling/check-contracts.mjs</files>
  <read_first>apps/server/mix.exs, apps/server/mix.lock, tooling/check-contracts.mjs, packages/contracts/vectors/conflicts.json, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md</read_first>
  <behavior>
    - Unknown actions/outcomes, duplicate case names, missing fixed identities/clocks, and zero executed cases fail the contract gate.
    - StreamData reproduces and shrinks long action sequences while invariant checks preserve identity, FIFO, dependencies, and monotonic canonical revisions.
  </behavior>
  <action>Pin research-approved test-only `stream_data` 1.4.0, extend the contract checker to validate the JSON Schema and report the executed vector count, and add seeded property sequences covering D-01 through D-06. Do not share persistence records or introduce a runtime dependency.</action>
  <acceptance_criteria>The schema gate rejects a known malformed fixture, executes every named case, and the seeded property suite passes with a reproducible seed.</acceptance_criteria>
  <verify>
    <automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix deps.get --check-locked &amp;&amp; mix deps.unlock --check-unused &amp;&amp; mix compile --warnings-as-errors' &amp;&amp; pnpm contracts:check &amp;&amp; ./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/sync/reference_model_test.exs'</automated>
    <fails_when>non-zero exit, lock drift, zero cases execute, or a malformed/unknown vector is accepted</fails_when>
  </verify>
  <done>Cross-runtime sync semantics have a pinned, shrinking, anti-vacuous proof harness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Serialized local command → reducer | Untrusted durable bytes must retain identity and closed shape. |
| Network acknowledgement → durable namespace | A response cannot settle a different mutation or account. |

## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-01-01 | Tampering | acknowledgement reducer | high | mitigate | Exact identity/fingerprint match and transaction-shaped tests. |
| T-KPL02-01-02 | Denial of service | lane scheduler | medium | mitigate | Bounded pulls/pushes and cycle/orphan rejection. |
| T-KPL02-01-03 | Information disclosure | vectors | medium | mitigate | Fixed synthetic identities/content and redaction assertions. |
| T-KPL02-01-SC | Tampering | Hex dependency | high | mitigate | Research-approved exact StreamData pin and committed lockfile. |
</threat_model>

<verification>Run the three focused commands and confirm all schema cases execute.</verification>
<success_criteria>The reference model makes every D-01..D-06 transition deterministic and storage-neutral.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-01-SUMMARY.md` when done.</output>
