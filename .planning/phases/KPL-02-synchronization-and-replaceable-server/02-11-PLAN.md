---
phase: KPL-02-synchronization-and-replaceable-server
plan: 11
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - apps/server/lib/keepling_web/auth.ex
  - apps/server/lib/keepling_web/controllers/device_grant_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/test/keepling_web/device_grant_controller_test.exs
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/generated/keepling.ts
autonomous: true
requirements: [SRV-04, QUAL-05]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "A native public client obtains an installation authorization code through an external user agent, exchanges it with exact redirect/state/S256 PKCE, and uses opaque short-lived bearer access without a client secret, password, browser cookie, or deployment credential."
    - "Refresh rotates one hash-stored lineage atomically; replay fences the grant family, and explicit revocation is separately visible and prevents future bearer access."
    - "Every authenticated sync request receives issuer, origin, stable server instance, account subject, and synchronization generation exclusively from the server-authenticated DeviceGrant."
  artifacts:
    - {path: apps/server/lib/keepling_web/controllers/device_grant_controller.ex, provides: "Thin authorization-code, token, refresh, grant-list, and revocation HTTP adapter"}
    - {path: apps/server/lib/keepling_web/auth.ex, provides: "Bearer access-token authentication and server-derived DeviceGrant namespace assignment"}
    - {path: apps/server/test/keepling_web/device_grant_controller_test.exs, provides: "ConnCase proof of the complete native grant transport lifecycle"}
  key_links:
    - {from: apps/server/lib/keepling_web/controllers/device_grant_controller.ex, to: apps/server/lib/keepling/accounts/device_grant.ex, via: "thin calls to authorization-code issuance/exchange, refresh rotation, listing, and revocation application functions"}
    - {from: apps/server/lib/keepling_web/auth.ex, to: apps/server/lib/keepling/accounts/device_grant.ex, via: "hash lookup of opaque Bearer access credential and server-derived namespace assignment"}
    - {from: apps/server/lib/keepling_web/auth.ex, to: apps/server/lib/keepling_web/controllers/sync_controller.ex, via: "device_grant_authenticated pipeline consumed by Plan 02-05"}
  prohibitions:
    - {statement: "MUST NOT use a browser session cookie, password, shared client secret, deployment credential, or client-asserted namespace to authenticate native API or sync requests."}
    - {statement: "MUST NOT let Phoenix implement grant rotation, replay fencing, revocation, or namespace semantics; controllers and plugs remain transport adapters."}
---
<objective>Close the executable native installation-grant transport required by D-16 and D-22 so bearer-authenticated synchronization has a real issuer/origin/server-instance/subject/generation authority.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@docs/architecture/REPOSITORY.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md
@apps/server/lib/keepling/accounts/device_grant.ex
@apps/server/lib/keepling_web/auth.ex
@apps/server/lib/keepling_web/controllers/auth_controller.ex
@apps/server/lib/keepling_web/router.ex
@packages/contracts/openapi/keepling.yaml</context>

## Artifacts this phase produces
- Closed OpenAPI operations/schemas for native authorization-code issuance, token exchange/refresh, installation-grant listing, and revocation; thin Phoenix adapters; bearer authentication; and ConnCase lifecycle proof.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Obtain, exchange, refresh, inspect, and revoke one installation grant over HTTP</name>
  <files>apps/server/lib/keepling_web/controllers/device_grant_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/device_grant_controller_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts</files>
  <read_first>apps/server/lib/keepling/accounts/device_grant.ex, apps/server/lib/keepling_web/controllers/auth_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/auth_test.exs, packages/contracts/openapi/keepling.yaml</read_first>
  <behavior>
    - D-22 `GET /oauth/authorize` accepts only the closed authorization-code request (`response_type=code`, installation `client_id`, exact registered `redirect_uri`, unpredictable `state`, `code_challenge`, `code_challenge_method=S256`) in an external user agent and redirects with the exact state plus a one-use short-lived code after user authorization.
    - `POST /oauth/token` has closed `authorization_code` and `refresh_token` request schemas; code exchange requires exact redirect and verifier, returns an opaque 15-minute Bearer access credential plus rotating refresh credential, and accepts no client secret.
    - Refresh rotation returns a new opaque pair, invalidates the predecessor, and replay produces the stable fenced-family response; `GET /api/v1/device-grants` and `DELETE /api/v1/device-grants/{installation_id}` expose separately visible/revocable installations and stable idempotent revocation.
  </behavior>
  <action>Define the exact OpenAPI operation IDs `authorizeNativeInstallation`, `exchangeNativeAuthorizationCode`, `refreshNativeGrant`, `listDeviceGrants`, and `revokeDeviceGrant` with closed request/response and Problem Details schemas. Implement one thin `KeeplingWeb.DeviceGrantController` that validates transport shape, delegates all issuance/exchange/rotation/list/revocation semantics to `Keepling.Accounts.DeviceGrant`, renders no raw stored token/code, and never accepts a shared secret. Add ConnCase tests for exact redirect/state preservation, S256 success and verifier/redirect/state failure, one-use code behavior, opaque short-lived access issuance, refresh rotation, predecessor replay fencing, separate installation visibility, idempotent exact-family revocation, and post-revocation denial. The external user agent may use the existing browser login only to authorize the code; token exchange, refresh, revocation, and native API access use grant credentials rather than a browser cookie.</action>
  <acceptance_criteria>A public native client completes the full D-22 grant lifecycle over the closed HTTP contract, while invalid redirect/state/PKCE, replayed codes/refresh credentials, and revoked grants fail with stable non-secret problems.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/device_grant_controller_test.exs --only transport' &amp;&amp; pnpm contracts:check</automated><fails_when>non-zero exit, an operation/schema/route is absent, redirect/state/S256 is bypassed, a client secret/cookie authenticates token or API access, refresh replay remains usable, revocation is not separately visible, or raw credentials persist/appear in diagnostics</fails_when></verify>
  <done>The authorization-code, exchange, refresh, visibility, and revocation transport is closed, executable, and application-owned semantically.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Authenticate Bearer access and derive the sync namespace at the server boundary</name>
  <files>apps/server/lib/keepling_web/auth.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/device_grant_controller_test.exs</files>
  <read_first>apps/server/lib/keepling_web/auth.ex, apps/server/lib/keepling_web/router.ex, apps/server/lib/keepling/accounts/device_grant.ex, apps/server/test/keepling_web/auth_test.exs</read_first>
  <behavior>
    - D-16 a `device_grant_authenticated` pipeline accepts exactly one opaque `Authorization: Bearer` credential, delegates hash lookup/expiry/revocation to the account application, and assigns authenticated grant identity plus issuer, origin, stable server instance, account subject, and synchronization generation from server state.
    - Missing, malformed, expired, replay-fenced, or revoked access receives a stable 401 problem and never reaches the protected controller; browser-session cookies alone cannot enter this pipeline.
    - Refresh rotation preserves the installation namespace until replay/revocation increments generation; the old access credential and every cursor/request bound to the fenced generation fail.
  </behavior>
  <action>Add a distinct bearer-auth pipeline in `KeeplingWeb.Auth` and the router, leaving the existing browser-session/CSRF pipeline intact. Parse only the standard Bearer header, call the inward grant-authentication function, and place a closed `device_grant_namespace` assignment on the connection for Plan 02-05; never derive any tuple field from request headers, query parameters, URL labels, account UUID claims, or OpenAPI DTOs. Exercise a bearer-protected grant endpoint in ConnCase to prove successful server-derived assignment and every failure boundary, including a cookie-only request, expired access, refresh replay, family revocation, issuer/origin/server/account mismatch attempts, and generation fencing.</action>
  <acceptance_criteria>Bearer-authenticated requests expose the exact five-field server namespace to downstream sync transport, and no cookie/client assertion can impersonate or cross namespaces.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/device_grant_controller_test.exs'</automated><fails_when>non-zero exit, malformed/expired/revoked access reaches a controller, cookie-only access succeeds, any namespace field is client-derived, or replay fencing leaves an old access/cursor generation authorized</fails_when></verify>
  <done>Plan 02-05 can attach sync routes to a proven bearer boundary and consume only server-derived DeviceGrant authorization context.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| External user agent/native client → grant HTTP adapter | Redirect, state, PKCE, code, refresh, and bearer inputs are untrusted. |
| Bearer credential → sync namespace | Only server grant state may establish account/server/generation authority. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-11-01 | Spoofing | authorization-code exchange | high | mitigate | exact redirect/state/S256, one-use code, no public-client secret. |
| T-KPL02-11-02 | Spoofing | bearer authentication | high | mitigate | opaque hash lookup, expiry/revocation checks, server-derived namespace. |
| T-KPL02-11-03 | Tampering | refresh lineage | high | mitigate | atomic rotation, predecessor replay fencing, generation increment. |
| T-KPL02-11-04 | Information disclosure | token errors/diagnostics | high | mitigate | closed Problem Details and no raw code/access/refresh values. |
| T-KPL02-11-SC | Tampering | dependencies | low | accept | No package install occurs. |
</threat_model>
<verification>Run the native grant ConnCase suite and contract drift gate, then confirm Plan 02-05 depends on this transport plan.</verification>
<success_criteria>D-16/D-22 have a complete HTTP acquisition and bearer-use path, and sync routes consume a server-authenticated installation namespace.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-11-SUMMARY.md` when done.</output>
