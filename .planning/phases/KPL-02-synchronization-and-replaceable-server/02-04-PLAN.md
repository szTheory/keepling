---
phase: KPL-02-synchronization-and-replaceable-server
plan: 04
type: execute
wave: 4
depends_on: [02-02, 02-03, 02-11]
files_modified:
  - apps/server/lib/keepling/application/compatibility.ex
  - apps/server/lib/keepling_web/controllers/compatibility_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/test/keepling/application/compatibility_test.exs
  - apps/server/test/keepling_web/compatibility_controller_test.exs
  - packages/contracts/openapi/keepling.yaml
  - packages/contracts/generated/keepling.ts
  - packages/contracts/vectors/compatibility.json
  - tooling/test-compatibility.sh
autonomous: true
requirements: [SRV-06]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The server authoritatively selects the highest intersecting integer protocol train and returns stable client/server-upgrade recovery on non-overlap."
    - "Current and oldest-supported codecs, receipts, cursors, generated fixtures, migrations, and image/schema pairs remain executable."
    - "FLAGGED ASSUMPTION (SRV-06 unclassified): current-dogfood-only support is valid until the first distributed build; the compatibility gate must be updated at that release boundary."
  artifacts:
    - {path: apps/server/lib/keepling/application/compatibility.ex, provides: "Protocol train policy"}
    - {path: tooling/test-compatibility.sh, provides: "Current/previous skew matrix"}
  key_links:
    - {from: apps/server/lib/keepling/application/compatibility.ex, to: packages/contracts/vectors/compatibility.json, via: "frozen train fixtures"}
  prohibitions:
    - {statement: "MUST NOT negotiate authorization, domain invariants, conflict meaning, or arbitrary feature flags."}
    - {statement: "MUST NOT shorten an advertised distributed-client support window except through a documented security emergency with preserved local intent."}
---
<objective>Ship compatible protocol evolution per D-25, D-26, D-27, D-28, D-29, D-30, D-31, D-32, and D-33.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md
@packages/contracts/openapi/keepling.yaml
@tooling/check-contracts.mjs
@tooling/test-phase-1.sh</context>
## Artifacts this phase produces
- `Keepling.Application.Compatibility.negotiate/2` and `metadata/1`, public `CompatibilityController`, frozen `compatibility.json`, and `tooling/test-compatibility.sh`.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Freeze protocol-train negotiation and stable upgrade outcomes</name>
  <reversibility rating="one-way">D-25 is an already-locked published-support promise; this task records its release-boundary implementation without reopening it.</reversibility>
  <files>apps/server/lib/keepling/application/compatibility.ex, apps/server/test/keepling/application/compatibility_test.exs, packages/contracts/vectors/compatibility.json</files>
  <read_first>.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md, packages/contracts/openapi/keepling.yaml, apps/server/lib/keepling/application/task_views.ex</read_first>
  <behavior>
    - D-25 declares unstable/current dogfood train until first distribution, then current plus immediately previous for at least 90 days.
    - D-26 selects the highest range intersection; D-28 maps non-overlap to non-retrying `client_upgrade_required` or `server_upgrade_required` while intent remains local.
  </behavior>
  <action>Implement a pure policy module with injected release time/config and frozen vector cases for exact overlap boundaries, deprecated-but-safe, older/newer mismatch, deadline edges, and emergency override evidence. Keep market/build versions metadata-only.</action>
  <acceptance_criteria>Every range pair yields one stable selected train/state/recovery code and no non-overlap retries automatically.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/compatibility_test.exs'</automated><fails_when>non-zero exit, highest intersection is not selected, or unsupported clients receive a retrying result</fails_when></verify>
  <done>Protocol-train semantics are deterministic and frozen.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Expose authoritative unversioned compatibility metadata</name>
  <files>apps/server/lib/keepling_web/controllers/compatibility_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/compatibility_controller_test.exs, packages/contracts/openapi/keepling.yaml, packages/contracts/generated/keepling.ts</files>
  <read_first>apps/server/lib/keepling_web/router.ex, apps/server/lib/keepling_web/controllers/task_view_controller.ex, apps/server/lib/keepling/application/compatibility.ex, packages/contracts/openapi/keepling.yaml</read_first>
  <behavior>
    - D-27 response includes server release, tested OCI digest, read/write/sync ranges, schema range, platform minimum builds, deadline, update location, and stable recovery codes.
    - D-29 keeps `/api/v1` and command version 1 closed semantics stable; incompatible shape/meaning changes use a new representation or downgrade adapter.
    - ConnCase proves the unversioned route is publicly reachable without a session, rejects malformed min/max claims, returns every D-27 field and stable recovery code, and excludes account, operator, credential, grant, cursor, and other private state.
  </behavior>
  <action>Add a public unversioned read-only route whose thin controller accepts closed min/max claims and calls the application policy. Add `KeeplingWeb.CompatibilityControllerTest` as a ConnCase transport suite for anonymous reachability, closed malformed-claim failures, the complete D-27 response, stable mismatch recovery codes, and the public/private-state boundary. Extend OpenAPI/generated TypeScript without making generated DTOs application/domain types. Reject missing tested digest or malformed ranges at configuration startup.</action>
  <acceptance_criteria>The endpoint is public but contains no account/operator data, generated drift is clean, and all required authoritative fields are present.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/compatibility_test.exs test/keepling_web/compatibility_controller_test.exs' &amp;&amp; pnpm contracts:check</automated><fails_when>non-zero exit, the route is absent/session-gated, malformed claims pass, generated drift exists, private state appears, a stable recovery code drifts, or any D-27 field is missing</fails_when></verify>
  <done>Clients can determine safe compatibility without coordinating an upgrade.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Execute current/previous codec, migration, and rollback compatibility</name>
  <files>tooling/test-compatibility.sh, packages/contracts/vectors/compatibility.json, apps/server/test/keepling/application/compatibility_test.exs</files>
  <read_first>tooling/test-phase-1.sh, tooling/runtime-preflight.sh, packages/contracts/vectors/compatibility.json, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-RESEARCH.md</read_first>
  <behavior>
    - D-30 retains supported receipt/cursor/result decoders and frozen generated fixtures.
    - D-31/D-32 prove expand→compatible code→backfill/validate→switch→age-out→contract using old-client/new-server, new-client/old-server, old-image/new-schema, and rollback-image/schema pairs.
    - D-33 preflight rejects exclusion of recently active supported devices absent an explicit documented break/emergency path.
  </behavior>
  <action>Create a repository-root matrix script using disposable databases and checked-in current/previous simulators/fixtures. Assert every lane executes a nonzero case count, records exact image/schema/train inputs, and rejects a known incompatible pair. Avoid a Cartesian platform matrix.</action>
  <acceptance_criteria>The supported skew matrix passes, its known-bad pair fails with the expected stable code, and rollback eligibility is schema/protocol checked.</acceptance_criteria>
  <verify><automated>pnpm contracts:check &amp;&amp; ./tooling/test-compatibility.sh</automated><fails_when>non-zero exit, a matrix lane executes zero cases, or the known incompatible pair is accepted</fails_when></verify>
  <done>SRV-06 has executable current/oldest-supported and rollback evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Client range claims → server policy | Claims are inputs, never authority. |
| Tested image/schema metadata → deploy preflight | Mutable or incompatible artifacts must be rejected. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-04-01 | Tampering | compatibility claims | high | mitigate | server-owned ranges and highest-intersection policy. |
| T-KPL02-04-02 | Tampering | rollback artifact | high | mitigate | exact digest plus schema/protocol range check. |
| T-KPL02-04-03 | Information disclosure | public metadata | low | accept | Response is closed and contains release compatibility only. |
| T-KPL02-04-SC | Tampering | dependencies | low | accept | No package install occurs. |
</threat_model>
<verification>Run `pnpm contracts:check && ./tooling/test-compatibility.sh`.</verification>
<success_criteria>SRV-06 remains safe across current/previous clients, schemas, codecs, and images.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-04-SUMMARY.md` when done.</output>
