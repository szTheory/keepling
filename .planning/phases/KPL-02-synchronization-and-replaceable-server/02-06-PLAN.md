---
phase: KPL-02-synchronization-and-replaceable-server
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - apps/server/priv/repo/migrations/20260901000300_add_ops_state.exs
  - apps/server/lib/keepling/application/ops.ex
  - apps/server/lib/keepling/adapters/postgres/ops_store.ex
  - apps/server/lib/keepling_web/controllers/health_controller.ex
  - apps/server/lib/keepling_web/router.ex
  - apps/server/lib/keepling/release.ex
  - apps/server/lib/mix/tasks/keepling.ops.ex
  - apps/server/test/keepling/application/ops/status_test.exs
  - apps/server/test/keepling_web/health_test.exs
  - tooling/keepling-ops
  - tooling/test-ops-cli.sh
autonomous: true
requirements: [OPS-04, OPS-05, QUAL-05]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "One stable ops vocabulary returns equivalent human/JSON closed results, exit codes, and copyable remediation from source and releases."
    - "Liveness is process-only; readiness proves bounded PostgreSQL, schema/protocol range, migrations, restore epoch, and willingness to accept traffic."
    - "Backup lag degrades operator status and deploy preflight without making an otherwise-correct server unready."
    - "FLAGGED ASSUMPTION (OPS-04/OPS-05 unclassified): exact exit-code numbers are planner-discretion and must be frozen in CLI tests before release use."
  artifacts:
    - {path: apps/server/lib/keepling/application/ops.ex, provides: "Inward operator semantics"}
    - {path: tooling/keepling-ops, provides: "Stable source/release wrapper"}
  key_links:
    - {from: tooling/keepling-ops, to: apps/server/lib/keepling/application/ops.ex, via: "thin release eval/Mix adapter"}
  prohibitions:
    - {statement: "MUST NOT put restore, deploy, compatibility, or recovery safety decisions in shell, Phoenix controllers, or Mix-only code."}
    - {statement: "MUST NOT make backup lag part of public serving readiness or expose operator status publicly."}
---
<objective>Deliver the stable operator surface and truthful health semantics locked by D-40, D-41, D-42, and D-43.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@AGENTS.md
@.planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md
@apps/server/lib/keepling/accounts/security_audit.ex
@apps/server/lib/mix/tasks/keepling.recover.ex
@apps/server/lib/keepling_web/router.ex</context>
## Artifacts this phase produces
- `Keepling.Application.Ops`, `Keepling.Adapters.Postgres.OpsStore`, `Keepling.Release`, `Mix.Tasks.Keepling.Ops`, health routes, `tooling/keepling-ops`, and `tooling/test-ops-cli.sh`.

## Scope rationale

This three-task vertical slice intentionally retains 11 files because every surface consumes the same closed `Keepling.Application.Ops` result/exit contract: Task 1 defines it, Task 2 proves its HTTP health projection, and Task 3 proves equivalent source/release CLI projection. Splitting would duplicate or sequence edits to `ops.ex` and `router.ex` without creating an independently usable capability; the 36,000-token estimate remains inside the configured smart zone, with low confidence explicitly inherited from zero calibration samples.
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define closed inward preflight, status, doctor, and recovery results</name>
  <files>apps/server/priv/repo/migrations/20260901000300_add_ops_state.exs, apps/server/lib/keepling/application/ops.ex, apps/server/lib/keepling/adapters/postgres/ops_store.ex, apps/server/test/keepling/application/ops/status_test.exs</files>
  <read_first>apps/server/lib/keepling/accounts/security_audit.ex, apps/server/lib/keepling/application/task_views.ex, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-CONTEXT.md</read_first>
  <behavior>
    - D-40 all nine verbs have closed success/refusal/failure codes, bounded facts, remediation, human and JSON rendering inputs.
    - D-41 application modules own target/source checks, compatibility, backup selection, restore verification, and deployment preflight policy.
    - D-43 status exposes release/digest/ranges/schema/epoch/backup/WAL/last restore verification with no raw identifiers or domain content.
  </behavior>
  <action>Add an additive singleton operations-state/restore-proof schema and an inward port/service with explicit structs/maps for `preflight|status|doctor|backup|restore|restore-verify|deploy|upgrade|replace-host`. Choose and freeze exact nonzero exit codes by failure class. The PostgreSQL adapter persists only bounded proof metadata and digests, never representative task values.</action>
  <acceptance_criteria>Every verb and refusal case yields stable JSON-compatible data and exit class; status is complete and privacy bounded.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling/application/ops/status_test.exs'</automated><fails_when>non-zero exit, an unknown result renders, required status facts are absent, or raw content/IDs enter output</fails_when></verify>
  <done>Operational correctness is owned by testable inward modules and ports.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Separate public liveness/readiness from operator status</name>
  <files>apps/server/lib/keepling_web/controllers/health_controller.ex, apps/server/lib/keepling_web/router.ex, apps/server/test/keepling_web/health_test.exs, apps/server/lib/keepling/application/ops.ex</files>
  <read_first>apps/server/lib/keepling_web/router.ex, apps/server/lib/keepling_web/controllers/task_view_controller.ex, apps/server/lib/keepling/application/ops.ex, apps/server/config/runtime.exs</read_first>
  <behavior>
    - D-42 liveness needs no database; readiness uses a positive bounded timeout and validates DB/schema/protocol/migrations/restore epoch/traffic willingness.
    - Backup age/WAL lag affects authenticated status and deploy preflight only.
  </behavior>
  <action>Add public liveness/readiness routes with closed minimal responses and an operator-authorized status path. Test dependency unavailable, pending migration, incompatible schema/train, unfinalized restore, and healthy cases. Never expose backup metadata or operator remediation through public routes.</action>
  <acceptance_criteria>Each readiness dependency independently makes readiness fail, liveness remains responsive, and backup lag alone leaves readiness healthy.</acceptance_criteria>
  <verify><automated>./tooling/runtime-preflight.sh --exec -- sh -c 'cd apps/server &amp;&amp; mix test test/keepling_web/health_test.exs test/keepling/application/ops/status_test.exs'</automated><fails_when>non-zero exit, liveness queries PostgreSQL, backup lag fails readiness, or operator facts are public</fails_when></verify>
  <done>Public probes and operator health report distinct truthful boundaries.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Invoke the same operations API from source and packaged releases</name>
  <files>apps/server/lib/keepling/release.ex, apps/server/lib/mix/tasks/keepling.ops.ex, tooling/keepling-ops, tooling/test-ops-cli.sh</files>
  <read_first>apps/server/lib/mix/tasks/keepling.recover.ex, apps/server/lib/keepling/application/ops.ex, tooling/runtime-preflight.sh, .planning/phases/KPL-02-synchronization-and-replaceable-server/02-PATTERNS.md</read_first>
  <behavior>
    - Source Mix task and release eval wrapper render equivalent human/JSON results, honor `--no-color`, and use stable exit codes.
    - Release commands start only the minimum required applications and never boot production endpoint/side effects for migration/restore checks.
  </behavior>
  <action>Implement thin OptionParser/release wrappers and repository-root `tooling/keepling-ops`; parse/format/exit only. Add black-box tests for every verb, JSON schema, no-color, invalid args, missing config, refusal, and redacted stderr. Use `Application.ensure_loaded/1`/`Ecto.Migrator.with_repo/2` in release-safe paths.</action>
  <acceptance_criteria>All nine verbs behave identically from source and release entry points and invalid/destructive requests fail closed.</acceptance_criteria>
  <verify><automated>./tooling/test-ops-cli.sh</automated><fails_when>non-zero exit, source/release outputs differ semantically, a refusal exits zero, or secrets/private values appear</fails_when></verify>
  <done>Operators have one stable automation-safe vocabulary.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Operator CLI → destructive operations | Arguments and environment may identify the wrong target. |
| Health/status → public/operator callers | Private recovery facts require authorization. |
## STRIDE Threat Register — ASVS Level 1
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-KPL02-06-01 | Elevation of privilege | operator status/actions | high | mitigate | distinct operator authorization and closed verbs. |
| T-KPL02-06-02 | Tampering | restore/deploy arguments | high | mitigate | inward validated explicit source/target/refusal policy. |
| T-KPL02-06-03 | Information disclosure | doctor/CLI output | high | mitigate | allow-listed bounded facts and black-box redaction scans. |
| T-KPL02-06-SC | Tampering | dependencies | low | accept | No install occurs. |
</threat_model>
<verification>Run ops application, health, and CLI black-box suites.</verification>
<success_criteria>OPS-04/OPS-05 have stable, privacy-safe, release-capable operator evidence.</success_criteria>
<output>Create `.planning/phases/KPL-02-synchronization-and-replaceable-server/02-06-SUMMARY.md` when done.</output>
