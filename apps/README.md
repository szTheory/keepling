# Applications

Application directories are created as their vertical roadmap phase begins. Planned boundaries are `server`, `web`, `desktop`, `ios`, and deferred `android`; see `docs/architecture/REPOSITORY.md`.

