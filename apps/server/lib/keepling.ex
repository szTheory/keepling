defmodule Keepling do
  @moduledoc """
  Keepling keeps the contexts that define the domain and application rules.

  Transport and persistence adapters call inward-facing Keepling modules;
  domain and application modules never depend on those adapters.
  """
end
