import SwiftUI

/// Placeholder root scaffolded by Task 2 so the app target links; Task 3
/// replaces the body with the real capture-sheet-hosting screen. Plan 04-09
/// replaces this again with the two-tab TabView/NavigationStack shell
/// (D-25).
struct RootView: View {
    var body: some View {
        Text("Keepling")
    }
}
