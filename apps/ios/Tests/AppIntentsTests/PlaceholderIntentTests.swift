import XCTest

/// Scaffolded by Task 2 so the AppIntentsTests bundle links; App Intents
/// themselves are out of scope for this plan (D-35/D-37 land later).
final class PlaceholderIntentTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
