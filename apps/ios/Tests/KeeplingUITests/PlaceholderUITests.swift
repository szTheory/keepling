import XCTest

/// Task 2 scaffolding placeholder so the UI test bundle links before Task 3
/// writes the real capture-flow UI test.
final class PlaceholderUITests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
