# Tooling

This directory is reserved for deterministic generators, drift checks, evidence tooling, and root coordination that native build tools cannot cleanly own. Tooling must not become an internal platform.

