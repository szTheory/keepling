{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://keepling.dev/contracts/export/account-settings.schema.json",
  "title": "Keepling exported account settings record",
  "description": "Exactly one line of data/account-settings.ndjson -- the single account's user-facing settings. Internal projection/view revisions and setup credentials are never exported; see classification.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["timezone"],
  "properties": {
    "timezone": { "type": "string", "minLength": 1 }
  }
}
